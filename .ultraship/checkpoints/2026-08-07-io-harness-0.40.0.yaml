product: io-harness
version: 0.40.0
timestamp: '2026-08-07T00:00:00Z'
reason: capacity
repository_state: passing
completed_acceptance:
  - F1, F2, F3, F4, F5, F6 (Linux arm CI-only), F7
  - N1, N2 (met as byte-identical rather than as two added lines), N3, N4
remaining_acceptance:
  - N5 and N6 are written but their gate evidence is incomplete
  - O2 — the nightly docs.rs gate was killed mid-run and is UNVERIFIED, not passed
  - O3 — cargo package --locked has not been run
  - O1, O4, O5 — belong to the PR, the matrix and the merge, none of which has happened
active_hypotheses:
  - >
    The Linux mount population (T05) is unverified on this macOS host by construction.
    Whether the ubuntu runner's kernel permits the remounts decides between shipping the
    release whole and invoking the contract's fallback_scope, which drops exactly that task.
next_task: US-IO-HARNESS-0.40.0-T07
required_context:
  - .ultraship/products/io-harness/releases/0.40.0.yaml
  - .ultraship/products/io-harness/execution/tasks.yaml
  - src/sandbox/linux.rs
  - tests/exec_contained.rs
