cargo fmt --check           clean
cargo clippy --all-targets -- -D warnings   clean
cargo test --all-targets    721 passed, 0 failed
cargo test --doc            239 passed, 0 failed
cargo test --lib tools::shell   19 passed (parser, incl. two full-ASCII sweeps)
cargo test --test shell         11 passed (F1, F2, F3, and the trace clause)
cargo test --test list_dir      12 passed (F10)
cargo test --lib sandbox        20 passed (incl. both JobLimits mapping tests)

NOT verified locally, by construction:
  F6, F8, F9 have never executed. No Win32 path has run on any machine.
  cargo check --target x86_64-pc-windows-msvc cannot pass libsqlite3-sys's
  build script on macOS, so the sandbox module was mirrored into a scratch
  crate to type-check every Win32 signature, the feature set and the Send
  bound under that target. The first CI Windows run is their verification.
  NF1 (three-OS CI) and NF3 (nightly docs.rs) complete on the pull request.
