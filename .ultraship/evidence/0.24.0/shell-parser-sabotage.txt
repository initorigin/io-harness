baseline: GREEN  (test result: ok. 19 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.03s)

RED    pass glob metacharacters through into the argv instead of refusing them
       tests::no_input_containing_an_unpermitted_character_is_ever_accepted

RED    read a lone `&` as a command separator instead of refusing background execution
       tests::every_excluded_construct_is_refused_by_name

RED    let an unrecognised character be absorbed into a word instead of reaching the catch-all
       tests::every_ascii_character_outside_the_permitted_set_is_refused

RED    drop the reserved-word refusal, so `if`/`for`/`while` parse as programs
       tests::every_excluded_construct_is_refused_by_name

RED    permit control characters inside quotes
       tests::non_ascii_is_a_literal_and_control_characters_are_not

RED    treat a quoted digit as a descriptor prefix, so `echo "2"> f` redirects stderr
       tests::a_quoted_digit_is_an_argument_not_a_descriptor

RED    stop refusing a pipeline stage that has no command
       tests::every_excluded_construct_is_refused_by_name

RED    check only the first stage of a pipeline, leaving the rest unreported
       tests::the_reported_stage_count_matches_the_operators_in_the_line

reverted: GREEN

Every sabotage was caught by a named test; suite green after revert.
