=== scope.excluded, verified absent rather than assumed (0.18.0) ===

-- no cost is stored: no cost column anywhere in the schema --
1748-        // what did it cost" is one read rather than a reconstruction.
1847-        // No cost column, deliberately. Money is derived at query time from a

-- Containment::max_total_cost is still documented inert and enforced by nothing --
src/containment.rs:34:/// // and never money, so `max_total_cost` is inert and stays `None`.
src/containment.rs:36:/// assert_eq!(containment.max_total_cost, None);
src/containment.rs:68:    pub max_total_cost: Option<u64>,
src/containment.rs:95:            max_total_cost: None,
src/containment.rs:406:            max_total_cost: Some(500),
src/run.rs:2545:///     max_total_cost: None,      // reserved and inert; bound money in tokens
src/run.rs:3413:            // it silently ignored. Enforced in 0.12.0. Its sibling `max_total_cost`

-- no configuration parser was added (0.19.0 owns config) --
no config/TOML dependency: OK

-- default dependency tree unchanged --
cargo tree | wc -l = 401

-- CHECKPOINT_FORMAT unchanged --
106:pub const CHECKPOINT_FORMAT: i64 = 7;

-- the store change is additive: no ALTER TABLE or new CREATE TABLE outside the two --
+            "CREATE TABLE IF NOT EXISTS provider_calls (
+             CREATE TABLE IF NOT EXISTS edits (

-- the context assembler and the sandbox are byte-identical to 0.17.0 --
(empty above means untouched)

-- the resume_tree policy defect is untouched, as scope.excluded requires --
2390:    pub fn record_run_policy(&self, run_id: i64, policy: &Policy) -> Result<()> {

-- no LSP, web, todo or question tool exists --
