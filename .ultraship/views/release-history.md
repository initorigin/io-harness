<!--
Generated from canonical UltraShip state. Do not edit directly.
Run `ultraship views` to regenerate.
-->
# Release history

## io-harness

| Version | Released | Mode | Delivered |
| --- | --- | --- | --- |
| 0.60.0 | 2026-08-16T18:00:00Z | published | Agents in one tree can talk to each other. Every agent has an address — `spawn_agent` takes an `as`, and one is derived as `<role>#<run id>` when it is omitted — and that address names ONE agent, which nothing in this crate could do before: `AgentDef::name` is a role two children of one definition share. `send_message` tells a named agent something; `read_messages` returns what was sent to this one, oldest first, delivered exactly once, optionally narrowed to one sender and optionally blocking for a stated number of seconds. A terminating agent posts one short line to its parent, so waiting for a named child and waiting for a message are one mechanism. Messages are rows, so a resumed tree reads the same ones in the same order, once. Every wait is bounded — `[run] max_wait_secs`, `TaskContract::with_max_wait_secs`, and `DEFAULT_MAX_WAIT` otherwise — because an agent that blocks holds its concurrency slot and the sibling that would answer it may be queued behind that slot. `max_wait_secs` is a narrowing key: a project scope may lower it and may not raise it. One additive table, one additive index, one additive column. No `CHECKPOINT_FORMAT` move, no new dependency, and no behaviour change for a caller who does not use it. |
| 0.59.0 | 2026-08-16T02:00:00Z | published | An operator on Windows can ask for a boundary and be given one, and can drive a browser there. `SandboxConfig::with_access_confinement()` selects an AppContainer inside the Job Object and reports `Backend::WindowsAppContainer`: writes confined to the paths the run resolved, and no capability to reach the network unless the run's policy permits egress. The Windows default is unchanged and is still the Job Object. And `browser::launch` no longer refuses on Windows — the browser launches, attaches, navigates, clicks, types and screenshots there, in the ordinary suite, behind neither an `#[ignore]` nor a platform gate. |
| 0.58.0 | 2026-08-15T00:00:00Z | published | An operator can see what a store is holding and decide what it keeps. Six methods on `Store`, no new dependency, no new table, no new column: `session_size` and `store_size` measure, `delete_session` removes one session whole in one transaction, `sweep_sessions` applies that to a date in one pass over the schema, `archive_session` keeps every row and empties every column that holds words, and `compact` returns the freed pages to the filesystem. Nothing expires on its own — the crate's position that retention is the embedding program's decision is unchanged, and this release is the instrument that was missing rather than a policy. **Two premises in the roadmap entry were wrong and both were corrected before any code was written — the fifth release running that reading the source first changed what shipped.** A session's cost on disk has no exact answer: `dbstat` is compiled into the bundled SQLite but attributes a page to a table, and a page holds rows belonging to any number of sessions, so `session_size` reports content bytes and `store_size` reports the file's page arithmetic beside it. And "the ledger and the accounting rows stay" does not hold table by table: `provider_calls` is the only pure-accounting table in the schema, while the user's own words are in `steps.prompt`, every tool result is in `ledger_observations.text` and whole file contents are in `snapshots.before`. Archiving therefore keeps every row and empties every word — the owner's decision, taken against the entry's prose, because an archive that emptied only `session_turns` would report a removal it had not performed. A resumable run is never swept by a date. A restore point that can no longer restore says so at the time rather than writing an empty file over a real one. And a note is never taken: a `memory` entry outlives the run that wrote it, though its recall rows do not, and what that costs 0.56.0's eviction ranking is stated in the guide rather than left to be discovered. |
| 0.57.0 | 2026-08-15T00:00:00Z | published | The notes a turn is handed are the notes that turn needs. Past the memory block's quarter-share of a turn — the regime 0.56.0's raisable caps created — the entries that survive the fit are ranked by how many normalised words they share with what the turn is about (the run's goal, and every path or subject a tool has already named in this run), then by how many separate runs have carried them, then by the order the store returned. An entry with no signal and no evidence keeps exactly the position it had, so a turn about nothing the store knows selects as 0.56.0 did. Relevance decides what is SELECTED, not the order it is printed in, and that is a correction to the roadmap entry made before any code was written. The memory block is a byte-prefix of the user turn and 0.44.0's second cache breakpoint is withheld unless that prefix repeats byte-identically, so reordering the print would have converted cache reads into cache writes on every marked wire, on every step whose signals moved, for a reordering the model gains nothing from. A store that fits its share therefore renders byte-identically however the turn moves. And `remember` stops writing a contradiction silently. On a write whose text closely restates an entry already held in the same scope under a different key, the tool result names that key and quotes what it holds, bounded. The write lands: it is a report, never a refusal. Rewriting the same key is not reported, and a workspace note restating a global one is not reported either — that is the override the second scope exists for. |
| 0.56.0 | 2026-08-15T00:00:00Z | published | What a run learns is kept for what it is worth rather than for how recently it was written. At a cap the store now drops the entry the fewest separate runs have carried, then the one least recently carried, and only then the oldest — which is 0.10.0's order kept as the tie-break, so an entry with no evidence yet behaves exactly as it did and only the proven entries move. The count is of **distinct runs** and not of recall rows: a row is written once per carried key per step, so rows measure steps elapsed since the write, which one long run would dominate and which is monotone in age — the very policy being replaced. The three caps became an operator's numbers — `[memory]` in `io.toml` and `TaskContract::with_memory_limits`, defaults unchanged, all three narrowing at project scope and never widening. A `forget` tool lets a run withdraw a note it learned was wrong: refused on a pinned entry, refused while a plan is unapproved, undone by `rewind_run`, and taking the key's recall rows with it where an eviction leaves them. And a scope above the workspace holds what is true of every repository, recalled by every run, with the workspace's own note winning a key collision and the two rendered under separate headings so a general note is never presented as something learned about this place. |
| 0.55.0 | 2026-08-14T00:00:00Z | published | An operator hands the harness a file and gets one of three honest answers instead of a silent wrong one. `read_file` classifies before it decodes — text with the encoding it was decoded from, an image or a document named and routed to the tool that opens it, and anything else named as binary with its size and its sniffed kind. A read that will not fit returns no content at all, naming the path, the size, the ceiling, which of the two ceilings that was, and both ways forward; `offset` and `limit` in lines make the offered remedy real, and a stored read that is later squeezed becomes a stub rather than a fragment. `[run] max_read_chars` makes the ceiling a number an operator chose instead of one that moves as the run spends. On the image side the accepted set at the door widened while the set on the wire stayed exactly the four every provider documents. `Media::attach` passes those four through byte-identically and converts BMP, TIFF, ICO, TGA and PNM to PNG; SVG, HEIC and AVIF are refused by name, with the reason and a one-line conversion, and a PDF is routed to `pdf_read`. Two silent-wrong-answer defects are closed by it. A binary read used to return `Ok("")` — the same answer a file that does not exist gives — and an oversized read used to return a tail wearing the shape of a whole file. |
| 0.54.0 | 2026-08-14T00:00:00Z | published | A read starts before the model has finished speaking. When a provider reports a finished tool call while its completion is still streaming, a streaming session turn starts the read-only ones then — the same `ReadWork` 0.41.0 built, in the same `JoinSet` shape, under the same `max_parallel_reads`. Only the starting line moved. **THE DECISION THAT DEFINES IT: a speculated result is used only if the settled completion asks for that same call** — same position, same name, byte-identical arguments — and what survives is kept as a contiguous prefix from position zero, so the batch that forms for the first unspeculated call can never overlap a speculated one. Keying by position alone is correct on every test where the settled completion matches what was streamed, which is every ordinary test, and folds one file's bytes under another file's call in the one case that matters. Sabotage S4 is exactly that key and it killed all three discard tests. **The roadmap's premise was half wrong, and checking it before writing the contract is what made the contract exact.** Both wires do reassemble tool-call arguments incrementally; `Provider::complete_streaming` cannot report one, because its sink is `&(dyn Fn(&str) + Send + Sync)` — assistant text and nothing else. So this release contains a trait change: `complete_streaming_calls`, a third **defaulted** method whose default reports no call at all. **`Record` and `Replay` override neither streaming method**, so a recorded or replayed run starts nothing early *by construction* rather than by anyone remembering to suppress it. The determinism claim is structural. The completeness edge is one rule for both wires — the accumulated arguments parse as a JSON object — because every proper prefix of a JSON object fails to parse. Anthropic's `content_block_stop` has no OpenAI-wire counterpart and is deliberately not read. |
| 0.53.0 | 2026-08-13T21:30:00Z | published | An agent drives a real browser and sees what it did. Six tools behind a `browser` cargo feature that is not in `default` — `browser_navigate`, `browser_read`, `browser_screenshot`, `browser_click`, `browser_type`, `browser_scroll` — with the console output and uncaught page errors each action produced riding that action's own observation rather than a tool the model must remember to call. **The transport was measured before it was specified, and that is why this contract could be exact.** A spike drove the installed browser over the pipe pair and completed the whole round trip before a line of the release was planned. Three facts came back and went into the scope rather than being discovered during implementation: the framing is one JSON object terminated by a single NUL; a child whose descriptors are absent says so by name and exits; and an uncaught page error carries the useless text `Uncaught`, with the readable message in the exception's own description. A client written against the first guess would have reported every page error as the word `Uncaught`. **THE DECISION THAT DEFINES IT: the gate is at the paused request, not at the tool's argument.** Checking the URL the model typed is easy, passes every test that only navigates by tool call, and leaves a click able to take the browser anywhere. F3 and F4 are two criteria with two sabotages for exactly that reason. **No new dependency, and the reason is a boundary argument rather than a budget one.** A remote debugging port is a TCP listener any other local process can drive with full control of the browser. The pipe transport opens none — and it happens to need no websocket client, no TLS to localhost and no protocol crate, so `cargo tree` is unmoved at 402. |
| 0.52.0 | 2026-08-13T03:01:36Z | published | The agent asks the codebase the questions an editor answers instead of the questions a text search answers. Five tools — `lsp_definition`, `lsp_references`, `lsp_symbols` (one schema, two behaviours), `lsp_hover` and `lsp_rename` — answered by a language server named in `io.toml` or on the contract with `TaskContract::with_lsp`, started once per run in the background so a cold index is paid once rather than inside a tool call. The client is written in this crate — `Content-Length` framing, JSON-RPC 2.0 correlated by `id`, server requests answered rather than dropped, work-done progress tracked — over `AsyncRead + AsyncWrite`, with the spawn as a thin wrapper. No new dependency: `cargo tree` is flat at 402. Two bounds ship with it and both are enforced rather than described. Starting a server is an `Act::Exec` check on its program, through the same `authorize_spawn` an `[[mcp]]` stdio server passes, and the refusal happens **before** any spawn is attempted. Every location handed back passes the same `Act::Read` check `read_file` passes, and a location the policy denies reading is dropped from the answer **with the omission counted in it** — because a quietly shorter list is a wrong answer to "who calls this". `lsp_rename` writes nothing: it renders the server's `WorkspaceEdit` as a patch series in 0.51.0's own format, which the model applies with `patch_file`, one `Act::Write` check per path. Server diagnostics are appended to the project's own checker in `check` and in the automatic post-edit note, never substituted for it, reusing `diagnostics::Outcome`'s four existing states. A run that configures no server is offered none of the five schemas and is byte-identical, in composed prompt and in trace, to the run it had on 0.51.0. |
| 0.51.0 | 2026-08-12T22:20:00Z | published | A change stopped being a pair of counts. Every `write_file`, `edit_file` and the new `patch_file` records the change itself as a unified diff of the whole file, in `edits.hunk`; `Store::patch` renders a run's whole change as a step-ordered patch series a human reviews; and `rewind_step` reverse-applies one step's hunks, so a twenty-step run whose step eighteen was wrong is no longer thrown away whole. Beside them, `patch_file` makes a multi-hunk change one call anchored to its own context — all or nothing — and `check` offers the project's own type-check as a question before a write rather than only as a note after one. **The crate's own source had been pointing at this release since 0.42.0.** `src/verify.rs` said, of the before-and-after rendering a reviewer is handed: *"Not a unified diff: computing one is 0.51.0's work, where the hunks are stored and a patch tool needs them."* That comment is corrected in this release rather than left to contradict the code. |
| 0.50.0 | 2026-08-12T18:20:00Z | published | A parent chooses how a child returns, and a child returns something worth reading. `spawn_agent` gains two optional arguments — `wait` (default true) and `background_after_secs` — so one call site can block as it always did, detach and collect the report at a later step, or wait a stated wall clock and then let the child carry on in the background. A tree still never returns while a child it started is running: the loop drains at its one return rather than at each of its twelve endings, which is what `run_agent` became a wrapper to do. **The release's real story is a plan-time correction, and it is recorded rather than absorbed.** The roadmap said a child "pours its whole tool output into the parent's context" and named a *forked* child shape to prevent it. That was false: a child has always run its own ledger, and the context saving the fork promised was already banked. What was actually missing was the other half — `compose_child` handed the parent a `Debug`-printed discriminant with no text at all, because `RunOutcome::Success` carries none, so a parent learned that a child succeeded and nothing it found. The roadmap entry was rewritten to say so, and the conclusion is what shipped. **Two decisions the type system made, not the design.** A detached child is a future in a `FuturesUnordered` on the parent's own task, driven against the parent's completion — it cannot be a spawned task, because `&Store` is `Send` and not `Sync`, the same constraint that decided 0.41.0's read batch. And the wall clock races the child against a sleep and **keeps the loser**: a `tokio::time::timeout` would drop the future and cancel the child, which is why F5 asserts both halves rather than only the observation. Durability rides existing tables rather than a migration. `AgentEvent::said` makes an agent's own words durable for the first time, and `AgentEvent::spawn_args` stores the whole spawn call, because the `spawns` row holds five of nine arguments and a rebuild from those five would resume a child under a WIDER policy than the operator allowed. |
| 0.49.0 | 2026-08-11T18:20:00Z | published | A turn is a turn. `CompletionRequest` carries an ordered `messages` transcript — user turns, assistant turns holding the calls the model made, and batches of results answering them — and each built-in wire maps it onto that vendor's own block types one to one: `tool_use` and `tool_result` on Anthropic, `tool_calls` and `role: "tool"` messages on the OpenAI wire. Through 0.48.0 the crate parsed the tool protocol off a response and then discarded it on the way back in, so a run's own history reached the model as prose narrating the assistant's past actions in the third person — off the distribution every model this crate targets was post-trained on, producing no error and nothing in a log, only degraded instruction following. Two consequences ship with it. A session's earlier turns arrive as their own user and assistant messages instead of `[earlier turn] you answered: …` inside somebody else's, and a classifying turn is no longer told it has a specification to meet and a success criterion to satisfy when it carries `Verification::None` — the same mismatch 0.48.0's `I03` fixed one block lower down. Plus `SystemPrompt::Preset`, opt-in by name, for an embedder who wants a shaped agent without writing a whole prompt. **The decision that kept the release small is that the ids are minted rather than remembered.** A vendor correlates a `tool_use` id with the `tool_result` answering it inside one request, and this crate rebuilds the whole request every step, so the id the vendor issued is never needed again. `ToolCall` gained no field — 199 literals across 73 files untouched — and `src/state.rs` has no DDL at all. |
| 0.48.0 | 2026-08-11T05:40:00Z | published | Everything a run starts is inside the boundary, and the boundary is the one the policy already describes. A backgrounded `shell_start` handle and the six git built-ins are contained like every other spawn, so which tool the model picks no longer decides whether the boundary applies. Each spawning tool declares the mode it needs and a call runs under the narrower of that and what the contract granted, with an unsatisfiable need refused before any process starts. And a run whose policy names hosts reaches those hosts and no others: its contained commands go through a loopback proxy the run owns, which asks the run's own `Policy` about every `host:port` and records the answer. **The release's own proof was written two releases ago.** 0.40.0 added a test asserting the coarseness it could not fix — one allowed host opened all of them — whose failure message said "if this assertion ever fails, per-host egress has been implemented and `docs/CONTRACT.md` must stop saying it has not". It failed on the proxy's first run and was inverted. Plus one correction that is not containment: a classifying turn is asked one question instead of two that disagree (`US-IO-HARNESS-0.48.0-I03`). |
| 0.47.0 | 2026-08-10T14:45:00Z | published | A contained run on a stock Ubuntu 24.04 host — which is what `ubuntu-latest` is — now confines writes and refuses outbound TCP, where every release from 0.40.0 to 0.46.0 applied neither and reported the portable floor while every interface said "contained". Linux is an ordered chain: Landlock first because it needs no namespace at all, `bwrap` where the host has a working one, the existing `unshare` backend after that, the floor last — and the rung a host takes is the strongest one that can enforce what the run asked for, with one rule that can send it lower: a run that denies egress is never given a rung that cannot deny egress. **Windows is not in this release and that is the release's other fact.** The AppContainer half was specified here and taken out whole on the owner's decision of 2026-08-10, rescheduled to 0.59.0, recorded as `US-IO-HARNESS-0.47.0-I01`. A Windows run gets exactly what 0.46.0 gave it — a Job Object, resources and not access — and every documentation surface says so. |
| 0.46.0 | 2026-08-09T14:00:02Z | published | A run's own commands are confined to the workspace without the caller having asked, and the run that genuinely needs the machine says so in a sentence a reader can find. `ExecMode` names three grants — `ReadOnly`, `WorkspaceWrite` and `FullAccess` — `TaskContract::exec_sandbox` carries one instead of an `Option`, and the default is `WorkspaceWrite` with **no resource cap at all**: containment is a claim about where a command may write, and the 0.6.0 ceilings would have been a claim about how long someone else's build may take. `with_full_access()` restores 0.45.0's behaviour exactly, in one call, at the construction site. The detected toolchain's own cache directories are writable roots, which is what makes a default-contained `cargo` or `npm` run at all — 0.40.0's recorded limitation, removed. Only roots that exist on this host are granted, because the Linux mount setup fails on a bind it cannot perform and a failed setup degrades the whole backend to the floor. The verification gate takes the same roots. `RunSpec` carries the mode and the roots to whichever backend `select` returned, and the macOS profile and the Linux bind loop are two renderings of one list. `EventKind::Contained { mode, backend, roots }` reports once per run — including for a full-access run — and names the backend that actually applied rather than the one requested. 0.45.0's boundary section names the mode, so an agent under `ReadOnly` is told it may not write rather than discovering it from a refused command. Two compile breaks, both stated with their replacement text: the contract field's type, and `RunSpec` becoming `#[non_exhaustive]` with a constructor — the second bought forward so 0.47.0's and 0.48.0's additions to that type cost nothing. |
| 0.45.0 | 2026-08-09T11:05:38Z | published | An agent is told the boundary it is working inside instead of discovering it one refused call at a time, and an embedder can give that agent its own voice without forking the crate. The system block now carries, in a fixed order: the description of the agent and its tools — the crate's, or the caller's through `SystemPrompt::Replace`; the tool and skill catalogues; the planning directive; the caller's own text through `SystemPrompt::Append`; the repository's own guidance, discovered from `AGENTS.md` by default; the boundary section, rendered from the very `Policy` the run enforces and, under containment, from the backend `sandbox::select` actually returned; and the crate's own ending sentence, last and un-overridable. A permissive, uncontained, uninstructed `Builtin` run sends 0.44.0's own sentences with that ending relocated to the end and nothing else altered. `PromptFamily` classifies the model answering — from the provider for the two built-in vendors, from the model slug for `OpenRouter` and `Compatible`, `Generic` for everything unrecognised — and decides delimiters and nothing else, asserted by stripping the delimiters and comparing the rest byte for byte. Zero compile breaks. Two behaviour changes, both in the changelog with migration notes: a discovered `AGENTS.md` moves out of `TaskContract::constraints` into the system block, and `[instructions]` is discovered without an `[instructions]` table (with `files = []` as the opt-out). One relocation, also stated: the crate's ending sentence now follows the catalogues it used to precede. |
| 0.44.0 | 2026-08-09T07:06:00Z | published | A long run stops paying full price for the part of the request that never changes. Once a run has compacted, everything from the top of the prompt through 0.43.0's written summary is marked as a cache breakpoint — the request's second, after 0.38.0's at the end of the system block — and the vendors that sell request-side caching serve it back instead of reading it fresh. Measured live: the system breakpoint alone served 7,408 tokens of a 13,113-token prompt from cache; with the transcript breakpoint the same request served 13,093. The half that decides when it is safe is the release's real content. "Everything before a compaction boundary is immutable by construction" is not true of the whole prefix — the memory block renders ahead of the summary and is re-read from the store every turn — so the crate holds the previous step's candidate prefix and marks only on a byte-identical repeat. It therefore never asks a vendor to cache bytes it has not already sent, and the failure mode of the whole feature is lost saving rather than lost money. |
| 0.43.0 | 2026-08-09T04:33:24Z | published | A long run stops forgetting its own beginning, and a request that does not fit stops ending the run. When the observation ledger crosses a share of the turn's context budget, everything but the newest few observations becomes one model-written paragraph — what was attempted, which files were touched, what was decided, what is still open — written by the run's own provider, costing one ordinary `provider_calls` row, and stored so a resumed, branched or replayed run replays it rather than buying it again. `ProviderErrorKind::ContextOverflow` tells an over-window rejection apart from any other 4xx by the vendor's own wording, and the loop answers it by folding unconditionally and asking once more with a smaller request; a second overflow escalates as before. Beside them, `Session::transcript` renders a conversation as one Markdown artifact — the whole tree, including the turns a branch left off the model's path, with each fold marked where the steps behind it used to be — and `Session::attach` stages images for the next turn, closing the one path an operator would hand a screenshot to. One public break, paid once: `ProviderErrorKind` gains the variant and `#[non_exhaustive]` together. Everything else is additive, and the store gains one table and one index with `CHECKPOINT_FORMAT` unmoved at 7. |
| 0.42.0 | 2026-08-08T18:05:00Z | published | An unattended run stops dying on a decision nobody is present to make, an operator stops needing a compiler to stop a tool call, and a review criterion stops being answered from the wrong evidence. `ModelApprover` installs a model as the `Approver`, reading the pending act, what it targets, the bytes a write would land, the rule and the layer that flagged it and the run's goal, and answering approve, deny with a reason the agent adapts to, or defer — which persists the action and stops the run for a person to answer later. It is refused before the first request is billed when it would be answering for its own model. A `[[hook]]` table gains `at = "before_tool"` with a `tools` filter, spawned with the pending call on its stdin before the call runs: a non-zero exit refuses that one call and its first line of stdout is the reason the model reads. And `Reviewer::review_change` hands a reviewer each written file as it was before the run first touched it and as it stands now, built from restore points the store has kept since 0.28.0. Every one of the three is additive: two defaulted trait methods on `Approver`, one on `Reviewer`, one field on `TaskContract`, two keys in a `pub(crate)` config table, and four new public types. Nothing is removed, nothing changes shape, and an embedder who upgrades and edits nothing behaves exactly as they did on 0.41.0. |
| 0.41.0 | 2026-08-08T15:04:14Z | published | A read-heavy step stops costing one round trip per file. When a completion carries several read-only tool calls, the loop dispatches them together instead of one after another, bounded by `TaskContract::max_parallel_reads` (default 10, floor 1). `grep`, `find` and `read_file` are read-only by construction; a registered tool says so for itself through the defaulted `Tool::effect`, returning the new `ToolEffect`. Everything else runs exactly as it did, one at a time, in the order the model asked — and so does every recorded artifact: observations, decisions, steps, `Edit` rows, budget draws and the events an `Observer` receives are folded back in call order, never in the order the calls finished. `with_max_parallel_reads(1)` reproduces 0.40.0's execution shape exactly. |
| 0.40.0 | 2026-08-07T00:00:00Z | published | A contract can ask for the project's own commands to run inside the sandbox. `TaskContract::with_contained_exec(SandboxConfig)` puts every command the `exec` tool and the foreground `shell` tool start behind the backend this host offers — the config's resource caps, filesystem writes confined to the workspace, and outbound network denied unless the run's `Policy` would permit `Act::Net` — with the **workspace root** as the working directory, so nothing is copied in, nothing is discarded, and an incremental build survives between commands. That working directory is the whole answer to `US-IO-HARNESS-0.17.0-I02`, which refused containment for `exec` precisely because the sandbox discarded what it ran on. A `shell` line is contained per sub-command rather than at its first stage, a cap kill reaches the model and the store naming the `Cap` that fired rather than a bare exit status, and the `SandboxEvent` lifecycle rows record the backend that **actually applied**, so a run that silently took `PortableFloor` is legible instead of indistinguishable from one contained as asked. On Linux the filesystem half became true in this release: the backend had unshared a mount namespace and remounted nothing into it, so the namespace existed and a write outside the working directory landed. It now remounts the tree read-only and binds back the working directory and the system temporary directory. The default is untouched — with the field unset, `exec` and `shell` behave exactly as they did in 0.39.0. |
| 0.39.0 | 2026-08-06T22:45:00Z | published | An operator asks for something wide inside a conversation and the conversation does it. `Session::turn_contained` and `Session::turn_contained_observed` take a `Containment` and drive the turn through the agent-tree loop, so the agent answering it is offered `spawn_agent` and decomposes the work into contained children — under the session's own policy narrowed per child through `Policy::contain`, one shared `Ledger` built for that turn, and the observer the operator is already reading. The turn stays one turn: one `session_turns` row, one `Session::history` entry, one move of the head, with the children as runs under the turn's own run id. The five turn entry points that predate this are untouched and still never offer the spawn tool, so a session that passes no `Containment` behaves exactly as it did in 0.38.0. Nothing public is removed, renamed or altered; no table, column or index is added; `CHECKPOINT_FORMAT` is unmoved at 7; and `docs/public-api.txt` is byte-identical. |
| 0.38.0 | 2026-08-06T00:00:00Z | published | A long conversation, and a long run, stop paying full price for the part of the request that never changes. The system instructions, the skill catalogue folded into them and the JSON schema of every tool on offer are marked as a cache breakpoint, and the vendors that cache serve them back instead of re-reading them. The Anthropic request sends `system` as a one-element content-block array carrying `cache_control: {"type": "ephemeral"}`. That wire orders a request's cacheable prefix tools-then-system, so the single marker covers the tool schemas and the instructions together, and no marker is placed on the tools array — a changed tool list already invalidates everything after it in that ordering. The OpenRouter request carries the same marker in the shape that wire spells it, added by `cached_system(flavor, system)` beside `web_key` (0.22.0) and `effort_key` (0.31.0). OpenAI and every endpoint reached through `Compatible` send nothing and their bodies are byte-identical to the ones 0.37.0 sent. OpenAI caches a repeated prefix by itself with no request-side control, and that path also fronts 21 endpoints this crate does not control, where an unknown body key would be a 400 it had caused. Measured rather than asserted: two consecutive calls over an identical system block through OpenRouter against `anthropic/claude-haiku-4.5` — the second read 7,408 of its 7,421 prompt tokens from the vendor's cache, while the control over the identical endpoint and model with no marker read none on either call. Those stored rows priced through the shipped table cost 2,336 micro-units against the uncached call's 22,335, which is the first time the cache branch of `src/pricing.rs` has executed against a non-zero count. Nothing else moved. No public item was added, removed or altered; no table, column or index; `CHECKPOINT_FORMAT` unmoved; `docs/public-api.txt` byte-identical. Zero breaks, and therefore no migration note owed. |
| 0.37.0 | 2026-08-06T00:00:00Z | published | An embedder holds a conversation in which not every turn is work. "hi", "thanks" and "what can you do" cost one completion each and leave behind no step, no gate attempt, no checkpoint, no snapshot, no plan gate and no call to the approver — while the same entry point, given the same words followed by work, runs the full loop exactly as 0.36.1 did, from step one onward. The decision is the model's and it costs nothing extra. The turn's first completion is made with the workspace tools offered, the conversation seeded and the operator's text as the goal, and what the model does with it decides what the turn was: stopping on text is an answer, reaching for a tool promotes the turn and the loop continues from that same completion. A turn that answers makes one provider call; a turn that promotes and takes two steps makes two, not three. There is no word list in this crate, in any form. io-cli's twenty-two-entry table of English greetings — the workaround that motivated the release — can be deleted rather than relocated, because `TurnResult::kind` is the fact an embedder branches on. |
| 0.36.1 | 2026-08-04T16:30:00Z | published | A contributor pushes and gets the same verdict in 6m58s instead of 17m51s, and a reader arriving from crates.io copies an install line that works and reads a product table that is true. Measured warm cache to warm cache, from the GitHub Actions API by run id. Baseline run 30834723995: workflow 17m51s, `test (windows-latest)` 17m51s, macOS 8m51s, Linux 8m23s. After, run 30925715697 attempt 2 (zero `No cache found` in any Windows job): workflow **6m58s** — `test (windows, all)` 6m58s, `test (windows, default)` 5m50s, `test (macos, all)` 4m10s, `doctest (windows)` 3m55s, `test (macos, default)` 3m23s, `doctest (ubuntu)` 3m03s, `test (ubuntu, all)` 2m59s, `test (ubuntu, default)` 2m24s, `doctest (macos)` 1m37s, `cross-version` 1m28s, `links-consumer` 1m25s, `docs.rs (nightly)` 1m16s, `lint` 55s, `msrv` 40s, `examples (compile)` 38s. A 61% reduction, and every job in the new graph is confirmed to have executed and reported by name. |
| 0.36.0 | 2026-08-03T16:06:55Z | published | A run lands as a branch, and can be put back. `git_branch` renders `git switch --create=<name>` and `git_worktree` renders `git worktree add -b <name> -- <path>`, built by the same closed-set construction as the five built-ins that existed. `switch --create` is the one shape of a checkout that cannot discard a working-tree change — the ref starts at HEAD, git refuses a name that already exists, and the tree is carried rather than replaced — which is why it is reachable while `checkout`, `reset`, `rebase`, `stash`, `push`, `fetch`, `clone`, `remote` and `filter-branch` stay unreachable and the test holding that list passes **unchanged** rather than relaxed. A branch name is fused into `--create=<name>` where git offers that form, and validated by an ASCII allowlist where it does not. `AgentDef` gains `worktree`: a child of that definition is rooted at its own checkout under `.worktrees/`, on its own branch, created before its first step. Two concurrent children of one definition now both keep their writes; with the flag off, one of them loses. The path is derived from `(agent, parent run, step, goal)` — the key a spawn is adopted by — so a resumed tree continues in the worktree it already made rather than re-creating it and discarding the child's work. A worktree that cannot be made fails the spawn instead of falling back to the shared tree. `rewind_run` widens a rewind from a path to a run: every file it wrote with the four verdicts 0.28.0 already distinguishes, every memory entry restored to the value before this run's **first** write to that key, and the spawn backlog it left queued. **Nothing in the trace is deleted** — the steps, the event stream, the spawn records and the ledger are untouched, and what the rewind took is written down before it goes, readable through `Store::rewinds` and reported as `EventKind::Rewound`. Two additive tables, `memory_snapshots` and `rewinds`. `CHECKPOINT_FORMAT` unmoved at 7, no existing table altered, `cargo tree` still 402. |
| 0.35.0 | 2026-08-03T17:55:00Z | published | A directory is a capability bundle, and everything it contributes says which bundle it came from. A `plugin.toml` declares skills, prompt templates, agent definitions, MCP servers, lifecycle hooks and policy layers in the vocabulary `io.toml` already deserializes. A `[[plugin]]` entry in any scope names one by path — appending across scopes the way `policy.layers` and `[[agent]]` do — and `Config::plugins()` loads them. `Plugins::apply_to`, `apply_to_policy`, `apply_to_hooks` and `templates()` install what they brought, at the three install points this crate already had. Every contributed name is namespaced `<plugin>__<name>` as it loads, so attribution needed no new storage: a refusal reads `rust-review__no-secrets` in `PolicyEvent.layer`, a call reads `rust-review__docs` in `McpEvent.server` and is offered as `mcp__rust-review__docs__search`, and a spawned child's tokens are billed under `rust-review__reviewer`. **No table, column or index was added and `CHECKPOINT_FORMAT` is unmoved at 7.** Namespacing also makes a collision impossible rather than unlikely: ids are unique, bounded to 32 characters of `[a-z0-9-]`, and may not contain the separator. A bundle is a stranger's directory, so 0.28.0's rule governs it: a plugin declared in the committed `io.toml` contributes skills, templates, agents and deny rules and may not contribute a `[[hook]]` or an `[[mcp]]`, and the refusal is **whole** — such a bundle contributes none of its other kinds either. `${cmd:}` is refused inside a manifest in every scope. Plugin policy may carry `deny` rules only. Loading has no error path at all. A bundle that cannot be loaded is dropped onto `Plugins::dropped()` with its reason and reported as `EventKind::PluginDropped`, and the run proceeds with every bundle that did load. |
| 0.34.0 | 2026-08-02T22:15:00Z | published | A second model checks the first, one failed gate is retried on its own, and which model answers stops being fixed for the whole run. `Verification::Review` carries a rubric the caller wrote; a `Reviewer` — `ModelReviewer` over any provider, a human, or a stub — reads the goal, the rubric and the files the run wrote, and returns a `Review { passed, reasons }` whose reasons reach the trace and every `Observer` as `EventKind::Reviewed`. It refuses to run on the model that produced the change, with `Error::Config`, **before a request is built** — proven by a reviewing provider that records zero calls, not by the text of an error. `Verification` gains `#[non_exhaustive]` in the same release that adds the variant, so the one break is paid once. Every gate evaluation is now a durable `gate_attempts` row: `Passed`, `Failed`, or `Errored` — a criterion that could not run at all, the distinction the crate never had. `retry_gate` re-runs **only** the criterion, against the workspace the run left, and the run's steps rows, its token ledger and its files are identical before and after. It refuses a gate that `Failed`, because that criterion ran and answered. `Routing` is three rules the run applies to the request it actually sends: escalate after N consecutive failed gates, downshift while the change is small, and `require_primary`, which asks the defaulted `Provider::reachable()` before the first step and refuses to start rather than spending a night on a fallback nobody chose. `EventKind::Routed` is emitted once, at the transition. |
| 0.33.0 | 2026-08-02T14:30:00Z | published | Two processes, one run. `Broadcast` wraps whatever `Observer` a caller already has and writes each `RunEvent` to a new `run_events` table on its way past, so the durable stream is the same value the in-process observer received rather than a reconstruction. `Attach` is what a second process opens on the same store file: a cursor it chooses, `poll()` for what is new, `waiting()` for what the run is parked on — an approval, a question or an unreviewed plan — and three answers that write into the durable row the run already reads. A run blocked in its `Approver`, `Responder` or `PlanGate` picks that answer up and carries on, never killed and never resumed. The first answer wins by compare-and-swap and the loser is told; the run reads the decision back from the row in both arms, fast path included. An attaching process has no method that starts, resumes or steps a run, so the observer dying changes nothing and the owner dying leaves exactly the resumable run 0.7.0 always did — both executed with real SIGKILLs rather than argued. |
| 0.32.0 | 2026-08-02T02:40:00Z | published | A fleet holds the ceiling instead of hitting it. `max_concurrent_agents` throttles a spawn past it into a durable per-tier FIFO queue and `max_total_agents` still refuses, so a hundred-agent task runs a hundred at a time until it is done rather than failing at its hundred-and-first child. `EventKind::Fleet { tier, working, queued, done }` reaches the observer on every enqueue, admission and completion. A process killed mid-fleet leaves exactly the backlog it had and the next one reports it — at the tier it had — before it authorises a provider; a child that only ever waited has no run row, no step rows and no tokens against the tree's ceiling. |
| 0.31.0 | 2026-08-02T00:35:00Z | published | The agent proposes before it acts, and a caller can say how hard it is allowed to think. **The plan gate.** `TaskContract::with_plan_gate` opens a run in a planning phase: the agent may read the workspace and may change nothing in it, and the only way out is a `Plan` — ordered steps, each optionally naming the `AgentDef` that will own it — that a `PlanGate` approves, sends back with a correction, or cancels. `Plan`, `PlanStep`, `PlanVerdict`, `PlanReview` and the trait sit in `src/approve.rs` beside `Question` and `Responder` and copy their shape exactly, down to `None` meaning "nobody in this process can answer". Two decisions carry the whole release, and both were made against the code rather than on paper. The first is **where the phase is enforced**. Not a list of tool names — a `plan-gate` policy layer denying every `Act::Write` and `Act::Exec`, merged into the run's effective policy. `Policy::explain` resolves deny-first across every layer, so one layer covers `write_file`, `edit_file`, `exec`, the four shell tools, the git built-ins, every registered `Tool` and every MCP tool — the last two because invoking one is an exec check on its own name. A tool added next release is covered the day it lands, and a refusal during the phase appears in the trace attributed to `plan-gate`, legible to a reader who has never heard of this feature. `remember` is the one write the policy cannot see, because it lands in this crate's own store, and it is refused explicitly — without that, "nothing is written" would have quietly meant "nothing the policy happens to see". The second is **where the phase's state lives**. `Store::approved_plan(run_id)` is asked at every loop entry; nothing is carried in a local. That single choice is the durability claim in both directions: a run approved in one process and killed in the next does not plan again, and one that was never approved does not start writing because the approval died with the process that held it. It is proven by a real `SIGKILL` of a fixture that proposed, stopped and parked, with the plan then read and approved by a process that never saw the first — and the assertion that carries it is an *absence*: the resumed run is never offered `propose_plan` again. An outcome assertion passes whether that is true or false. Four resume entry points mirror the four `resume_*_with_answer` ones. `Cancel` deliberately does not resume: a human refusing the approach is as final as a human denying an action, so the run is finished as `PlanRejected` rather than asking the model to propose the same thing to the same person. **Reasoning effort per role.** `Effort` — `Low`, `Medium`, `High`, with `Ord` so "at least Medium" needs no match — sits on `AgentDef` and on `TaskContract`, and reaches the wire projected onto whatever each vendor calls it: `reasoning.effort` on OpenRouter, `reasoning_effort` on OpenAI and `Compatible`, and on Anthropic — which has no tiers at all — a `thinking` budget with `max_tokens` raised to clear it, because Anthropic refuses a request whose budget is not strictly below the cap. That inequality is asserted in the body test so the failure is local rather than a 400 on the wire. Every projection has a control asserting the *whole* no-effort body, not merely the absence of a key: a caller who asks for nothing sends the bytes 0.30.0 sent. What the crate refuses to pretend is as much of this half as what it does. **OpenAI's Chat Completions returns no reasoning text at all**, so a tier there changes how the model behaves and leaves `CompletionResponse::reasoning` at `None`; that is stated in `docs/CONTRACT.md` as a row in the per-vendor table rather than worked around. Nothing is refused for asking for a tier a model cannot honour, because the crate has no way to know which models reason — `Usage::reasoning_tokens` is what says whether anything happened. And the thinking is **shown once and billed once**. Where a provider returns it — parsed from `delta.reasoning` and `delta.reasoning_content` on the OpenAI wire and from `thinking_delta` blocks on Anthropic — it reaches an `Observer` as `EventKind::Reasoning` and is never appended to the observation ledger. A vendor charges for thinking once as output; a harness that folded it into the next request would be charged for it again as input, every turn, for the rest of the run. F7 asserts that against the `CompletionRequest`s the provider double actually received, not against stored rows, because a loop that put it on the ledger and a loop that re-added it at assembly time are different bugs and only the first shows up in a row. One additive table, `plans`, indexed on `(run_id, verdict)` because that is the shape of the question the loop asks at every step — 26.3µs per lookup at 20,000 plan rows. `CHECKPOINT_FORMAT` is still 7. A real io-harness 0.29.0 binary from crates.io opens a store this tree wrote carrying a proposed and approved plan and reads everything it knows about, because its queries never name the new table. `cargo tree` reads 402 lines, exactly as it has since 0.23.0. No dependency was added; nothing here needed one. |
| 0.30.0 | 2026-08-01T22:05:00Z | published | The store answers *why*, for three questions it could already answer the *what* of. **Which file decided this setting.** `Config::origin(key)` reports the scope and the path that won a dotted key, and `Config::origins()` reports every key a file set, for a caller rendering a whole settings list. It is an addition beside `Config::sources()`, which answered "which files were read" and still answers exactly that. Three bounds are built in rather than discovered. An empty answer is the crate's default speaking and is never dressed up as a file. `policy.layers` and `agent` append across scopes, so they report *every* contributing file — naming one winner for a value two files built would be false. And a `${env:}` or `${cmd:}` value reports the file the substitution was written in, which needed no implementation work at all: `parse` substitutes per file before anything merges, so the property falls out of where the code already was. The test asserts it against a literal control anyway. The case the whole feature exists for is 0.27.0's: an operator allows `exec` in their own file, a cloned repository narrows it to `deny`, and until now nothing could say so. That is a named test, and it asserts both the origin and that `Policy::check` still returns `Deny` — the origin describes the resolution and does not alter it. **Why a fact is remembered, and whether a run may change it.** `MemoryEntry` gains a `#[non_exhaustive]` kind and a pinned flag. Pinning is a caller's act, never a run's: a pinned entry is not overwritten by the agent and is exempt from cap eviction, so a correction does not disappear because the agent wrote twenty notes afterwards. The refusal is the half that matters and it is not a silence — it is a `memory_refused` row in the trace *and* an observation handed back to the model, because an agent that believes it corrected something and did not will act on the correction it thinks it made. `Store::memory_write` returns that refusal as a value; `memory_put` is unchanged and is that call with the kind fixed to `Fact`, so there is one write path rather than two that can drift. `Store::memory_recalls` is the other half: which entries a run actually drew on, one row per key per recall, in a table rather than a flag on the entry — an entry recalled by two runs is two facts and a flag keeps only the later one. `Store::memory_list` says what the agent knows about a workspace; this says what *this run* used, which is what tells a reader whether an entry was load-bearing. **How often gates fail, and what recovery caught.** Five grouped accessors in the shape 0.18.0 established for cost: `runs_by_outcome`, `runs_by_day` and `gate_failures_by_phase` returning `Tally` rows, plus `first_try` and `recovery`. Rows out, no rendering, no derived opinion. What they refuse to do is as much of the release as what they do. `first_try` returns three counts and no rate, because *of the ones that worked* and *of everything we tried* are both legitimate denominators and returning one number would pick between them invisibly. `Recovery` has no escalation count: nothing records an escalation as an event, and an escalation is the opposite of a rescue — it is the run handing the problem back. And gate failures group by the *phase* the trace holds rather than by criterion text, which is stated plainly instead of dressing a column up as something it is not. **Nothing breaks, and both directions were executed rather than argued.** Two nullable columns on `memory`, one `memory_recalls` table, six indexes, `CHECKPOINT_FORMAT` still 7. An entry written before this release reads back as an unpinned `Fact`, which is what it was. A tree 0.29.0 stopped mid-flight resumes here without re-running a committed step or re-charging the finished child. And a real io-harness 0.29.0 from crates.io — built from a pinned generator, not this tree wearing a label — opens a store carrying a pinned decision and the recall table and reads everything it knows about, because its queries never name the new columns. `cargo tree` reads 402 lines, exactly as it has since 0.23.0. No dependency was added; nothing here needed one. |
| 0.29.0 | 2026-08-01T21:40:00Z | published | The model a run uses is no longer a choice between three vendors. A provider is a base URL, an auth style, a key and a model name, so every OpenAI-shaped endpoint is reachable without writing one: thirteen hosted vendors — Groq, xAI, Mistral, DeepSeek, Together, Fireworks, Cerebras, Perplexity, Gemini through its compatibility endpoint, Moonshot, Zhipu, Qwen and MiniMax — and eight runtimes a developer starts on their own machine: Ollama, llama.cpp, vLLM, LM Studio, LocalAI, Jan, SGLang and KoboldCpp. The local half is the one that had no equivalent before, and it is the half that costs nothing per token. It is one type, and that was the whole structural argument. `openai.rs` and `openrouter.rs` were 159 and 161 lines differing in four strings over a wire module that was already shared and already `pub(crate)`, so a third OpenAI-shaped vendor is a row in a table rather than a file. `Compatible` reuses `openai_wire` verbatim — the same body builder, the same SSE parser, the same `ensure_success`, asserted by a test that a 429 through the new provider classifies exactly as it does through the three original ones. The base is the *whole* prefix each vendor documents, which matters more than it sounds: six of the twenty-one presets are not a host plus `/v1`, and a field that had assumed otherwise would 404 against every one of them. A connected provider also says what it can run. `Provider::models()` returns the vendor's own catalogue — ids, context length, maximum output, whether it takes images or tools, and the price — and it is **defaulted to an empty list**, because a required method on the crate's one externally-implementable trait would have broken every out-of-tree implementation. The default is proven by a test that implements `Provider` with `complete` and nothing else, which is a compile-time assertion as much as a runtime one. What the release refuses to do is as much of it as what it does. `ModelInfo::price` is `Option<Price>` and `None` means the vendor did not say — never `Price::ZERO`, which would report real spend as free. Nearly every vendor returns identifiers and no cost at all, so that distinction is the common case rather than an edge. A local runtime is the one place zero is *true* and it is recorded as a stated zero. Every price carries a `PriceSource`, because a reference price is what an aggregator charges to serve a model rather than what the vendor charges, and a number whose origin is unrecoverable is what makes an invoice unarguable. The opt-in reference catalogue fills the gap for the vendors that publish nothing, and its three bounds were built in rather than discovered. Matching is an exact slug or one documented normalisation and a miss stays `None` — an ambiguous normalisation resolves to nothing rather than to a coin toss, because a wrong match is a wrong invoice and is worse than the gap it filled. The lookup dials a host the caller never named, so it is off by default, and when it is on that host appears in `Provider::endpoints()` where the run's existing egress authorisation governs it: a policy that denies it makes the run refuse rather than silently skip. That boundary needed no new code, which was the point of `endpoints()` existing. Price tiers were not in the plan and are in the release. Reading the live catalogue showed 44 of its 336 models pricing by prompt length, with the rate typically **doubling** past 200k — and a long agentic run is exactly what crosses that floor, so pricing one against the base row reports about half of what it cost. `PriceTier` hangs off `PriceTable` rather than living inside `Price`, because a `Vec` there would have cost that type its `Copy` impl and broken `..Price::ZERO`, which is a public break bought for a data shape. A table with no tiers registered prices exactly as 0.28.0 did, asserted rather than argued. See `iterations/US-IO-HARNESS-0.29.0-I01`. The one thing a caller genuinely cannot discover for themselves is now in the contract, under its own heading: **vLLM and SGLang emit no tool calls at all** unless the server was started with a tool-call parser flag a client cannot set. No status code, no warning, nothing in the trace — the agent simply talks, and it looks exactly like a model that chose not to act. Nothing breaks. `Provider` gained a defaulted method, `ProviderSpec` gained the variant 0.27.0's `#[non_exhaustive]` was added for, `Price` gained no field and kept `Copy`, no store schema changed and `CHECKPOINT_FORMAT` stays 7. `cargo tree` reads 402 lines, exactly as before. |
| 0.28.0 | 2026-08-01T18:30:00Z | published | An operator can now shape a run from `io.toml` instead of from Rust. A `[[hook]]` table names the events it wants and one thing to do with them: `append` writes one JSON line per matching event, which is an audit log; `run` spawns a fixed argv with that JSON on its stdin, which is a notification, a formatter, or — with `on_failure = "cancel"` — a local policy check that ends the run. `Config::hooks()` returns a `Hooks`, which *is* the `Observer` this crate has had since 0.12.0, so the caller installs it exactly as it installs its own and no run loop changed. The argv is a TOML array and stays one: there is no shell between the file and the process. The rule that bounds it shipped with it rather than after it. The whole `[[hook]]` array is refused in a project-scoped `io.toml`, inside a `[profile]` too — not its executing half, because a hook that runs an argv is the `${cmd:}` primitive 0.27.0 refused there arriving one release later, and a hook that appends is a write to a path a stranger chose, which is the same hazard by a shorter route. `io.local.toml` and the user file are the stated alternative, in the words 0.27.0 already used. This is that release's narrow-never-widen rule applied to a new key, and it still does not claim that cloning a repository is safe. A formatter is a hook rather than a second pass at the edit site, and that is a property of the code rather than a preference. 0.25.0's diagnostics pass runs after a successful edit and **reads**; a formatter **writes**. A writer there would leave the model reading `[wrote src/x.rs] (412 chars)` about bytes no longer on disk, and would run after `Wrote::moved_the_workspace` — the signal 0.11.0's stall detection rests on — had already been computed. At the `step` boundary both are settled, so three lines of TOML is where a formatter belongs. `docs/guide/hooks.md` carries the example rather than a paragraph explaining an absence. And a file the agent changed can be put back the way it was. Both write paths already read the previous contents to measure the edit; the first one seen per path is now kept as a row, so `rewind` restores a path to what it was before the run first touched it — including deleting a file the run created — and survives a kill and a resume, which was the criterion rather than a bonus. `Rewind` carries four answers because a caller must be able to tell them apart: `NotKept` and `NotRecorded` both change nothing, and collapsing them would tell a caller a file was untouched when the run had rewritten it and the harness cannot undo that. A new table, so `CHECKPOINT_FORMAT` stays 7 and a 0.27.0 store opens, resumes and replays unchanged. The release also closed what 0.25.0 recorded, and it was load-bearing rather than tidy. `every_kind()` matched on the items of its own vector, which proves the arms exhaustive and never proves the vector complete, so `TodoWrote`, `QuestionAsked` and `QuestionAnswered` had been round-trip-untested since 0.21.0. A hook names an event by its wire tag, so a tag the crate had forgotten would have been a hook an operator writes and that silently never fires. `EVENT_NAMES` now lives beside the enum, is proven complete by a test that reads `pub enum EventKind` out of the source, and `every_kind()`'s tags are asserted **equal** to it. Nothing in this release breaks. No public item was renamed or removed, `EventKind` gained no variant, and a file, a program and a database written for 0.27.0 all behave exactly as they did. |
| 0.27.0 | 2026-08-01T04:45:00Z | published | `io.toml` can now say which provider and model to run and what stands behind it. `[[provider]]` is an ordered chain — the first table is the provider, each later one the next link — projected by `Config::provider_spec()` and `Config::fallback_specs()` onto a `ProviderSpec` the application constructs from. It is a spec rather than a provider because `Provider::complete` returns `impl Future` and the trait is not dyn-compatible; that is the reason for the shape, not a compromise reached under it. `ProviderSpec` is `#[non_exhaustive]` from this first line, which is what makes 0.29.0's `Compatible` variant additive, and a test reads `src/config.rs` to keep it that way — with the same helper run against `pub enum Scope` so it can be seen answering no. Four things ride with it. `[app]` is one table this crate stores and never validates, reached through a generic `Config::app::<T>(key)` so that no `toml` type enters the public API; it is now literally true, rather than aspirationally true, that io-cli and io-studio keep their settings in the same file. `[profile.<name>]` overlays a named set of choices through the same `merge` the scopes already use, so it has no merge semantics of its own — and a typo inside a profile that is never selected is still rejected at load. `IO_CONFIG` names the user-scope file outright; it names a *scope* rather than bypassing the merge, which is the whole reason the scopes stay four and `Scope` gains no variant. And `[instructions]` discovers the `AGENTS.md` a repository already carries, landing it in `TaskContract::constraints` — the field that already exists for exactly this — so no public field was added and no break was carried. The sharpest half is `${cmd:}` and the rule that bounds it, which shipped together rather than one after the other. A credential can now come from a command: split on whitespace, no shell between the string and the process, trimmed stdout, and a missing program, a non-zero exit or empty output each an error naming the key. Because parsing has never run anything before, and because `io.toml` is the file a `git clone` delivers, it is refused in the project scope. Beside it, four keys whose widening value a project file may no longer write — `policy.defaults.exec = "allow"`, `policy.defaults.net = "allow"`, `sandbox.allow_network = true`, `sandbox.force_floor = false` — refused in `io.toml` only, at that value only, and inside a `[profile]` too. A project file may narrow the boundary and may never widen it. The rule proved itself before any test was written for it: it rejected two of this crate's own fixtures, including `EVERY_KEY`, the most complete configuration this repository has. Both were moved rather than exempted, because a rule that has to exempt its author's own file is not one. |
| 0.26.0 | 2026-08-01T03:00:00Z | published | Killing a process handle on Windows now reaches the whole tree, including a grandchild whose own parent has already exited. Each stage of a handle's line is created suspended, assigned to a Job Object belonging to that handle, and only then resumed; the kill closes the job, and `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` walks nothing, so membership cannot be escaped or outlived. That is `US-IO-HARNESS-0.25.0-I01`'s deferred clause met, and it is proven the way the unix half was: with a negative control showing `taskkill /F /T` — the strongest kill the platform offers short of a job — leaving the grandchild alive on the same fixture. The mechanism is reused rather than rewritten. `Job::create`, `Job::adopt` and its resume already existed on the sandbox path; what a handle needed was not a second implementation but a longer *lifetime*, so the job is created when the handle is reserved, held by its registry record, and dropped by the kill. The crate still contains exactly one `AssignProcessToJobObject`. The interface change `US-IO-HARNESS-0.25.0-I01` identified as the blocker — `Capture::on_spawn` carrying a pid where a job needs a handle — is made here: it takes the `Child`, and it can fail, because a handle whose processes are not contained is one whose kill cannot be relied on. And the Windows AppContainer exists, is proven on the CI runner, and is honest about what it is. `sandbox::appcontainer` creates a container profile, derives its SID, grants a path to it with an explicit ACE, and spawns into it through `CreateProcessW` with a `PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES` attribute list — which the crate had to own, because neither `tokio`'s `Command` nor `std`'s can carry an attribute list on stable Rust. A payload inside one is refused a read it was not granted and has no route off the machine, each against a negative control that must succeed outside the container. It is **not** what `Sandbox::select` chooses on Windows, and the platform table is unchanged because of it. The obstacle is not the mechanism — a binary nothing had blessed runs inside a container once its own directory is granted, measured rather than assumed — but knowing which directories to name for an arbitrary language toolchain, which is a discovery problem this release did not close. Recorded with its evidence in `US-IO-HARNESS-0.26.0-I02` rather than smoothed over, because a table that describes the intent instead of the build is the defect 0.9.1 and 0.16.1 were both spent on. |
| 0.25.0 | 2026-08-01T00:30:00Z | published | A run can start something that does not finish and stay in control of it. `shell_start` returns a handle instead of a result, `shell_poll` returns the output produced since the previous poll, and `shell_kill` ends the process and everything it spawned. The line is parsed and checked by exactly the machinery `shell` uses — literally the same `parse`, `plan` and `check_shell_line` — so a handle is a different lifetime for a command line rather than a second way to run one, and a line whose second stage is denied starts no first stage. A handle recorded by a previous process is orphaned on resume and never re-attached, polled or signalled. That rule lives in `record_resume_markers`, the one function every resume path passes through, because a rule that holds on three paths out of four is worthless and this is the rule whose failure kills an unrelated process on the operator's machine. Beside it, an edit now comes back carrying what the project's own checker thinks of it. That is deliberately not the LSP client the roadmap named: push diagnostics have no completion signal, the 3.17 pull model is not portable, and rust-analyzer's pull path never consults flycheck, so it omits the borrow-check and clippy errors the feature exists for. Consuming the compiler's own machine-readable output costs no new dependency and carries more. |
| 0.24.0 | 2026-07-31T13:17:32Z | published | A model can write a command line and the boundary holds against what it wrote. `shell` parses the line in this crate, checks every sub-command's program and joined argv against `Act::Exec` and every redirect target against `Act::Write` or `Act::Read` by path, and does all of it before the first spawn — so a line whose second stage is denied does not run its first. Each stage is then spawned as argv the way `exec` spawns one, with this crate wiring the pipes. There is no `sh -c` and no `cmd /c` after the parse, verified by inspection rather than asserted. The admitted grammar is a conservative subset of POSIX and everything outside it is refused by name with a reason. The mechanism is an allowlist at the lexer rather than a blocklist, so a construct nobody anticipated fails closed. `list_dir` lists one directory one level deep through the same path-resolved read check `read_file` uses. Windows gained the containment designed in 0.6.0 and never built. `Backend::WindowsJobObject` is what the sandbox reports there, memory, CPU and active process count are real bounds, the process tree dies with the job, and `SandboxLimits::max_processes` is enforced for the first time on any platform. It is resource containment only: a Job Object has no filesystem facility and no network facility, and the platform table says exactly that rather than "Native". `TaskContract::workspace` lost its positional `Verification` and defaults to `Verification::None`, with `with_verification` for projects that have a criterion. `EventKind`, `Backend` and `Cap` are `#[non_exhaustive]`. |
| 0.23.0 | 2026-07-31T02:28:58Z | published | A Rust program already using a current `rusqlite` can embed this crate. Before 0.23.0 that was not a matter of friction but of possibility: `libsqlite3-sys` declares `links = "sqlite3"` and cargo refuses two versions of a `links` package in one graph, so a consumer on `rusqlite` 0.33 or later got a resolver error and no build, with nothing they could do about it. That wall is gone, and its absence is checked by a committed consumer crate rather than asserted. A database written by 0.22.0 opens unchanged, a run it checkpointed resumes across the boundary without re-running a completed step or double-charging its budget, an approval it deferred is resolved here, and no table, index or stored format is altered. `src/state.rs` is byte-identical to 0.22.0. |
| 0.22.0 | 2026-07-30T17:40:00Z | published | Provider-executed web search and fetch, declared once and translated per vendor. `WebAccess` on a `TaskContract` — or a `[web]` table in `io.toml` — puts an Anthropic `web_search_20250305` / `web_fetch_20250910` server tool, an OpenAI `web_search_options`, or an OpenRouter `web` plugin on the wire, with the beta header Anthropic's fetch needs and only when it needs it. What a vendor cannot carry is refused by name before a request is sent rather than trimmed to fit. The sources a provider cited land in a new `citations` table and the calls it ran — including the ones that failed inside an HTTP 200, with the vendor's own error code — land in a new `server_tool_calls` table, both readable from SQLite after the process is gone, both emitted through one new `EventKind::ServerToolUsed`. A failed search reaches the model as an observation instead of reading as a search that found nothing; a `pause_turn` stop reason is a continuation instead of ending an unverified run mid-search; `WebAccess::from_policy` projects the policy's `Act::Net` rules onto the vendor's domain filter, with an allow-everything rule projecting to no filter rather than to an empty one; and a spawned child inherits the root's declaration while the spawn arguments the model writes are never read for it. `CHECKPOINT_FORMAT` stays 7, the two tables are additive, and the default `cargo tree` is still 407 lines. |
| 0.21.0 | 2026-07-30T13:11:10Z | published | The agent becomes legible. It writes down a plan an operator reads out of SQLite from another process while the run is still going, and when the goal is genuinely ambiguous it asks what was meant instead of guessing — answered in-process by a `Responder`, or by pausing the run, persisting the question and resuming in a later process on a human's answer, exactly as an approval already does. A question is not an approval anywhere in the crate: an answer is text delivered as an observation, and every tool call after it is re-checked against the same `Policy` by the same code. A sub-agent gets an identity — `AgentDef`/`Agents`, programmatic or from `[[agent]]` tables in `io.toml` — carrying a role, a model string and narrowings that can only ever pass through `Policy::contain`, so "search on the cheap model, write on the strong one" is finally expressible and a definition can never widen what its parent allowed. Prompt templates reuse the skills discovery and frontmatter parser to turn a named `.md` file plus arguments into a `String` the caller hands to a turn, and nothing else. Repetition detection rides the 0.11.0 stall machinery with a counter a workspace write does not reset, so the looping agent 0.20.0's live run had to run to its step cap is now replanned and then stalled. And the defect that live run exposed is fixed: a git built-in refused by the exec policy costs one step and an observation instead of ending the run. The live proof is a real OpenRouter run — a three-item plan read from a second connection mid-run, a question answered "beta.txt" with beta.txt the file that changed, and two named agents served by openai/gpt-4o-mini and openai/gpt-4o under a root on openai/gpt-5.6-luna, each asserted by the example itself. |
| 0.20.0 | 2026-07-30T08:20:00Z | published | The crate stops being one-shot. An operator opens a durable session over a workspace, sends a turn, watches the answer arrive token by token while the model is still producing it, says something else mid-turn or interrupts, comes back in a later process from the id alone, and branches the conversation from any earlier turn without disturbing what came after it. A turn is a run — its own trace, budgets, boundary and checkpoint — so a session is durable for exactly the reason a run already is, and `TaskContract` became what the roadmap said it always was: an optional bound on a turn, or a headless one-shot. The live proof is a real OpenRouter session: 13 deltas streamed ahead of the step that carried them, an operator's mid-turn message delivered at step 2 of 6 and recorded as a `steered` row, a branch whose path is root -> branch with the abandoned turn intact, and a reopen in a second connection that recalled the conversation's first prompt. 28,461 tokens over four turns, all readable from SQLite afterwards. |
| 0.19.0 | 2026-07-29T21:55:00Z | published | An operator configures the harness in a file instead of in Rust. One `io.toml`, four scopes merged in a fixed order — the crate's defaults, a user file, a committed project file, and a gitignored local one — every key of which lands in a type this crate already had: a `Policy`, a `SandboxConfig`, a `toolchain::Toolchain`, a `pricing::PriceTable`, `McpServer`s, and the run-shaping defaults applied to a `TaskContract` through `Config::apply_to`. `${env:...}` and `${file:...}` keep a credential out of the file; an unknown key and a failed substitution are each an error naming the key and the file, because a boundary that silently disarms itself is the worst thing a config layer can do. Nothing is loaded implicitly, which is what makes the one honest guarantee true: a config the agent writes mid-run cannot widen the boundary that run is already under. The live proof is a real run against OpenRouter whose boundary, budgets and price table came entirely out of two TOML files: the local scope's `max_steps = 8` beat the project's 40, the denied write never happened, and the four provider calls cost 11,206 micro-units from a price the operator typed — against 11,205.9 computed by hand from the trace, the crate rounding once at the end exactly as it documents. |
| 0.18.0 | 2026-07-29T19:30:00Z | published | An operator can answer "what did that run cost, and which model spent it" from the trace alone. Every provider call is its own row — the model that served it, its six token counts, its server-tool requests, its latency and time to first token, its finish reason, and the failure where there was one — so a step that retried is several rows rather than one collapsed integer. Every file change records the lines it added and removed. Money is derived at read time from a price table the operator owns and is never stored, which is what makes correcting one price repair the whole history. Two live runs against a real provider are the proof: the first recorded three calls naming the model the API answered with, real reasoning-token counts, and a TTFT inside each call's own latency; the second, over the same prompts with the vendor's cache warm, recorded 4,913 cache-read tokens and derived 4,846 micro-units for the work the cold run derived 18,606 for — the cache discount visible in money, from figures the vendor reported rather than any this crate invented. |
| 0.17.0 | 2026-07-29T03:05:00Z | published | Point the harness at a project in any language and the agent does real work in it. A live run against a JavaScript project it was told nothing about, recorded in evidence/0.17.0/live-run-node-project.txt, went: find the project, read its three files, install its dependency from the real npm registry through `exec`, run `npm test` and get exit 1, re-read the file, and fix the bug with a one-line `edit_file` replacement — six steps, 8,951 tokens, ending `Success` because the project's own `npm test` exited zero. No Rust is on that path anywhere, which is the whole claim. The same run under a policy denying the command is refused before anything is spawned, with the rule and the layer in the trace. |
| 0.16.2 | 2026-07-28T03:35:23Z | published | The crate root's nightly documentation gate is now `#![cfg_attr(docsrs, feature(doc_cfg))]`. It was `doc_auto_cfg`, and that feature was removed in Rust 1.92.0 and merged into `doc_cfg` (rust-lang/rust#138907), so the crate root failed to compile on docs.rs — which builds on nightly — for both 0.16.0 and 0.16.1. Neither published version has a rendered page, and neither can be given one: a published version's sources are immutable. 0.16.2 is the first version of the 0.16 line with documentation on docs.rs. The defect was invisible to everything this repository runs. The attribute sits inside `cfg_attr(docsrs, ...)`, and `docsrs` is set by docs.rs and by nothing else, so every stable `cargo doc` — in CI, in the release gates, on a contributor's machine — compiles the crate without reaching the line, and did so without a warning. That is why two consecutive releases shipped it, and it is why this release's verification is a nightly build with `--cfg docsrs` rather than any command that had already passed. The labelling the gate exists for still happens. `doc_cfg` performs the automatic per-item labelling itself since the merge, so the rendered output carries "Available on crate feature `x` only" for every item behind `barcode`, `docx`, `media`, `pdf`, `pptx` and `xlsx` — checked by reading the generated HTML, not inferred from the build exiting 0. Nothing else changed. No public item was added, renamed, or removed, no behaviour differs, and a 0.16.1 caller upgrades by changing the version number. |
| 0.16.1 | 2026-07-28T08:15:00Z | published | The README's last section now describes the initorigin products as they are rather than as they were intended to be. Three rows — io-harness, io-cli, io-studio — a Status column, and exactly one link, io-harness's, because it is the only public repository. io-cli and io-studio are named without links and marked "In development"; `io-eval` and `website` are gone from the page, since neither has been built and a roadmap entry is not a product. A sentence under the table states the reason there is one link, so the absence reads as a decision rather than an oversight. This was the one section 0.16.0 carried forward unchanged from the release-history README it replaced, and the only claim on that page none of its five checkers inspects. For a release whose whole thesis was that the documentation is part of the contract, four links returning 404 to every reader who is not the owner is the same class of defect as a wrong return value — reached from the last screen instead of the first. `.env.example` no longer asks a contributor for a crates.io publish token. It is the maintainer's credential and is read from the gitignored `.env`; its presence in the public template implied a permission a contributor neither has nor needs. Deliberately not in the changelog: no consumer of the crate ever sets it. No code changed. No public item was added, renamed, or removed, and a 0.16.0 caller upgrades by changing the version number. |
| 0.16.0 | 2026-07-28T06:40:00Z | published | An external Rust developer arriving cold reaches a running example from the README's first screen, and a developer already depending on the crate can read what is public, what pre-1.0 means here, and what does not work today, in one place. Both front doors were release histories and are now landing pages. The README went from 1072 lines organised as "Usage (v0.4)" / "MCP and network egress (v0.8)", with its first code fence on line 107 and a status paragraph two releases stale, to a landing page with the quickstart inside the first screen. The crate root — the page docs.rs opens — went from 230 lines of "v0.2 adds" with v0.3 filed after v0.14 and v0.10 through v0.13 absent, to 209 lines describing what the crate does. Twelve guide pages under docs/ carry the depth, and every "the limits, stated plainly" block travelled with its capability. Two of the twelve are new writing rather than moved prose: observability and replay had never been documented for a reader at all. All 106 public items re-exported from the crate root carry a worked example; before this release one did. 49 breaking changes across 16 versions are marked in the changelog with a migration note saying what to write instead. docs/CONTRACT.md states what is public, the deprecation cycle, the MSRV policy, the feature table, the platform matrix, and eleven limits that hold. Four checkers ship so the claims cannot quietly stop being true, each with a negative control proving it can fail: the worked-example check, the public-surface snapshot that fails a rename or a removal until the snapshot is edited by hand, the MSRV and feature-list drift assertions against Cargo.toml, and the changelog migration-note check. A fifth asserts the README split lost nothing. One code change: resume_tree_from_stored_policy and its observed twin, closing the last asymmetry between the three resume loops. No existing signature changed. |
| 0.15.0 | 2026-07-27T21:30:00Z | published | A developer hands the agent an image, and the agent hands its work back as commits. The caller attaches images to the task with `TaskContract::with_images` and they ride every request; the agent looks at one itself with `view_image`, gated on `Act::Read` against the path the model named. `Provider::accepts_images` defaults to false, so a provider written before this release inherits a refusal rather than a silent drop, and the refusal happens before the body is built and before anything is spent. Anthropic gets a base64 image content block, the OpenAI-shaped body an `image_url` data URL, and a request with no images still sends a bare content string. MCP tool results carrying images pass through, retiring a comment that promised it for 0.11. Five git built-ins — `git_status`, `git_diff`, `git_log`, `git_add`, `git_commit` — each constructing its own complete argv, so `push`, `fetch`, `clone`, `reset`, `checkout` and `rebase` are unreachable by construction rather than excluded by a rule. The model supplies paths, a message and a count, never a subcommand or a flag; every path goes after `--` and a leading `-` is refused. Repository hooks do not run. `git_add` needs `Act::Read` on each path it stages, so a policy-denied file cannot reach a commit, and `git_commit` needs `Act::Write` on `.git`. Video is cut from the roadmap entirely rather than deferred, on the evidence that Anthropic and OpenAI Chat Completions accept no video at all and OpenRouter says support varies by model with no way to ask which. Carried over and closed: `resume_from_stored_policy`, and `fill_form` generating real appearance streams. |
| 0.14.0 | 2026-07-27T14:40:00Z | published | A developer runs a task over real documents, and the documents are governed by the same rules as source files. The agent reads an `.xlsx` and changes one cell of it with the rest of the workbook intact, reads and generates a `.docx`, reads the text of a `.pptx`, generates a PDF and extracts its text, stamps a watermark across its pages and fills its AcroForm fields, and decodes a barcode or QR code out of an image. Every one of those is a built-in dispatched on `Act::Read` or `Act::Write` against the path the model named, over new byte-level workspace IO that no document module bypasses, and `Verification::DocumentContains` gates a run on a document's extracted text. All of it is behind an opt-in `documents` feature; the default build is byte-for-byte the dependency tree 0.13.0 had. Proven live: a real model read a real workbook and edited it in place under a permissive policy, and was refused the identical write under a policy that denied it. |
| 0.13.0 | 2026-07-27T02:35:00Z | published | A developer who resumes an interrupted run gets the run they had. `resume_with` and `resume_with_observed` resume under the caller's policy; the bare `resume` refuses a run that was started under a policy rather than continuing it permissively; the policy a run was started under is durable and readable; and the observation ledger is durable and restored in both the workspace and sub-agent loops, so a resumed run asks the model what the uninterrupted one would have. Proven live: a real model tried to read a denied path on a resume-driven step and was refused. |
| 0.12.0 | 2026-07-27T00:00:00Z | published | The twelfth and last pillar. A developer embedding the harness can now watch a run as it happens, and a developer measuring it can prove a change made things better. Watching was not supported at all: src/lib.rs exported no observer, callback or channel, so an application showing progress had to open the SQLite file with a second connection and poll a schema the crate never promised -- having first configured the file itself, because Store::open set neither WAL nor a busy timeout. An Observer is now called as the run proceeds, for all seventeen kinds of thing the trace records, and every event serialises to a flat tagged JSON object so a host can forward it to a UI in another language without hand-writing a mapping. The events are asserted against the trace the same run wrote, so the two surfaces cannot drift into disagreeing. A run can also be stopped from outside it for the first time: previously the only way was to drop the future, which left runs.status as running forever, indistinguishable from a crash. Measuring had no surface either. Nothing recorded a provider response and the provider types carried no serde derives, so there was no format to record into. Record and Replay are now combinators over the Fallback pattern; recordings go to a file rather than the store because the type system forced it -- Provider::complete's future must be Send and rusqlite::Connection is !Sync -- and Replay keys on request content rather than a call counter, which is what makes it compose with 0.7.0's resume instead of fighting it. The determinism defects mattered more than the features. src/context.rs rendered run_id into every memory note, and run_id is an AUTOINCREMENT, so the same case run twice sent the model different prompt bytes -- persisted into steps.prompt. Sub-agent results composed in buffer_unordered completion order, so steps.decision and steps.result varied run to run. Both are fixed, and the comparison that proves it is itself proven capable of failing: two negative controls reintroduce exactly those divergences and must break equality. A finished run now reports whether it worked, how many steps it took, what it spent and how long it took. Three of those were derivable only by knowing which of eleven free-text strings means success, that steps is MAX(step) and not COUNT(*) because retry rows share a step number, and that spend is SUM(steps.tokens). The fourth was not available at all: nothing recorded when a run ENDED. The audit for this release found twelve defects the roadmap had not anticipated, including one that was security-relevant: a run a human refused network egress for, when resumed, dialled the socket, wrote the file and reported Success -- because "refused" had no RunOutcome variant and resume substitutes a permissive policy. And 0.11.0's O4 had shipped as documentation of a capability nobody had: the request deadline was documented as overridable while every path to it was pub(crate). All twelve pillars now hold. |
| 0.11.0 | 2026-07-26T12:45:00Z | published | A long unattended run now survives the failures that used to end it, and the caller learns which one happened. The audit for this release found three failures worse than the roadmap assumed and all three are closed: `net::http_client()` had no request timeout at all, so a server that accepted a connection and then stopped writing hung a run forever — producing no step, so 0.7.0's checkpointing never fired, the time budget (checked only at the top of a step) was never reached, and the 0.5.0 ledger saw no draw; both stream parsers skipped un-parseable lines, so a wholly malformed response returned `Ok(CompletionResponse::default())` and the loops read it as "the model chose not to call a tool"; and `"escalated"` was absent from `terminal_outcome` while `finish_run` filed it as a plain completion, so a run that escalated at 3am was silently re-run by whatever resumed it. The two the roadmap named are closed too. `Error::Provider(String)` becomes a structured variant carrying a `ProviderErrorKind`, the HTTP status and the server's `Retry-After`, so a 429 and a 401 are no longer the same value retried with equal eagerness; retries now wait, doubling to a ceiling, honouring `Retry-After` above it, and refusing to sleep past the run's own time budget. `Fallback` is itself a `Provider`, so it needs no new entry point and nests for three, and it falls through only on a failure another provider might not have. An agent that stops getting anywhere is stopped for it. A stall needs both halves — nothing changed in the workspace, and a tool call this window already saw — resting on a new `Wrote` signal, since `write_file` never read the old content and a byte-identical rewrite was indistinguishable from an edit. A live run proved it: 20-step cap, told once at step 4, stopped at step 11. Recovery and retry is the eleventh of the twelve pillars. Only observability and evaluation remains. |
| 0.10.0 | 2026-07-26T10:45:00Z | published | The prompt stops growing. Through 0.9.1 the workspace loop kept one `String`, appended every tool result to it, and re-sent the whole thing verbatim every turn; a live run on 0.9.1 went from 552 to 16,692 to 32,832 prompt characters in three steps, and nothing in the crate could bring that number down. 0.10.0 assembles the request instead of accumulating it: on the same task, the same workspace and the same provider, the prompt peaks at 25,405 characters and then FALLS to 19,384 and 13,363 as each write invalidates the read of that path. With an explicit 1,500-token ceiling it goes flat — 1,210 estimated tokens from step 6 through step 20, sixteen turns of four reads each, roughly 320 KB of raw observations held inside the line. A developer also gets an agent that remembers. The built-in `remember` tool writes a keyed note against the workspace; a later run over that workspace opens with it, attributed to the run and step that wrote it and rendered as the agent's own notes rather than as instructions. The operator can list, read, delete and clear them through `Store`. Two of the twelve pillars close here — context construction and state and memory — leaving recovery and retry (0.11.0) and observability and evaluation (0.12.0). The one public break is additive: `TaskContract` gained a `context: ContextBudget` field, which only a struct-literal caller notices. No public item was renamed or removed, and `CHECKPOINT_FORMAT` is unchanged, so a 0.9.1 store opens and a 0.9.1 checkpoint resumes. |
| 0.9.1 | 2026-07-26T09:00:00Z | published | The three-OS matrix is green on one commit for the first time in the project's history. A developer running io-harness on Linux gets the execution-based verification gate that has been advertised since 0.3.0 and the sandbox caps advertised since 0.6.0; both were entirely broken there. On Windows the suite terminates, the wall-clock cap kills the process tree, and the caps the platform cannot apply are documented as absent and never claimed in a trace. The most consequential fix is one nobody had filed, because nobody could see it: a path deny rule failed OPEN on Windows. `matches()` normalised the target's separators but not the pattern's, so `deny_read("C:/secrets/*")` silently protected nothing. That is the permission model — shipped in every release since 0.4.0 — and it is now symmetric, Windows-only, and scoped to path acts. No public API change. A 0.9.0 caller upgrades by changing the version number. |
| 0.9.0 | 2026-07-26T07:00:00Z | published | A developer embedding io-harness extends it inside their own binary. They implement the public `Tool` trait for an action their product already performs, register it with `TaskContract::with_tools(...)`, and the model is offered it beside `grep`, `find`, `read_file` and `write_file` — governed by the same 0.4.0 policy (an `Act::Exec` check on the tool's name), refused rather than trusted when the policy says no, size-capped where its result enters the context, recorded in the rusqlite trace like any built-in, and inherited by a 0.5.0 child under that child's own narrowed policy. A tool that fails is an observation the agent adapts to, not a failed run. The same developer shapes how the agent approaches a class of task without writing Rust, by pointing the contract at a directory of markdown skills. Names and descriptions go into the system prompt; bodies do not. The agent loads the one body it wants through a built-in `read_skill` tool, as an ordinary policy-checked file read, and a directory that is missing, is not a directory, or exceeds the 64-skill cap is a typed `Error::Config` at run start rather than a silently partial set. Skills carry instructions and execute nothing. Upgrading from 0.8.1 is a version bump and nothing else: 42 public items added, none renamed, removed, or changed in signature, and no runtime dependency added. |
| 0.8.1 | 2026-07-25T00:00:00Z | published | A file under verification can no longer defeat the gate that verifies it. On 0.8.0 a subject could define `#[macro_export] macro_rules! assert` and make `assert!(false, "this gate can never pass")` report success, or open with `#![cfg(any())]` and pass on a test binary containing zero tests; the compile-only gates could be passed by a body that does not type-check at all. All three are closed, the class rather than the cases, and an honest implementation — including a private one — passes exactly as it did on 0.8.0. |
| 0.8.0 | 2026-07-25T00:00:00Z | published | A developer configures MCP servers on a task contract and the agent uses their tools beside the built-ins, namespaced `mcp__<server>__<tool>` so nothing shadows `write_file`. Both transports ship: stdio (the harness spawns the server) and streamable HTTP (it dials a URL). Tool calls carry a timeout, results are size-capped, failures come back as observations the model adapts to, and a server that never starts fails the run with a typed `Error::Mcp` rather than the run proceeding without a capability it was told it had. Egress is governed for the first time. `Act::Net` joins read, write, and exec in the same layered deny-first policy: an outbound connection has a `host:port` target, `allow_net`/`deny_net`/`ask_net` decide it, an `Ask` routes to the `Approver` and survives a full process restart, and a 0.5.0 child inherits its parent's network rules and can only narrow them. Every connection the harness opens — provider, HTTP MCP server, any harness fetch — passes one checked entry point before a socket exists, and a refusal opens no socket (asserted against a listener that counts accepts). A run under a network-deny base still reaches its model through a named `provider` layer, visible in the trace rather than exempt from it; an explicit deny of that host still wins and fails fast. Proven live: a real model and a real MCP server completing a task under a policy whose network default is Deny, then the same task against an MCP host no rule allows, refused before dialling. |
| 0.7.0 | 2026-07-24T19:15:23Z | published | Durable, unattended runs. After every completed step the harness commits that step's trace row and a checkpoint event in one rusqlite transaction, so the committed checkpoint is the step's completion marker — a crash leaves a whole step or none of it. On restart `resume` (single/workspace) and the new `resume_tree` (a whole 0.5.0 tree) reconstruct the run from the store and continue every agent from its own last committed step: completed steps are skipped (a `skipped` event recorded), the shared `Ledger` budget is restored from durable tree totals via `Ledger::from_state` (never reset or double-charged), and the time budget counts real wall-clock elapsed from a stored `started_at`, not process uptime. Replay is idempotent — an already-applied edit is re-observed and overwritten exactly once (effect committed before the checkpoint), and re-running a resume is a no-op via a terminal short-circuit. `spawn_child` became spawn-or-adopt: on replay a parent adopts the child it already spawned (persisted `spawns` table, keyed parent+step+goal) and resumes it from its own checkpoint rather than duplicating it. A 0.4.0 approval survives a full process exit via `resume_tree_with_decision` (an agent paused by a child leaves that step uncommitted so the parent replays and re-adopts the paused child). Ephemeral 0.6.0 sandboxes are never checkpointed — an exec in flight at crash re-runs in a fresh sandbox. New public surface is SemVer-additive and 0.6.0 callers compile unchanged: `resume_tree`, `resume_tree_with_decision`, `RunStatus` + `Store::run_status`, `CheckpointEvent`, `Error::Resume`, `Ledger::from_state`, `CHECKPOINT_FORMAT`. A resume against a newer-format or missing checkpoint is a typed `Error::Resume`, never a panic. Additive rusqlite migration (`checkpoint_events` + `spawns` tables, `status`/`started_at` columns, `PRAGMA user_version` stamp) — a 0.6.0 database migrates in place. Built, tested, documented, packaged, and PUBLISHED to crates.io (v0.7.0 resolves), tagged as the git Release `io-harness 0.7.0`. |
| 0.6.0 | 2026-07-24T18:05:00Z | published | Every command the verification gate runs — the `rustc` compile and the produced test binary, run on the host since 0.2.0 — now executes inside an ephemeral, per-run sandbox, so model-produced code no longer runs on the host directly. One OS-neutral `Sandbox` trait (RPITIT, no OS type in its signature) routes the exec, with a real native backend per platform over a portable floor that runs everywhere; `select` picks the strongest available and records which ran. macOS `sandbox-exec` is live-run (profile confines writes to the workdir and denies network; RLIMIT_CPU caps CPU and an RSS monitor caps memory, since macOS does not enforce address-space rlimits); Linux namespaces (user/mount/pid/net) and Windows Job Objects are cfg-gated, compiled, and unit-tested on the macOS host but not live-run. `SandboxLimits` caps CPU (SIGXCPU), memory (RSS poll-and-kill), and wall-clock, killing rather than throttling and returning a typed `Cap` hit. Network is denied by default (egress allow-list deferred to 0.8.0). The workdir is torn down on every exit path and the child is kill-on-drop, so nothing leaks; produced files copy back only where the 0.4.0 write policy allows. An additive rusqlite `sandbox_events` table records create/exec+backend/cap_hit/destroy, reconstructable from a reopened store, and a 0.5.0 database migrates in place. Sandboxing is the new default for the gate and transparent to it; a caller opts out with `ExecGuard::no_sandbox()` for the exact 0.5.0 direct path, and in a 0.5.0 tree each child is sandboxed. A sandbox start failure is a typed `Error::Sandbox`, so one failed child never takes down its siblings. Built, tested, documented, packaged, and PUBLISHED to crates.io (v0.6.0 resolves), tagged as the git Release `io-harness 0.6.0` — the first release taken all the way to published. |
| 0.5.0 | 2026-07-24T00:00:00Z | release-ready | A developer hands io-harness one large task and a parent agent decomposes it at run time, spawning contained sub-agents on demand through a typed `spawn_agent` tool. Each child runs the same observe/reason/act/verify/stop loop from run.rs (not a second implementation) over the shared workspace root and the single rusqlite store, receives a parent-supplied context brief, and composes its RunOutcome plus a result summary back to the parent as the tool result. Children run as bounded concurrent tokio tasks under `max_concurrent` and may nest to `max_depth`, counted from the root. A `Containment` value handed in once at root construction caps the whole tree — max_total_agents, max_concurrent, max_depth, and an aggregate spend ceiling drawn down by the entire tree together — and no spawned TaskContract can raise it. The 0.4.0 Policy becomes the containment boundary: a child inherits its parent's effective policy and may only narrow it (denies union, allows intersect, sensitive tier tightens only), a separate code path from 0.4.0's widening layer merge, enforced in the harness and holding downward through arbitrary depth. A spawn breaching any cap returns a typed refusal the parent adapts to and is recorded, exactly as an out-of-policy action is in 0.4.0. One Approver serves the whole tree; a child Defer persists and resumes via resume_with_decision. Every spawn, parent/child edge, containment refusal, and budget draw is recorded in rusqlite (additive schema — a 0.4.0 database migrates in place), so the tree is a reconstructable graph. Sub-agents are opt-in: a 0.4.0 caller who constructs no Containment gets no spawn tool and the exact 0.4.0 surface and behaviour. Built, tested, documented, and packaged (release-ready); crates.io publish deferred to the release/merge stage by owner decision, as for 0.1.0-0.4.0. |
| 0.4.0 | 2026-07-24T00:00:00Z | release-ready | A developer runs io-harness on sensitive work under an explicit permission policy. A Policy is a stack of named layers evaluated deny-first across the whole stack, so an overlay adds capability but can never re-allow what a layer beneath it denied. Path rules govern read_file/write_file and filter grep/find so a denied path yields no results; they are evaluated on the resolved path, and a deny matches either a symlink's own path or its target. A command policy gates what verification may spawn, with every spawn's full argv traced. Enforcement lives in the tool and verification layers, not the prompt. Anything the policy marks Ask reaches one object-safe Approver trait with three decisions: Approve (optionally rewriting the action or remembering rules, both re-checked so neither can cross a deny), Deny, or Defer — which persists the pending action so a human can decide after the process exits, with resume_with_decision continuing the run under its original id and re-checking the policy. A denied action never reaches the approver and is reported to the model as an adaptable tool result. Every refusal and decision is in the rusqlite trace, attributed to the rule and layer. Policy is serde-serializable and composes via merge, so io-cli and io-studio can each keep their own config over one shared base. Built, tested, documented, and packaged (release-ready); crates.io publish deferred to the release/merge stage by owner decision, as for 0.1.0-0.3.0. |
| 0.3.0 | 2026-07-24T00:00:00Z | release-ready | A developer hands io-harness a repository task: the agent greps and finds across a workspace, reads what it found, and edits several files in one run, verified together (WorkspaceTestPasses compiles the edited set with a test, so a partially-correct set fails). Two more providers ship behind the same Provider trait — Anthropic (/v1/messages stream) and OpenAI (OpenAI-style stream shared with OpenRouter) — selected only by which one is constructed and passed to run; the trace records which provider ran. The 0.2.0 single-file loop, budgets, retry, trace, resume, and execution-based verify are unchanged and still pass. Built, tested, and packaged (release-ready); crates.io publish deferred to the release/merge stage by owner decision. |
| 0.2.0 | 2026-07-24T00:00:00Z | published | A developer runs a longer, budgeted io-harness task and can trust it: step, time, and cost (token) budgets each stop the run with a distinct outcome; a failing provider/tool step is retried and then escalated with every attempt in the trace; each step's prompt, tool call, and token usage is persisted to rusqlite; an interrupted run resumes under its original id; and verification is execution-based — the produced file is compiled (and optionally tested) with rustc, so a substring stub cannot pass. Published to crates.io as 0.2.0. |
| 0.1.0 | 2026-07-23T16:11:56Z | release-ready | A developer embeds the io-harness crate, hands it a task contract to edit one file to a spec, and the harness runs the loop (observe, reason, act, verify, stop) with the filesystem tool and the OpenRouter provider, confirms the file meets the spec with a deterministic check, persists every step to rusqlite, and stops on success or the step cap. |

### 0.60.0 known limitations

- **The record was sealed before the crates.io upload.** `mode: published` states the authorized target; the upload follows the feat→develop merge, the develop→main merge and the GitHub Release, and is done by hand and last.

- **N4 was met in a different form, and the difference is its own number.** The criterion predicted `docs/public-api.txt` would gain exactly one line. It gains **five**: `struct AgentMessage`, `const ROOT_ADDRESS`, `const SEND_MESSAGE_TOOL`, `const READ_MESSAGES_TOOL`, `const DEFAULT_MAX_WAIT`. The prediction applied 0.59.0's lesson too literally — that file tracks every NAME re-exported from `src/lib.rs`, consts included (`SPAWN_TOOL` has been listed since 0.12.0), not root-level types only. The three tool and address constants are `pub` for exactly the reason `SPAWN_TOOL` is: an `Observer` matching on `EventKind::ToolCall` needs the string. The substance of the criterion — that the surface addition is small, additive and enumerated — holds; the number was wrong.

- **`cargo test --all-features` did not run locally, and ran on CI instead.** A cold all-features build of 88 test binaries exhausted this machine's disk twice; the default suite, both lint polarities, the nightly rustdoc gate, the cross-version gate, the sabotage pass, the live run and the packaging all ran locally, and the all-features matrix runs on three platforms on the release PRs. Stated rather than implied.

- **The derived address uses `#`, not the `-` the scope names.** `scope.included` says a child the parent did not name is called `<role>-<run id>`. It is `<role>#<run id>`, and the change is the reason the collision class is closed: an assigned address may not contain `#`, so a derived name and an assigned one cannot meet. Under a hyphen a parent could assign `searcher-7` and collide with a future run id, which would have needed a check about a number nobody has allocated yet. One character, decided during implementation, and the substance of the criterion is stronger for it.

- **Per-agent egress, ordering across a tree, and delivery latency are not claimed.** A message is ordered by row id within one store, which is a total order; nothing here promises that two sends from two agents in the same wall-clock instant land in the order a human would have chosen.

- **A tree spawned before 0.60.0 and resumed on it has children with no address.** Their `spawns` rows carry an empty `as_name`, so they cannot be addressed and what they send is attributed to a derived name. Only a resume across that version boundary reaches this.

- **A wait is bounded but it is not free, and the deadlock question is answered only in the weak form.** An agent that waits holds its concurrency slot. The clock means a tree whose agents wait on one another spends its clocks and carries on rather than stopping; it does not mean the scheduler is aware of the dependency. The contract's open question stands, and the answer this release gives is "survivable", not "impossible".

- **An empty `verify_contains` makes any run succeed immediately**, including one that wrote nothing: a missing file has read as empty since 0.1.0 and `"".contains("")` is true. Pre-existing and outside this release's scope; recorded because building the live instrument hit it.


### 0.59.0 known limitations

- **Per-host egress is not enforced under Windows access confinement, and this is a platform property rather than a deferral.** A process inside an AppContainer cannot reach a loopback listener under any capability set, nor the host's own network address, so 0.48.0's proxy — the mechanism that makes a per-host policy enforceable under containment — has nothing to bind to. Egress there is the capability: all of the network, or none of it. A contained Windows command is given no proxy at all, and the agent's own boundary section says so. `US-IO-HARNESS-0.59.0-I03`.

- **The Windows default is unchanged and that is deliberate.** The container is opt-in until the derived grant set has run a real cargo build, a real npm install and a real python payload on the CI image without a decline. Nothing in this release claims it has.

- **`cmd.exe` cannot start a script named by an absolute path inside the container**, and `call` with the byte-identical path can. Answered rather than fixed: it is not the grant set, not the extension, not the separator and not the path's ancestors, because the same path works outside the container and works inside it behind `call`. Documented with its workaround.

- **F2 and F3 are met by the suite rather than by tests of their own, and that is stated rather than implied.** F2 — the descriptors by number — is asserted by the fixture reading 3 and 4 and the whole browser suite failing if either moves; a dedicated test would assert the same thing twice. F3's stated risk, that a leaked pipe end hangs the run rather than failing it, cannot occur in this implementation: `Spawned`'s `Drop` kills the child before closing its handles, and `no_browser_process_survives_the_run` asserts that. The criterion was written before the implementation settled and the implementation is the narrower claim.

- **`docs/public-api.txt` did not move, where the contract predicted it would.** The snapshot records root-level types, and every addition here is either a variant of an existing enum, a method, a field, or a type under `sandbox::windows`, which that file has never tracked. Recorded because a prediction that was wrong is worth more than a number quietly adjusted.

- **The matrix disagreed with the seal, and the record says so rather than being quietly reconciled.** When this record was first written the three-OS matrix had not run; the feature PR then passed 19 of 19 first try and the release PR failed both Windows legs on one test out of 1,443 — a profile-name race that only a process-per-test runner exposes. It was fixed, the record was amended and re-pinned before the merge into `main`, and the defect is listed with the others above. This is the second release to use that window (0.46.0 and 0.51.0 were the first two) and it is the only one in which an amendment is legal.


### 0.58.0 known limitations

- **Nothing removes a run that no session reaches.** A run started by `run` or `run_with` rather than by a session turn is named by nothing, so no retention call finds it. Stated in the guide. Removing it needs an entry point keyed on a run, which is a different instrument with a different failure mode and is not in this release.

- **Pruning a session lowers the standing of the notes it drew on.** 0.56.0's eviction ranks by `COUNT(DISTINCT run_id)` over `memory_recalls`, and a removal takes that session's recall rows. A note that earned its place mostly through runs now removed can be evicted by a later write that would previously have evicted something else. This is the honest reading — evidence from a run that no longer exists is not evidence — and it is in the guide beside the eviction rules.

- **A removal is linear in the rows AND in the number of tables, and most run-keyed tables carry no index on `run_id`.** Each delete is therefore a full scan of its table. Measured rather than guessed (N5): 1.140 ms for a 23-row session, where thirty-two statements are nearly all of the cost. If a store's history makes this expensive, the upgrade path is indexes on the run-keyed tables in a later release, and that conclusion belongs with a number rather than with this sentence.

- **F8 is met in a different form, and the form is stronger.** The criterion names `rusqlite`'s trace hook; that needs the `trace` feature and a connection the test owns, and `Store::from_conn` is private. The count is instead returned by `pub(crate)` `prune_counted` and `sweep_counted`, and asserted both invariant across sweep sizes AND equal to `RUN_TABLES.len() + 3` — which pins it to the schema rather than only to itself.

- **The archive's fact list is a judgement, and its default is to clear.** A column a later release adds and does not name in `is_fact_column` is treated as words. That is the safe direction — a lost number the trace can live without, rather than a kept sentence the archive promised to remove — and it is asserted rather than left to the reader.

- **An archive removes what is in the database and can say nothing about anything else.** Not the operator's own logs, not their provider account, not their filesystem. Stated in the guide at the call.

- **`compact` needs free disk of roughly the file's own size while it runs**, because `VACUUM` rewrites the database. There is no incremental option: every store this crate has created was created without `auto_vacuum`, so `PRAGMA incremental_vacuum` does nothing on any existing file.

- **A deletion cannot be undone by this crate.** That is the nature of the instrument. It is why the sweep refuses a resumable run, why it names what it refused rather than proceeding, and why every call reports what it did while the information still exists.


### 0.57.0 known limitations

- **The fit is a greedy prefix, not a knapsack.** It stops at the first note that does not fit rather than continuing to pack smaller ones behind it, so one large note near the top of the ranking can cost several small relevant ones. This has been true since 0.10.0 and is unchanged here; it is more visible now that the walk order is relevance rather than age.

- **A recall is a carriage, not a use.** Nothing observable says whether the model read a line of the memory block, so the evidence the second rank term reads is "this entry was in the prompt of a run". 0.56.0 recorded this for eviction; this release adds a second consumer of the same signal.

- **Token overlap is a weak notion of "about".** Two notes on one subject that share no vocabulary rank as unrelated. The measure is chosen because it is exact, local and replayable. The clock tail means a turn with no signal degrades to 0.56.0's behaviour rather than to noise.

- **Ranking is recomputed every turn and is linear in entries** — about 1.1 ms at the default 64 entries and about 120 ms at 4,096, on every turn. The upgrade path if a store that large becomes ordinary is to cache each entry's token set against its `created_at`, not to make the ranking approximate.

- **Past the share, a run that reads new files may pay a cache write it would not have paid on 0.56.0.** The selected set can move as the signals grow, and a moved prefix withholds the marker. O8 asserts the fitting case costs nothing and that a moving prefix can only withhold, never add.

- **A spawned child does not inherit the root's `ContextBudget`.** A child's `TaskContract` is built fresh by the spawn, so it always assembles under the default 24,000 tokens however tightly the root was bounded. Pre-existing, found while sizing F7's fixture, and outside this release's scope.

- **The elision line no longer says "older", which `scope.included` said would be unchanged.** The substance of that line was kept and one word that had become false was removed: the notes are elided for being about something else, not for being old.

- **F13's parenthetical was wrong.** It claimed a float comparison would flip the exact-threshold case. It does not — an exactly-60% ratio divides to the same `f64` the literal parses to. The integer comparison is kept because there is then no rounding to reason about, and the boundary is asserted in both directions instead.

- **0.56.0's eviction plan test claims ten thousand recall rows and builds 9,984.** Sixty-four keys over 156 steps. Found by the new plan test's own row-count guard; the new test asserts the count rather than leaving it as arithmetic in a loop bound. The sibling is left as it is.

- **Three arms of the sabotage pass survived their first run and all three were test defects**, not superfluous guards — recorded in the evidence rather than quietly fixed.

- **One handle test fails under full-suite parallelism on this machine and passes serially**: `a_handle_that_exits_on_its_own_stops_reading_as_running`. It races a process reaper, touches nothing this release changed, and is the tenth instance of the standing "never gate a test on a clock" trap.


### 0.56.0 known limitations

- **A recall is a carriage, not a use.** Nothing observable says whether the model read a line of the memory block, so the evidence eviction ranks by is "this entry was in the prompt of a run", which is the strongest signal this crate can see. The guide says so plainly rather than letting the word "recall" imply more.

- **The recall aggregate is unbounded in time.** An entry carried heavily a year ago and useless since is protected by history it can no longer earn. A window would be a second untunable constant; it is recorded in `open_questions` as the first thing to revisit if the order proves sticky.

- **`docs/public-api.txt` gained three lines where N2 predicted two.** The third is `MemoryForget`, the `forget` return type, which the contract described in prose without counting it as a public item.

- **A capped write is linear in `max_entries`** — about 73 ms at 4,096 entries. Flat in the size of the recall table, which is what the index buys, but not free, and the guide says so where an operator is deciding whether to raise the cap.

- **A restored-after-`forget` entry carries the withdrawing run's attribution.** `memory_snapshots` holds the value, the kind and the step, not the id of the run that originally wrote the entry, and that row died with it. `pinned` restores as 0, which is not a guess: a pinned entry cannot be forgotten.

- **Two sabotage arms survived their first run, and both were the test being wrong rather than the guard being unnecessary.** S4 removed the guard keeping the just-written key out of the candidate set and failed nothing, because the existing arm writes into a store where nothing has been recalled — the new key is then the newest zero-recall entry and sorts last either way. The guard is load-bearing only when every other entry carries evidence, which the added test constructs. S9 made a `forget` of an absent key report a removal and failed nothing, because the test counted trace rows and neither arm writes one for an absent key; asserting the message instead exposed something real in the product, not the test — the absent answer wore the same `[forget <key>]` prefix a removal wears, and now reads `[forget: nothing to forget]`.

- **The N5 measurement found a defect that review did not.** Its first run reported 0.069, 0.143 and 0.068 ms — flat across an eightfold growth in input, which is a bug report. The cap comparison was `chars <= limits.max_chars as i64`, and the measurement's `max_chars: usize::MAX` wraps to `-1` there, so the loop's exit condition could never hold and every write evicted the whole workspace down to the key it had just written. Exact for this crate's own 16,000 and catastrophic for the first large number an operator writes — reachable only because this release makes the caps settable. The comparison is `u128` now, with a guard and a 3-character control.

- **One defect was found by re-reading new code rather than by any gate.** The memory block charged for both scope headings unconditionally, so a caller with no global notes — every caller until one is written — would have had its own block shrunk by a heading the prompt never contains. Its test carries a control that fails if the ceiling is generous enough for the assertion to hold for the wrong reason.

- **Three tests fail under full-suite parallelism on this machine and pass serially**: `a_handle_takes_the_cpu_cap_a_foreground_stage_takes`, `a_handle_that_exits_on_its_own_stops_reading_as_running` and `killing_a_handle_kills_a_grandchild_whose_parent_already_exited`. All three race a clock or a process reaper, none touches anything this release changed, and the serial re-run is recorded above. This is the ninth instance of the standing "never gate a test on a clock" trap.

- **Nothing promotes a workspace fact to the global scope automatically, and nothing seeds the global scope from `io.toml`.** Both were excluded deliberately; a config-declared note would be a fourth writer with its own precedence question against the store, the pin and the workspace override.

- **The record was sealed before the crates.io upload**, which follows the two merges and the GitHub Release under the standing flow.


### 0.55.0 known limitations

- **The all-features suite did not complete locally, and ran on CI instead.** `cargo test --all-features --no-fail-fast` reached 39 of its binaries — 995 passed, 0 failed — and then died with `no space left on device`: an all-features target for this crate is ~20 GB and this machine had 14 GiB after a clean, with a sibling repository's `target/` holding 37 GB. Nothing was failing when it stopped. The gate therefore runs on the release PR's three-platform matrix, which is the 0.53.0 rule — a record may honestly say a gate ran on CI rather than locally, provided it says so. This entry says so.

- **Document reads are still bounded to a tail.** `xlsx_read`, `docx_read`, `pptx_read` and `pdf_read` return a rendering of a file rather than the file, they were not in this release's `scope.included`, and they keep `bound`'s tail-preserving behaviour. The same argument that makes a truncated `read_file` a lie applies to them more weakly and not not at all; it is a candidate for a later version rather than an oversight.

- **A `[profile]` block can still set `max_read_chars` outright at project scope.** The narrowing rule is applied where a scope's own table is merged, and `with_profile` overlays a profile after every scope has been folded together, so the scope its keys came from is no longer knowable there. A project-scoped profile therefore sets the key rather than only lowering it. The budget-derived ceiling still applies to every read regardless, so this cannot widen the effective boundary past what the run's budget allows — it is a gap in the narrowing rule, not in the ceiling.

- **`view_image`'s transcode note carries the source type, the wire type, the PNG byte count and the digest — not the decoded dimensions** that `scope.included` mentioned. Reporting dimensions would have meant changing what `Media::attach` returns for one line of prose. Stated rather than glossed; F5, which is the criterion, asks for the source type, the byte count and the digest, and all three are there.

- **S7's sabotage failed far more than the criterion predicted, and that is a finding.** Decoding UTF-16 without requiring a byte-order mark killed 10 tests rather than F7's one, because any even-length ASCII file then decodes into UTF-16 nonsense. The byte-order-mark check is load-bearing for every text read in the crate, not only for the UTF-16 ones.

- **A sabotage script must assert its own edit applied.** S13's first attempt silently matched nothing — `cargo fmt` had joined the `merge(...)` call onto one line after the pattern was written — and the suite passed. A sabotage that did not apply is indistinguishable from a guard that protects nothing. The rerun asserted the pattern first and killed the named test.

- **The record was sealed before the crates.io upload**, which follows the two merges and the GitHub Release under the standing flow.

- **A charset detector is deliberately absent.** UTF-16 without a byte-order mark, and every single-byte legacy encoding, is `Binary` — named, with its size, rather than guessed at. Adding a detector is a new dependency and a new class of confident wrong answer.


### 0.54.0 known limitations

- **Speculation only reaches session turns that STREAM, and streaming is a property of the turn entry point.** `Session::turn_observed`, `turn_steered`, `turn_bounded_observed` and `turn_contained_observed` set `stream: true`; `Session::turn`, `turn_bounded` and `turn_contained` pass `TurnExtras::default()` and start nothing early. This cost the release's own first measurement, which showed no saving at all through `turn_bounded` while the feature was working perfectly. Now stated in `docs/CONTRACT.md`, the providers guide and the example.

- **`run_with` and the other one-shot entry points do not stream at all**, so they are unchanged. Making the one-shot loop stream would start it emitting `EventKind::Token`, which is a real change to what a `run_with` produces and belongs to its own release.

- **`agent_loop`, the tree/child-agent loop, has neither batching nor speculation.** It never took 0.41.0's batch path either, so this release does not widen a divergence it did not create — but the divergence is now two releases old and is recorded here rather than left to be rediscovered.

- **`dispatch` emits an `EventKind::Contained` per call where `read_batch` does not**, so 0.41.0's claim that "an observer cannot tell from the event stream whether a batch ran concurrently" is not strictly true today. Pre-existing, not caused or fixed by this release, and found because the first trace-identity control compared the batch path against the serial one and failed for that reason. The consequence for anyone testing here: compare the same cap with the feature on and off, never cap 10 against cap 1.

- **0.41.0's `read_batch` skips `resolve_call_mode`, and that hole is left open.** A registered tool needing more containment than the run grants is refused when it is the only read-only call in a completion and runs when there are two or more. 0.54.0 closed it for the speculative path and deliberately did not widen the fix into 0.41.0's batch path, which is its own release's decision with its own evidence.

- **`settle` joins every speculated task before checking the match**, so a slow read-only tool can be waited on even when its result is about to be discarded. Reachable only from a provider that streams one call and settles a different one — a provider bug, which F5 constructs deliberately; the retry and fallover paths abort through `reset()`. Left as a limitation rather than restructured after the gates had run.

- **A discarded speculation still emits `tracing` output.** `ReadWork::Custom` logs at `info!` when it runs, so a run's log can name a registered tool call the settled completion never made. `Watch`, `Store`, the ledger and the event stream are unaffected — "the trace is identical either way" is a claim about those, and this states the one surface where it is not.

- **A run with any tool hook configured starts nothing early**, so `EventKind::Speculated` can never appear for it and a `[[hook]] on = "speculated"` can never fire. The gate is the presence of a `Hooks` object rather than of a `before_tool` entry, so a run carrying only event hooks also loses the feature. Correct but blunt; stated in the event's own rustdoc.

- **A speculated read observes the workspace at speculation time rather than at fold time.** Within one completion nothing else in the run writes — the prefix rule guarantees it — but an external process could. Replay is unaffected: it reads recorded observations and never speculates.

- **An accumulator entry whose name has not arrived blocks every later call from being reported.** The position of every later call depends on whether that one becomes real, and while the stream is open that is unknowable, so nothing past it is reported. The cost is a missed opportunity; the alternative is a position the settled completion disagrees with.

- **F10's empty-argument arm is satisfied by NOT speculating.** An empty fragment set never parses as a JSON object, so such a call is never started early; `finish` still maps it to `{}`. The criterion said each shape is "started exactly once", which holds — serially.

- **The open question about Anthropic's `content_block_stop` resolved against using it.** The parse edge is sufficient and uniform, and a rule built on that event would have no counterpart on the OpenAI wire, which sends none.

- **The record was sealed before the crates.io upload**, which follows both merges and the GitHub Release. `mode: published` states the authorized target; the flow is what makes it true, and the sequence is deliberate — publishing before the git flow puts an unreviewed artifact on a registry that cannot recall it.


### 0.53.0 known limitations

- **Windows is unsupported in 0.53.0, planned out rather than discovered out.** The pipe transport needs two inherited descriptors at fixed numbers, which the standard library does not expose there; every entry point returns a typed configuration error naming the platform. Decided at specification time on the 0.47.0 precedent, where the Windows half was cut only after ten matrix rounds.

- **Subresources are not individually policy-checked.** Document navigations bound where the browser *goes*; images, stylesheets, fonts and XHR are the page's own traffic to a host already permitted. Under containment everything it sends takes the run's egress proxy, but an uncontained run gets no per-asset decision.

- **Console attribution slips by one action when an action FAILS.** `tools::browser::act` drains the console while building its `Outcome`, so a failed action — a refused navigation, a selector that matched nothing — returns before the drain and its console lines attach to the NEXT action's observation. F6's claim holds for every action that succeeded, which is what the criterion tests. Found by reading the code rather than by a gate, and stated here rather than left for a consumer to find.

- **F4's sabotage prediction was wrong, in the release's favour.** The contract said gating the tool's argument would fail F4 while F3 still passed. It fails BOTH, because F3 asserts on what the browser was told rather than on the refusal text — an implementation that refuses at the argument never lets the request reach the browser, so the browser is never told to fail it. F3 therefore already discriminates gate placement; F4 adds the case with no `browser_navigate` call anywhere in the run.

- **S8 SURVIVED its first form, and the claim proved over-determined.** Removing `Drop`'s `start_kill` and `shutdown`'s `close` failed nothing, because the spawn also sets tokio's own `kill_on_drop(true)` — a third guarantee that alone makes "no browser process survives the run" true. Reported as a finding rather than propped up; S8b removed all three and killed the named test. All three are kept, because they cover different exits: a panic, a tidy end, and a dropped future.

- **N6 was met in a different form.** It named a byte diff of the composed prompt; the difference is asserted as the **tool list** instead — one test names the six schemas exactly and sorted, another asserts no `browser_` name appears at all. Same claim at the altitude the catalogue is built at, and it does not rot the way a whole-prompt byte diff would.

- **The all-features suite was not completed locally, and the reason is stated rather than glossed.** The development volume was shared with another repository's build that held eight of nine compile slots for hours; the local run was abandoned in favour of CI, which ran the all-features suite on all three platforms. The default suite, the sabotage pass, the live run, the packaging and every lint DID run locally.

- **O1's three-platform matrix could not run locally at all** — every local gate is macOS, and the Windows arm is the one that proves the refusal. It runs on the PRs.

- **The record was sealed before the crates.io upload**, which follows both merges and the GitHub Release.


### 0.52.0 known limitations

- **The record is sealed before the crates.io upload.** `target_mode` is `published` and the publish is the last step of the standing flow, after the feat merge, the release merge and the GitHub Release. The record states the authorized target; the flow makes it true.

- **THE LIVE RUN FOUND THREE DEFECTS THE FIXTURE COULD NOT, and that is the release's argument for keeping it.** (1) The client never advertised `window.workDoneProgress`, so `rust-analyzer` sent no `$/progress` at all and the crate had no way to tell "still indexing" from "no references". (2) A semantic request answered during start-up comes back **empty, not as an error**: `documentSymbol`, which needs only the syntax tree, answered correctly while `definition`, `references` and `hover` all returned nothing — precisely the silent empty this release promises never to produce. (3) `lsp_symbols` with a `query` and no path matched the empty string against every server's `extensions` list and reported that no server answered, while five other tools were being answered by one.

- **An empty answer still cannot always be told from an unready one.** The protocol has no readiness signal. The crate tracks the work a server announces and retries an empty answer until that work settles, bounded by the server's own `timeout_secs`; a server that announces nothing about a start-up phase can still answer "nothing" when it means "not yet". Stated in `docs/CONTRACT.md` rather than left to be discovered.

- **The settle window is a constant, not a knob.** `SETTLE` is 1.5 seconds in `src/lsp.rs` — the width of the gap between two of a server's start-up jobs, measured against a real `rust-analyzer` (`Fetching`, then `Building CrateGraph`, then `Roots Scanned`). A server that reports its start-up in coarser pieces needs it larger and nothing configures it.

- **A language server runs at the harness's own privilege.** Like an `[[mcp]]` stdio server, and unlike a command run through `exec` or `shell`, it is not placed inside the run's execution sandbox. The roadmap entry said the argv is checked "under the run's sandbox mode"; what shipped is the `Act::Exec` check, which is the half that decides whether the process exists at all.

- **A server indexes the whole root it is pointed at, including files a `deny_read` rule covers.** What this crate does about that is refuse to carry those locations into the model's context and say how many it dropped. What it cannot do is stop the server process reading them.

- **`EventKind::LspStarted`'s middle field is `root`, not `backend_root`.** The contract named `backend_root`; the value is the workspace root the server was told to index and there is no backend to name. Renamed at implementation time rather than shipped under a misleading name. It is also emitted the first time the run **observes** a server usable — the first navigation call — rather than from the handshake task, because a `&Store` cannot cross a task boundary and `ready_ms` must be the number that was measured.

- **`authorize_spawn` was made `pub(crate)` in `src/mcp.rs` rather than moved to a new module.** The contract said "moved to a shared location". One function does not earn a module, and moving it into `src/run.rs` would put a policy-event writer in a 14,000-line file. The property the contract wanted — one check, called by both sessions, unchanged — holds.

- **The contract said nine run entry points; there are eight.** `McpSession::connect` appears at eight sites in `src/run.rs`, and `LspSession` is wired at all eight through one `lsp_for` helper. Counted from the source at implementation time.

- **A sabotage that failed nothing, reported as a finding rather than propped up.** F5's first form asserted only that the fixture had not written its marker file, and a build that spawns first and checks second still passed it: the child is dropped with `kill_on_drop` on the way out and can be killed before it is ever scheduled. An assertion that depends on winning a race is not an assertion. Replaced by one no scheduling decision can change — for a command that does not exist, the gate's answer is `Error::Refused` and the spawn's answer is `Error::Lsp`.

- **The live path is proven by an example, not by a test.** `examples/lsp_live.rs` is run by hand, following `documents_live` and `media_git_live`: a real server's index is minutes on a real repository and a CI gate that waits minutes is a gate someone eventually deletes. Its output is stored under `evidence/0.52.0/lsp-live-run.txt` rather than enforced by the suite.

- **O1's three-platform matrix could not run locally.** These numbers are the macOS leg; the Linux and Windows legs run on the release PR, and a hand-written wire protocol is exactly the code that passes on the host that wrote the fixture.

- **Completion, signature help, code lens, semantic tokens and call hierarchy are not offered**, and neither are push diagnostics. Excluded at specification time and unchanged.


### 0.51.0 known limitations

- **The record is sealed before the crates.io upload.** `mode: published` states the authorised target; the upload follows the merge into `main` and the GitHub Release, in that order, as the standing flow requires.

- **`docs/public-api.txt` gains THREE lines, not the two N3 named — a criterion met in a different form, stated rather than glossed.** The third is `fn rewind_step_observed`. The contract named `Reverted` and `rewind_step`; the observed twin exists because that is the shape `rewind`/`rewind_run`/`rewind_run_observed` already has and because `EventKind::Reverted` needs an observer to reach. The substance of N3 holds — the additions are enumerated, no name was removed or renamed, and the count was not the claim.

- **A fourth public addition the contract did not name: `Edit::with_hunk` is `pub`, not `pub(crate)`.** It was written crate-private and `Store::patch`'s own doctest could not call it — a doctest compiles as an external crate. Making it public is the coherent answer rather than a workaround: `Edit::measure` and `Store::record_edit` are already public, so a caller recording its own edit rows would otherwise be able to write a count and never a change, which makes `Store::patch` useless to them. It adds no line to `docs/public-api.txt`, which enumerates crate-root re-exports and not methods.

- **F2's sabotage failed nothing on its first attempt, and the corpus was strengthened rather than the sabotage abandoned.** Measuring the line counts from the whole-file texts passed the original five-case corpus, because for a tidy whole-line edit the two measures agree — trimming the common head and tail of the fragment finds the same changed span as trimming them over the file. A brute-force search over 400,000 single-substring replacements found the class that diverges: a replacement that does not begin and end on a line boundary. Deleting a substring *inside* a line is (0 added, 1 removed) over the fragment and (1, 1) over the file. Two such cases joined the corpus and the sabotage then failed the named test and only it. **The honest consequence: for most edits the two measures coincide, so "the counts did not move" is a narrower claim than it sounds — it is exact, and it bites only on off-boundary replacements.**

- **F3's first fixture was too weak to catch its own sabotage.** Every hunk replaced one line with one line, so a running-rewrite applier accumulated no drift and passed. The fixture now adds a line in its first hunk and removes one in its third, and the reason is written into the test's own doc comment so the next person does not weaken it back.

- **The stored hunk is one head-and-tail-trimmed hunk per edit, not a minimal diff.** For an `edit_file` — one contiguous replacement by construction — it *is* the minimal diff. For a `write_file` that rewrote two distant regions of a file it is one hunk spanning both: a valid unified diff that reverse-applies exactly, and not the shortest one. A Myers or patience diff is a dependency or several hundred lines, and it buys shorter output rather than a capability.

- **`patch_file` matches every hunk at its own recorded position, exactly, with no fuzz and no offset search.** A patch written against a file that has since moved by even one line is refused rather than relocated. That is deliberate on an all-or-nothing write path, and it means a model must write its patch against a current read. Real `git diff` output satisfies it; a hand-shifted patch does not.

- **The `check` tool runs its checker outside the run's containment**, exactly as the automatic post-edit checker has since 0.46.0. It is policy-gated — which the automatic one is not — but it is not sandboxed, so `Act::Exec` is the whole of the boundary on it. Bringing both under `TaskContract::exec_sandbox` is its own change and was not made here.

- **`rewind_step` is an operator-facing API and not a tool.** No model can call it, so nothing tells a model that the workspace moved under it. If it is ever exposed as a tool, the gate story and the question of re-running the checker after a revert both change.

- **Walking a run back is the caller's loop, and there is no automatic ordering.** Reverting out of order reports `Reverted::Stale` and changes nothing, which is safe but silent about what to do next; the newest-first rule is documented at the function, in `docs/CONTRACT.md`, in the guide and in the changelog rather than enforced.

- **O1 — the three-platform matrix has NOT run.** Every gate above is macOS. A hand-written unified-diff parser is precisely the code that passes on the host that wrote the fixture and fails on the one with different line endings, which is why the CRLF and final-newline cases are asserted explicitly and why the criterion says outright that a green macOS gate is not evidence here. The matrix runs on the release PR.

- **The same flake TWICE, on both `--all-features` runs, and it is a family already recorded.** `a_handle_takes_the_cpu_cap_a_foreground_stage_takes` — a 0.48.0 containment test — reported a spinning `awk` still `running` after the CPU cap should have ended it, both times on a machine that had just built the whole all-features tree from clean. It passed standalone 5 of 5 on both occasions and in every default-suite run. Nothing in this release touches the handle or sandbox path. Twice is no longer a coincidence: the test is load-dependent, and the honest reading is that it asserts a wall clock it does not own.

- **DEPENDABOT MERGED INTO `develop` WHILE THIS RELEASE WAS IN FLIGHT — 0.37.0's trap, second occurrence — AND THE WHOLE GATE SET RE-RAN BECAUSE OF IT.** PR #149 landed five patch bumps above the feature merge, including **`rusqlite` 0.40.1 → 0.40.2 and `libsqlite3-sys`**, this crate's storage dependency. `git diff --quiet feat/0.51.0 develop` was therefore NOT quiet: `Cargo.lock`, 28 lines. The extra commits ship, so every gate was re-run against the merged tree before the release PR was opened, which is the only window in which a sealed record may be corrected. **Re-gate results, all on the merged tree:** `cargo fmt --check` exit 0; `cargo clippy --all-targets --all-features -- -D warnings` exit 0; `cargo test` exit 0, 81 binaries, **1,656 passed**, 425 doctests — identical to the pre-merge run; `cargo test --all-features --no-fail-fast` on a cleared target, 81 binaries, **1,742** (one being the flake above), 428 doctests; `RUSTDOCFLAGS="--cfg docsrs -D warnings" cargo +nightly doc --all-features --no-deps` exit 0; `cargo package --locked` exit 0 at 1,699,911 bytes; `cargo tree` still **402**; and the cross-version generator gate green, `a_current_store_is_read_by_a_0_29_0_binary` ok — which is the one that matters most here, since a real 0.29.0 binary reading a store written by this build is what a `rusqlite` patch bump could have broken.

- **This record was amended after it was sealed, and that is legal only before the merge into `main`.** The amendment carries the re-gate above, the corrected package size, and the second occurrence of the flake. Nothing it claimed became false; the tree it describes moved, and the record moved with it rather than being left describing a tree that no longer existed.

- **One ordering to state rather than gloss.** The nightly rustdoc gate's fix — a doc-comment link in `src/state.rs` — landed after the two suites had run. It cannot change behaviour, but rather than assert that, `cargo test --doc` was re-run against the shipped tree (425 passed, exit 0) and `cargo package` compiled the packaged crate from it.


### 0.50.0 known limitations

- **A process that dies inside the drain leaves a parent `completed` and a child `running`.** The drain runs after the loop has recorded the parent's ending, so resuming that parent reports the terminal outcome and re-adopts nothing. The child is not lost — it is resumable by its own run id — but it is not picked up automatically. Found because the first F7 test crashed in exactly that window, stated in `docs/CONTRACT.md` under "The one window that is not covered", and carried here rather than fixed: closing it means deferring a run's own ending until its children finish, which is a larger change than this release's scope.

- **N5 was met as step counts against a parked scripted provider, not as a timed benchmark — stated, not glossed.** The criterion asks for "the parent's completed step count over that interval", and that is exactly the number recorded: the detached arm's report arrives at step 3 of 3, so **2 parent steps completed while the child ran**; the blocking arm folds both children into step 1 for a parent total of 1 step, so **0 parent steps completed while the children ran**. Both satisfy the criterion's own test (>0 detached, 0 blocking). What was not done is a wall-clock benchmark, and deliberately so — O3 forbids gating a claim on a clock the test does not own. The figures were taken from a temporary `eprintln!` in the two existing tests, which was then reverted and `tests/subagents.rs` proven byte-identical by sha256.

- **O1 had not run at the time this record was sealed.** The full suite on all three platforms, including 0.47.0's `contained (ubuntu, userns restricted)` leg, runs on the release PR. Every gate above is macOS, and a green macOS gate is explicitly not evidence for a concurrency release — that is the criterion's own wording. If the matrix finds a defect, the correction is available only before the merge into `main`.

- **The record was sealed before the crates.io upload.** `mode: published` is the delivery this release is authorised for and carries out; the upload itself follows both merges and the GitHub Release, which is the standing order of this flow.

- **A run that opts into detachment gives up step-for-step trace reproducibility** for the calls that use it. A run that does not is unchanged, which is what N6 asserts and what F4 is the negative control for.

- **One behaviour change has no opt-out, and it is the release.** A parent's composed child observation now carries the child's conclusion instead of a `Debug`-printed outcome. An embedder asserting on the exact text of that line will see it; the changelog states the old and new shapes so the fix is a one-line edit rather than a discovery.


### 0.49.0 known limitations

- **The record is sealed before the crates.io upload**, which follows the `develop` → `main` merge and the GitHub Release. `mode: published` states the authorized target; the flow makes it true.

- **A resumed run's pre-resume history is prose, exactly as it was in 0.48.0.** The ledger a resume restores holds observation text and no tool-call structure, and nothing durable was added, so the steps a resumed run did not itself drive stay in the first user message. Everything from the resume point on is role-tagged. Reconstructing them structurally needs the calls to be durable and is a schema change this release excluded.

- **A step whose results do not line up with the calls it made is sent as prose too.** Correlating them positionally would answer the wrong call, so the loop falls back rather than guessing. It is not silent — `messages` is empty or short and a reader can see it.

- **`CompletionRequest::user` is retained for one release and is derived.** It will be removed in a later version. Until then a request has two renderings of one emission, and only the fact that both come from the same pass keeps them from disagreeing.

- **On the OpenAI wire only a `role: "user"` message is ever marked.** An assistant message's content is `null` whenever the turn was a bare tool call, and whether that wire carries a `cache_control` on a `role: "tool"` message through to the vendor behind it is not something this crate can assert. Marking less costs a smaller cache hit; marking something the vendor drops would cost a cache write on every step. So a boundary falling on a results batch marks the user message before it, and the transcript's marked span is asserted against the byte offset rather than assumed.

- **A CRITERION MET IN A DIFFERENT FORM, stated rather than glossed.** The scope named a `TaskContract::conversation(root)` constructor beside `TaskContract::workspace`. It was **not added**: that constructor already defaults to `Verification::None` (`src/contract.rs:470`) and the operator's words already reach the model as the first user text through `conversational_user_prompt`, so the new one would have been byte-identical to the old — a synonym rather than an affordance. The substance of the item, that a conversation is not framed as a specification, landed where it is actually read: the classifying turn's system block.

- **A SECOND CRITERION MET IN A DIFFERENT FORM.** O3 required every cassette in the repository to be re-recorded. None needed it and none exists to re-record — the checked-in fixtures are store databases, and every recording in the suite is made in-test. What the criterion was for, that a stale recording must not silently miss, is delivered by `Replay`'s key excluding the transcript and is asserted in both directions.

- **A session turn's flat prompt changed wording.** The seed's `[earlier turn] the operator asked:` and `[earlier turn] you answered:` became `[operator]` and `[agent]`, because the attribution moved into the message's role. A program parsing the old wording out of a prompt will not find it. Two existing tests changed with it, each carrying the sentence saying why.

- **The single-file loop sends no transcript.** It drives one tool over one file and has no observation ledger to build one from, so it is byte-identical to 0.48.0 on the wire. Not a regression and not a gap this release set out to close.

- **Reasoning replay is expressible now and is still not shipped.** An assistant message is a place to put `CompletionResponse::reasoning`, which remains never persisted and never re-sent. The vendor that requires it belongs to a later release, and shipping the carrier and the payload together would have put two unrelated breaks in one version.


### 0.48.0 known limitations

- **The record is sealed before the crates.io upload**, which follows the `develop` → `main` merge and the GitHub Release. `mode: published` states the authorized target; the flow makes it true.

- **Per-host egress was sealed as proven on macOS and unproven on Linux; CI answered it, and it holds.** Every F6/F7 assertion had run against `macos-sandbox-exec`, where SBPL names the loopback address exactly, and no Linux host had executed the Landlock rung's port rule. On the `contained (ubuntu, userns restricted)` leg — the leg that does **not** relax `kernel.apparmor_restrict_unprivileged_userns`, and therefore takes the Landlock rung rather than a namespace one — `a_direct_dial_past_the_proxy_is_refused_by_the_sandbox`, `a_host_the_policy_names_is_reached_through_the_proxy` and `egress_under_containment_reaches_the_named_host_and_no_other` all pass. The limitation is left here rather than deleted because it is the honest history of what the seal knew.

- **Under Landlock the scoping is port-scoped, not address-scoped.** Another host on the proxy's port number is reachable. The port is ephemeral and per-run, which narrows it in practice and is not a proof; `docs/CONTRACT.md` carries this per backend rather than as a footnote.

- **On the portable floor and on Windows the proxy is advisory** — the variables are set and a command that ignores them reaches the network. Reported in the agent's own boundary section in that word.

- **A handle still live when the run ends writes no `destroy` row.** The registry kills it on drop and there is no step left to record on, which is the position `record_handle_ended` was already in.

- **The harness's own `git worktree add`** — the one that roots a spawned child's worktree — is not contained. It is a fixed argv the crate builds, on a path already checked against the parent's policy, and is not a model-initiated command; the six git *built-ins* the model calls are contained.

- **A registered `Tool`'s declared mode is a refusal mechanism, not a confinement one.** The crate does not see that tool's own spawn and makes no claim to govern it.

- **The record was sealed, and then corrected twice before the merge into `main`** — which is the only window that permits it. `2a6a680` installs the Landlock net-port rule the plan carried and `Ruleset::build` never read, and `b8caa3c` grants `/dev/null`. Every gate quoted above was re-run in full against the corrected tree and this record re-pinned; the CI run it cites is on `b8caa3c`, which does not contain this record, for the structural reason a release record can never cite CI on the commit that carries it.

- **Debug prints from the proxy diagnosis reached three commits** before being removed in `1aa86f5`. `cargo fmt` reformatted them between writing and removing them, so the removal did not match. They are absent from the shipped tree; noted because a released tree's history should not be read as clean when it was not.


### 0.47.0 known limitations

- **The record is sealed before the crates.io upload**, which follows the `develop` → `main` merge and the GitHub Release. `mode: published` states the authorized target; the flow makes it true.

- **Windows enforces no filesystem or network boundary**, and this release does not change that in either direction. `ExecMode` is routed and reported on that platform and enforces nothing for the filesystem, exactly as in 0.46.0. The access half is 0.59.0 (`US-IO-HARNESS-0.47.0-I01`), and `docs/CONTRACT.md`, the README, `src/lib.rs` and the changelog all say resource containment only.

- **Three acceptance criteria were removed rather than met**: F7, F8 and F9 were the Windows arms and went with the deferral. F10 and F11 were renumbered F7 and F8, and F7 lost its Windows arm and kept its Linux one. Stated here rather than left for a reader to notice the criteria list and the roadmap disagreeing.

- **`sandbox::appcontainer` ships compiled, unit-tested and selected by nothing**, carrying `allow(dead_code)` as it did from 0.26.0 until this release. It keeps three corrections this release paid for — an ACE must carry specific rights rather than generic ones, a tree grant must survive a descendant another process holds open, and a directory-only grant must not enumerate the directory — so 0.59.0 starts from them rather than re-finding them.

- **`shell_start`'s backgrounded handles are still uncontained**, which is 0.48.0's, and the limitation stays stated in `docs/CONTRACT.md` until then.

- **A workspace located inside the system temporary directory is not confined** on any unix backend, because every one of them grants that directory writable so a toolchain can open a temporary file. Found by the CI matrix in this release and documented rather than worked around.


### 0.46.0 known limitations

- **The record was sealed before the crates.io upload.** `mode: published` states the authorized target; the upload follows the feat→develop and develop→main merges and the GitHub Release, and is the last step of the flow.

- **On a host whose backend cannot enforce a mode, the default enforces nothing for the filesystem.** A Windows Job Object has no filesystem facility, and a Linux host that refuses an unprivileged user namespace — a stock Ubuntu 24.04, which is `ubuntu-latest` — takes the portable floor. This is 0.40.0's finding and **0.47.0 is the release that closes it**; the honest cost of shipping the default first is one release in which "contained by default" is true of the API and not of every host. It is reported three ways: `EventKind::Contained`'s `backend`, the per-command `SandboxEvent` rows, and the agent's own prompt.

- **A defect the default flip would have shipped, found by an existing test rather than a new one.** A contained spawn is of the backend's *wrapper* (`sandbox-exec`, `unshare`), which exists, so a missing payload came back as the wrapper's own non-zero exit instead of `[exec unavailable]` — and with containment defaulted on, every "no such program on PATH" would have become "your command failed". Fixed at the root by resolving the program against `PATH` before a contained spawn, so the answer is the same contained or not. It deliberately does not decide *executability*: a file that exists and cannot be executed is a real failure with a real message, and reporting it as "not installed" would be worse than the operating system's own answer.

- **Four existing tests changed, each with its reason in its own comment.** Three of 0.45.0's prompt tests now ask for `with_full_access()` where they assert the *absence* of a boundary — a contained run has one, and what they assert is that a run enforcing and confining nothing is still told nothing. The fourth, `a_one_shot_run_emits_no_deltas_and_a_session_turn_does`, sees `Contained` beside 0.45.0's `PromptComposed`; an additive variant on an enum `#[non_exhaustive]` since 0.24.0 is what this crate has told observers to expect, and the claim that test is really making is asserted separately and untouched. Changing a test because the behaviour deliberately changed is legitimate; doing it without the sentence is not.

- **The read-only fixture cannot be a `tempfile::tempdir()`, and this is the second release to pay for that.** The macOS profile blanket-allows `/private/var/folders`, so a read-only workspace placed there is writable no matter what this release does and the test would have passed on the development host while asserting nothing. 0.40.0 moved the *escape* targets under `target/` for the same reason; F3's workspace moved for it too.

- **A sabotage that appears to survive must first be checked for having applied.** One run of the sabotage pass reported the full-access arm as surviving; the patch had been written against pre-`cargo fmt` source, matched nothing, and sabotaged nothing. Re-applied against the formatted text it fails F2 immediately. A harness that does not assert its own patch landed manufactures findings — recorded here because the false finding is cheaper to prevent than to investigate.

- **The default flip found a defect in `shell` that no local gate could see, and CI is what caught it.** The Linux wrapper *enters* the directory it is handed, and the shell tool was handing it the workspace root while setting each stage's own `cd` target through `Command::current_dir`. The wrapper's `cd` runs inside the namespace and after the spawn, so it won: `cd src && rustc --crate-type lib lib.rs --out-dir ../out` ran in the workspace root instead of in `src`. Uncontained it never mattered and containment was opt-in, so nothing had ever exercised it — and `sandbox-exec` never changes directory, so **every macOS gate passed and both `ubuntu-latest` legs failed**, on `a_sequence_with_cd_runs_every_stage_in_the_right_directory` (`tests/shell.rs`), a test written in 0.25.0 for something else entirely. Corrected before the merge into `main` and the record re-pinned: the wrapper is told where to run, and what may be written is the writable-root list, which now carries the workspace root explicitly — and does not under `ExecMode::ReadOnly`, where naming it would hand the workspace back through the side door. The general lesson is 0.40.0's again from the other direction: **a platform whose backend does less is a platform whose gates prove less**, and a release that changes a default is exactly where that gap opens.

- **A caller cannot supply a writable root, deliberately.** The roots are derived from the mode, the workspace and the detected toolchain; a program whose agent writes to a configured output directory outside the workspace has only `with_full_access()` today, which is wider than it needs. Adding the knob later is additive; removing a badly-shaped one is not.

- **`ReadOnly` does not imply denying egress**, and the toolchain cache roots are not granted under it. The policy remains the one authority on the network, and a read-only run has nothing to build. Both are open questions against 0.48.0, where sandboxed egress becomes per-host.

- **The `shell_start` / `shell_poll` / `shell_kill` handles are still not contained**, and per-tool modes are still not a thing. Both are named in 0.48.0 and neither was borrowed forward.

- **N5 was measured on one host and one backend.** The ~6.25 ms per command is `macos-sandbox-exec` on the development machine; a Linux namespace and a Windows Job Object have their own costs and this release does not claim them.


### 0.45.0 known limitations

- **The record was sealed before the crates.io upload.** `mode: published` states the authorized target; the upload follows the feat→develop and develop→main merges and the GitHub Release, and is the last step of the flow.

- **F3 was corrected before implementation and the correction is `US-IO-HARNESS-0.45.0-I01`.** The contract asked for byte-identity with 0.44.0 while also requiring the crate's ending sentence to be emitted last; the ending was not last in 0.44.0 (it sat inside the base description, ahead of the tool and skill catalogues), so the two could not both hold. The criterion became a byte-exact prefix plus a byte-exact suffix, which still admits no sentence being added, dropped or reworded — and admits exactly the one relocation the release makes on purpose. Approved by the owner before any code was written.

- **One sabotage did not fail the test whose criterion it attacks, and that is a finding.** Emitting the ending before the catalogues — 0.44.0's order — fails `the_ending_follows_the_catalogues_it_used_to_precede` and `no_caller_prompt_can_get_past_the_ending`, while F3's own no-catalogue arm still passes. With no extra tools and no skills the two orders produce the same bytes, so a test written without a catalogue would have rubber-stamped an implementation that relocated nothing. The catalogue-bearing arm exists for exactly that reason and is what makes F3 discriminating.

- **Family shaping ships the mechanism and one difference.** `PromptFamily` has three variants and today only `Anthropic` delimits differently (tagged blocks, as that vendor's guidance asks for); `OpenAi` and `Generic` share the plain form. This is stated in the type's own documentation rather than left for a reader to notice: the enum exists so a family *can* differ when there is a reason to, not so that each one must. No claim is made anywhere that one family's wording performs better than another's, because this release cannot measure that.

- **A rule an approver remembers mid-run is not reflected in the boundary section.** The prompt is composed once — which F10 requires, since 0.38.0's cache breakpoint sits at the end of this block — and a remembered rule widens what is permitted, so the section stays conservative rather than wrong. A plan gate *is* reflected: the narrowed policy is what the planning prompt describes, and the loop already switches prompts when the phase ends. Documented in `docs/CONTRACT.md`.

- **Single-file mode gets no boundary section and no ending.** It enforces no policy (0.4.0) and has no turn to classify, so there is no rule for a caller's prompt to weaken and nothing true to say about a boundary. `SystemPrompt` still applies there. Stated rather than left to be inferred from a test that only covers the workspace loop.

- **Repository-controlled text now speaks in the system block, deliberately.** What bounds it is structural — delimited, framed as the repository's guidance, emitted before both the boundary section and the ending, and granting nothing because the `Policy` is enforced before any call runs. What the crate asserts is the composition. **What a model does with a prompt is not a claim this crate can make**, and nothing in this release claims it.

- **The release's premise is not measured.** That telling an agent its boundary reduces refused steps is the reason the section exists and is not an acceptance criterion: measuring it needs a matched pair of long live runs under an identical policy, and the difference would be confounded by the model's own variance. It is recorded as an open question.

- **One existing test's expected event sequence was changed**, with the reason in its own comment: `a_one_shot_run_emits_no_deltas_and_a_session_turn_does` (`tests/session_streaming.rs`) asserted the exact sequence a 0.19.0 consumer sees, and `PromptComposed` now arrives before the first step. An additive variant on an enum `#[non_exhaustive]` since 0.24.0 is what this crate has told observers to expect; the claim that test is really making — that streaming adds no `Token` events to a one-shot run — is asserted separately and is untouched.


### 0.44.0 known limitations

- **The record was sealed before the registry upload.** `cargo publish` is done by hand and last, after both merges and the GitHub Release exist. `mode: published` describes the release's target and its completed flow, not a step taken before this line was written.

- **The marker is always one turn behind the boundary, and this is deliberate.** The step a fold happens on presents a prefix that has been sent zero times, so it is never marked; the step after it is. A note written mid-run withdraws the marker for exactly one step for the same reason. The alternative — marking whenever a fold exists — buys one turn and pays a cache write every time the prefix moves, which is the one way this feature could cost money instead of saving it.

- **A run whose stubs collapse never presents a stable prefix, and this was not measured on a real long run.** When elision's stub ceiling is crossed, `assemble` emits a single line naming how many observations were elided — a count that changes as they accumulate — and it is emitted *ahead* of the carried entries. Such a turn's candidate prefix differs every step and the guard correctly withholds the marker. It should be rare after a fold, because a fold leaves few entries to stub, but "should be rare" is a prediction and not a measurement. Recorded as an open question rather than asserted either way.

- **A request carrying an image gets no transcript breakpoint on Anthropic.** That wire orders image blocks before the text, so there is no text prefix to mark. The OpenAI-shaped wire puts text first and does honour it. Both are asserted; the difference is a property of the two vendors' orderings.

- **A prefix under the vendor's minimum cacheable length is silently not cached**, the marker inert and `cache_read_tokens` zero with no error to read. 0.38.0's limitation, unchanged and restated because the second breakpoint inherits it.

- **A run cached through OpenRouter still under-reports what the write cost.** That wire reports no cache-write counter, so `Usage::cache_write_tokens` was zero on every call of the live run, including the ones that must have written the entries. The crate does not infer the write from the prompt length.

- **The live measurement is one vendor, one route, one model, one moment.** It says the marker reaches a vendor that honours it and that the counters come back. It says nothing about any other vendor, and the second call's saving depends on the entry still being alive, which is the vendor's clock.

- **A resumed run re-earns the marker.** `PrefixGuard` is run-scoped and not restored, so a resume spends one unmarked step rebuilding the comparison. Correct rather than merely acceptable: from where a resumed run stands it has sent that prefix zero times.

- **F8 was met in substance and not in its prose's arithmetic — stated, not glossed.** The criterion named "exactly two `CacheMarked` events for four marked steps". The test asserts exactly two events across two distinct prefixes and that marked steps strictly outnumber events, which is the claim that discriminates; the literal count of four marked steps was a plan-time guess about how many steps the fixture would run after its fold and is not pinned. Every substantive half — fires on change, not per step, `prefix_bytes` equal to an offset actually sent, none at all in a run that cannot fold — is asserted.

- **A SABOTAGE FAILED NOTHING, and the finding was about the test rather than the code.** The first `trim_start()` sabotage of F1 passed every assertion, because the fixture's remainder began with a letter and trimming its start was a no-op. The fixture now carries whitespace on **both** sides of the split; both trim arms then fail F1 alone. Reported rather than quietly repaired: the test was only half discriminating until this was run, and F1 is the assertion the whole release rests on.

- **`cargo clean` was not run before `cargo package`.** The standing gate list pairs them; the package was taken on a warm tree. `cargo package` unpacks and compiles the packaged sources in `target/package/io-harness-0.44.0`, so the artifact and its verification come from the packaged files either way, and CI's release job runs the gate on a clean checkout of `main`.

- **O1's three-platform matrix is CI's, not this machine's.** Every gate above ran on macOS. Nothing in this release spawns a process or shapes a path, so the matrix is the routine bar here; the `media` arm is what covers the two-wire image assertion.


### 0.43.0 known limitations

- **The record is sealed before the crates.io upload.** `mode: published` states the authorised target; the flow makes it true. The upload follows the `develop` → `main` merge and the GitHub Release, in that order, as it has for every release since 0.7.0.

- **F3 is over-determined, and only the combined sabotage discriminates.** Two independent mechanisms satisfy it: `restore_ledger` replaying the stored folds, and `compact_ledger` looking a summary up by the boundary it is about to fold at. Removing either alone fails **nothing**; removing both fails the named test. Reported rather than propped up, and the second mechanism is kept because it is what makes an identical replay free even when the first has nothing to replay.

- **F5's sabotage is a hang, not a failing assertion.** Removing the once-per-step bound makes the suite non-terminating rather than wrong, because an unbounded recovery is an infinite loop. It was run under a 420-second timeout and recorded as a hang. That is the honest shape of the evidence, and it is why the test asserts a refusal *count* rather than an outcome.

- **A summariser that returns empty text costs one call per step and folds nothing.** The fold declines to replace the notes with nothing — stubbing is strictly better than an empty paragraph — but it does not remember that the attempt failed, so a model that keeps returning empty text is asked again at the next step that crosses the threshold. Bounded by the same order of cost as the retry policy, and not worth run-scoped state until a real provider does it.

- **`Compaction` is deliberately not `#[non_exhaustive]`**, unlike most structs this crate adds. It is `ContextBudget`'s sibling — same module, two knobs, the same `#[serde(default)]` config story — and every caller and doctest writes `Compaction { .. ..default() }`, which `#[non_exhaustive]` refuses outside the crate with `E0639`. The cost is stated rather than hidden: a third field would be a break.

- **A summary can be wrong, and a paragraph is less visibly incomplete than a stub.** `keep_recent` keeps the newest observations whole beside it and the folded ones stay in `ledger_observations`, but nothing checks that the model summarised the thing that mattered. Stated in `docs/CONTRACT.md` as a property of the feature.

- **The overflow signature list is a heuristic and is not exhaustive.** A vendor wording it does not know costs exactly what an overflow cost on 0.42.0. Deliberately asymmetric in the safe direction; a broader match is the tempting fix and the wrong one.

- **No live provider was asked to produce a real over-window 400.** Every criterion here is met with fixtures, which is what makes them deterministic; whether a real vendor's wording matches the list is recorded as an open question rather than gated on a third party's release schedule.

- **`US-IO-HARNESS-0.43.0-I01` corrected two claims the contract made about how a fold survives a resume.** The row is keyed on `folded` rather than on the step, and `restore_ledger` replays the stored folds. Both prose sites were corrected, not only the scope bullet — the 0.42.0 lesson about grepping the whole contract for a claim, applied at implementation time this once rather than at sealing time.

- **docs.rs is not verified at seal.** The nightly gate passed locally and in CI on this tree, so it builds; the hosted build is queued after the publish. Check it once rather than assume.


### 0.42.0 known limitations

- **Three public items were added beyond what the scope prose named, both for the same reason — the type system asked for them.** `Approver::self_approval_allowed` is a **third** defaulted trait method, not the two the scope named: the run loop holds a `&dyn Approver` and could not otherwise read a knob that lives on the concrete `ModelApprover`. And `FileChange::new` / `with_before` / `not_kept` plus `ChangeReview::new` were added because both types are `#[non_exhaustive]`, which forbids the struct literals their own doc examples were written with. All four are additive and none is a break; they are stated here rather than left for a reader to notice the record and the scope disagreeing.

- **A `run_tree` start still does not run `preflight_review_and_routing`.** That is pre-existing, since 0.34.0, and this release covers only its own half of it: `refuse_self_approval` is called from the tree entry as well as from the preflight, so a self-approving model is refused in a tree. The review-criterion checks and `Routing::require_primary` still are not, and closing that would be a behaviour change to trees that this release did not promise.

- **A reviewer that does not override `review_change` sees the outcome and not the change.** That is the price of zero breaks and it was the owner's decision, taken with the alternative stated: a field on `ReviewRequest` would have reached every reviewer and cost the release its zero-break streak. `ModelReviewer` — the reviewer almost everyone uses — overrides it.

- **A file whose restore point was not kept is reported as `unkept`, and this is untested.** Over the store's 1 MiB snapshot cap, or not UTF-8, the reviewer is told the before was not kept rather than being shown an empty one; a path with no snapshot row at all reports "no restore point was recorded". Both are the deliberately conservative answer — claiming a run created a file it only rewrote is the one wrong answer a reviewer could not detect — but no criterion in this contract exercises either, so they are argued rather than proven.

- **A file the run wrote and something then removed is omitted from the change**, exactly as `written_files` has always omitted it. There is nothing to review, and reporting it as an empty `after` would read as a deletion of the whole file.

- **A `before_tool` hook blocks the loop's thread while it runs.** It is a synchronous spawn, like every `[[hook]]` since 0.28.0, bounded by `timeout_ms`. An unfiltered hook therefore costs one process spawn per tool call and slows the step it is in; the `tools` filter is how that is paid for, and the guide leads with it rather than mentioning it.

- **A plugin declared at local or user scope can contribute a `before_tool` hook.** That follows from 0.35.0's rule unchanged — a project-scoped `io.toml` plugin may contribute no `[[hook]]` at all, and a locally-declared one may contribute all six kinds — and plugin manifests go through the same validator. It is stated because the capability a hook now has is larger than the one that rule was written against.

- **The approving model is a filter over the grey tier, not a defence against a hostile workspace.** A write's content reaches its prompt, so a file the agent did not author can address it. The policy's denies never reach it, it cannot rewrite an action and it cannot remember a rule, so the worst a successful injection buys is an approval of something already marked as a question — but `DenyAll` remains the honest posture for a run that must never act, and `docs/CONTRACT.md` says so in those words.

- **The record was sealed before the crates.io upload**, which follows the `develop` → `main` merge and the GitHub Release. `mode: published` states the authorized target; the flow makes it true.

- **A record can never cite CI on the commit that contains it.** The evidence above is from the commit before this one; the commit carrying this record has its own run, and that ordering is stated in the merge and the final report rather than amended in here.


### 0.41.0 known limitations

- **N3 was met in a different form, and this is stated rather than glossed.** The criterion says `docs/public-api.txt` diffs by exactly three lines. It diffs by exactly **one** — `enum ToolEffect src/tools/custom.rs`. The checker enumerates the names re-exported from `src/lib.rs`, so `Tool::effect` is a method on an already-listed trait and `TaskContract::max_parallel_reads` / `with_max_parallel_reads` are members of an already-listed struct, and neither adds a line. The substance of N3 holds in full: three items added, none removed or altered, and a consumer who never calls the new method and never sets the new field compiles unchanged. This is the same shape as 0.39.0's deviation; plan such a criterion as "the lines the file can see" next time.

- **`view_image` is `Mutating`, and the contract's two lists did not settle it.** It is absent from the three named `ReadOnly` built-ins and absent from the enumerated `Mutating` ones, while the scope prose anticipated carrying its media back through the fold. It was left mutating: the explicit read-only set names three tools, no criterion mentions media, and the choice keeps `PendingMedia` unshared, which the scope itself gives as the goal. The consequence is that a completion mixing `view_image` with reads splits into two batches at the image.

- **`list_dir` is `Mutating` too**, for the same reason — it reads the workspace and changes nothing, and it is outside the set this release measured. Widening to it costs nothing structurally and was not taken without evidence.

- **The fold-order guarantee is proven by F6, not by F2.** F2 proves the two caps produce identical stored rows, which is what an embedder cares about; it cannot discriminate a fold that used completion order, because synchronous file reads finish in the order they were spawned. Anyone strengthening this later should keep F6's uneven settle, which is what makes the two orders different answers.

- **The record was sealed before the crates.io upload**, which follows the `develop` → `main` merge and the GitHub Release. `mode: published` states the authorized target; the flow makes it true.

- **A record can never cite CI on the commit that contains it.** The run above is on `cdeac4e`; the commit carrying this record has its own run, and that ordering is stated in the merge and the final report rather than amended in here — the 0.29.0 and 0.38.0 precedent.

- **MCP tools stay `Mutating` and the open question is unanswered.** Whether `McpSession` can multiplex concurrent requests over one stdio transport, and whether servers in the wild tolerate it, is recorded for the release that widens the read-only set rather than guessed at here.

- **Concurrency across steps or completions is untouched**, as is `max_concurrent_agents`. The unit is one completion's tool calls.


### 0.40.0 known limitations

- **N2 was met in a weaker letter and a stronger substance, and this is the second release running to hit it.** The criterion said `docs/public-api.txt` "gains the lines for `TaskContract::exec_sandbox` and `TaskContract::with_contained_exec`". It gains **none** and is **byte-identical**. The file enumerates the names re-exported from `src/lib.rs`; a field and a method on an already-listed type are not re-exports, and hand-adding lines fails the checker. Byte-identical is the stronger zero-break claim, not a weaker one. 0.39.0 recorded the same trap and the criterion was still written this way — plan such a criterion as "unchanged".

- **F6 cannot be verified on the machine this release was built on, and that nearly shipped a boundary that existed nowhere.** The Linux assertion runs on `ubuntu-latest`; a macOS developer host can only run the macOS arm, and every local gate passed throughout while Linux confined nothing. It is verified now — asserted on the runner, on the native backend, read out of the job log — but the standing limitation is the shape of the risk, not this instance of it: a platform-specific boundary is only ever as true as the platform that runs its test, and this project has exactly one such platform in CI and none on the developer's desk.

- **Linux confinement is verified on a host whose apparmor restriction has been lifted, and that is not the Ubuntu 24.04 default.** CI now sets `kernel.apparmor_restrict_unprivileged_userns=0` before the suite, which is what most distributions ship and what makes the namespaces backend reachable at all. The honest consequence for a user: on Ubuntu 24.04 with the stock setting and no privilege to change it, a contained command takes `PortableFloor` — the resource caps still apply, and the filesystem confinement and egress denial **do not**. The backend reports the floor and the `SandboxEvent` row records it, so this is legible rather than silent, but a caller who assumes containment on a stock Ubuntu 24.04 host will not get the filesystem half. This is the single most likely way for this release's promise to be misread in the field.

- **The two escape tests assert the floor on a floor host rather than skipping**, which is what F6 demanded and what the first implementation did not do. The consequence worth stating: a green suite on an unknown host no longer proves confinement — it proves the backend's *reported* behaviour matches what it applied. Which of the two branches ran is the thing to read, and on the ubuntu legs after this fix it is the native one.

- **This record cites CI on a commit that does not contain it.** A release record can never cite a run on the commit that carries the record, because the record is written first. The ordering is stated in the merge and the final report rather than amended into the sealed file — the 0.29.0 precedent, applied for the fourth time.

- **The record is sealed before the registry upload.** `mode: published` is the delivery target and the flow is fixed: both merges, then the GitHub Release, then `cargo publish` by hand, last. The upload follows this seal rather than preceding it.

- **N6's macOS cost was measured by probing the mechanism, not by running a cold `cargo fetch` or an `npm install` end to end.** `sandbox-exec` with the crate's own profile was given three writes: inside the workspace (succeeded), into a second temporary directory (succeeded, because the profile re-allows the whole of `/private/var/folders`), and into `$HOME` (refused, `Operation not permitted`). The third is exactly the claim — a toolchain populating `~/.cargo/registry` or `~/.npm` fails under containment on macOS — and it decided the escape tests' target directory. A full cold toolchain run was not performed.

- **Egress under containment is one boolean per run and cannot be otherwise today.** A policy that allows exactly one host gives a contained command a route to every host. Per-host filtering at the sandbox wall means a proxy or a packet filter, therefore a dependency and a daemon, both of which the crate's standing constraints refuse. The coarseness is asserted by a test rather than only documented.

- **`Effect::Ask` on `Act::Net` counts as not permitted, and it has no honest mapping.** A namespace is built before the command starts and an approver answers about one action at the moment it is attempted, so the sandbox cannot ask. Denying is the safer of two surprising answers; it remains an open question rather than a settled one.

- **The Linux mount fix reaches the verification gate too, and that is a real behaviour change beyond this release's own scope.** The gate uses the same `LinuxSandbox`. A gate command that wrote outside its working directory on Linux succeeded before this release and fails after it. That is the boundary the module always claimed arriving late rather than a new restriction, and it is in the changelog under Changed and in `docs/guide/sandbox.md` rather than left to be discovered.

- **`shell_start` / `shell_poll` / `shell_kill` handles are not contained**, and a contained `shell` stage is contained slightly less than an `exec` command: it gets the wrapper, the filesystem confinement, the egress denial and the CPU and open-file rlimits, but not the memory monitor, and a stage killed by a cap is not attributed to a `Cap`. Both are stated at the field and in `docs/CONTRACT.md`.

- **Two criteria, N7 and N8, were added during the completion phase** by `US-IO-HARNESS-0.40.0-I01` on owner request, after every functional criterion had passed. They are documentation only — every io-cli link removed with the product marked "Not built", and the README brought up to what the crate does at 0.40.0. `src/lib.rs` is edited, so `fmt`, `clippy`, the nightly docs command, the doctests and both suite polarities were re-run afterwards and are the figures reported above. `CHANGELOG.md` and `.ultraship/views/` still name io-cli and are deliberately untouched, because they record what past releases said at the time.


### 0.39.0 known limitations

- **This record was sealed before the merges, the GitHub Release and the crates.io upload**, which follow it in that order. `mode: published` states the release's target and what was actually done is reported with the merge. A record can never cite CI on the commit that contains it: the three-OS matrix runs on the pull request that carries this commit, so O1's evidence is stated in the merge and the final report rather than amended into a sealed record — the 0.29.0 precedent.

- **N2 was met in a different form than its prose names, and this is the deviation.** The criterion said `docs/public-api.txt` gains exactly two lines. It gains none: `tests/public_api.rs` enumerates the items re-exported from `lib.rs`, and `Session::turn_contained` and `Session::turn_contained_observed` are inherent methods on the already-listed `struct Session`. The substance is stronger than the criterion asked — the crate root's surface is byte-identical — and both methods carry a worked doctest anyway. Adding lines by hand would have failed the checker, which compares the parse against the snapshot.

- **A paused contained turn's row reports the pause, not the continuation, and it reads `running` rather than `awaiting_approval`.** `Session::drive` closes the turn with what `Store::run_summary` said when the turn returned, and a run parked on an approval has no summary outcome yet. F8 asserts the value as measured rather than as first assumed, and `docs/CONTRACT.md` states it. The run is the record that moves; there is no session-side refresh, which was not scoped here.

- **One sabotage failed nothing, and it is a finding rather than an omission.** S4 handed a child its parent's policy verbatim instead of `parent_policy.contain(&overlay)`, and every test still passed — with an identical policy the boundary genuinely still holds. What F4 discriminates is *widening*, which S4b (a permissive child) does fail. Inheritance-without-narrowing is not observable through this criterion, and the criterion is about the narrowing.

- **The ledger is per turn, not per session**, by owner decision. A conversation has no single spend ceiling; its total is the sum of its turns'. Recorded in `docs/CONTRACT.md` and left in `open_questions`.

- **A contained turn cannot be steered.** `turn_contained` and `turn_contained_observed` ship without a `turn_contained_steered`; an operator stops a fan-out with `Flow::Cancel` from the observer, honoured at the root's step boundary. Whether a mid-turn message should reach children in flight is an open question rather than an omission.

- **No live provider exercised this release.** Every criterion is deterministic against fixture providers, which is what the contract specified — the fan-out's evidence is stored rows, outbound requests and refusals, none of which needs a vendor. No live smoke run was made.


### 0.38.0 known limitations

- **The transcript is not cached, and will not be until the assembler changes.** The breakpoint covers the instructions, the skill catalogue and the tool schemas — not the observation log, which `context::assemble` supersedes, invalidates, re-reads and re-fits on every turn and which is therefore not the byte-stable prefix a cache needs. This is the larger half of the available saving and it is deliberately unclaimed; the roadmap outcome for this version named "the settled part of the transcript" and the release does not deliver that half. Marking it would be billed as a cache write on nearly every turn, costing money rather than saving it.

- **A run cached through OpenRouter under-reports what it cost, and this was measured rather than predicted.** `cache_write_tokens` came back **zero on all four live calls**, including the one that must have written the entry the next call read, because that wire reports no write counter and `src/provider/openai_wire.rs:310` records zero by construction. The tokens that were written are therefore priced as ordinary fresh input, below what the vendor charged. Not closed by inferring the write from the prompt length — that would put a number in the trace the invoice does not contain.

- **A prefix used exactly once now costs more than it did in 0.37.0** — roughly a quarter more on that block, since a cache write is billed above a fresh read. The break-even is the second use. Accepted deliberately: a run makes more than one call and a session more than one turn. There is no switch to turn it off; an operator whose endpoint rejects the key uses `Compatible`, which never sends it.

- **A prefix below the vendor's minimum cacheable length is silently not cached**, and a cache entry expires on the vendor's own clock. In both cases the marker is accepted, the response is normal, and `cache_read_tokens` is zero with nothing to say why. The crate declares a cache; it does not operate one.

- **The live evidence is one vendor, one route, one model, one moment.** It was taken through OpenRouter because that is the only provider with working credentials in this checkout — the Anthropic and OpenAI entries in `.env` are empty placeholders — so the **native `Anthropic` provider's marker has never been exercised against Anthropic's own endpoint.** Its body is asserted by F1 and the wire shape is the vendor's documented one, but no live call has confirmed it end to end.

- **F4 deviates from its own wording and the substance is met in a different form.** The criterion named an `examples/` fixture with `required-features`; the fixture carries none, because the two existing live examples are gated only for optional *dependencies* and this one needs none — inventing a feature would have added a name to the public feature surface in a release whose claim is that it adds nothing. It is an example, so no CI job runs it.

- **This record was sealed before the crates.io upload.** `mode: published` states the authorised target; the upload follows the merge into `develop`, the release PR into `main`, and the GitHub Release, in that order.

- **A first attempt at the live evidence was refused with HTTP 402** against `anthropic/claude-sonnet-4.5`, because the account's remaining credit could not cover that model's default maximum output — the OpenAI wire sends no `max_tokens`, so the vendor applies the model's own. Re-aimed at a cheaper model of the same vendor rather than worked around, and recorded because the absence of a `max_tokens` on that wire is a real property of the crate.


### 0.37.0 known limitations

- **The tree this release shipped is not the tree this record was first sealed against, and every gate was re-run rather than assumed.** After `feat/0.37.0` merged into `develop`, the owner merged two Dependabot pull requests into `develop` above it: **#114 `rmcp` 3.0.0 to 3.1.0** and **#115 `zip` 2.4.2 to 8.6.0**, the second a major-version jump inside the optional `documents` feature. The owner decided on 2026-08-06 that 0.37.0 ships with both, on the condition that the whole gate set is re-run first. It was, on `develop` at 79b47ca: `cargo fmt --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` exit 0 with **63 suites and 1435 passed**, identical to before; `cargo test` exit 0 with **63 suites and 1356 passed**, identical to before; `cargo test --test cross_version_0_29_0 -- --ignored` 1 passed; the nightly `docs.rs` command clean; `cargo tree` still **402**; and `cargo clean && cargo package --locked` producing 1,336,755 bytes. Nothing this release claims changed. What did change is that the record was amended and re-pinned after being sealed once, which is only legitimate before the merge into `main` — it had not happened, and the amendment preceded it.

- **This record was sealed before the crates.io upload.** The publish follows the develop-to-main merge and the GitHub Release, by hand, and is always last.

- **The system-prompt wording was never put in front of a live vendor.** The contract's `resource_profile` expected "live runs against at least two vendors before the wording settles", and its risk register named that as the mitigation for the one genuinely uncertain part of this release. It did not happen: every arm of every criterion is driven by a scripted provider. What is proven is that the wording reaches the first completion and only the first, that a scripted completion which stops on text closes as a reply and one carrying a call promotes — that is, the machinery. What is **not** proven is how often a real model answers in prose where it should have acted. That is the release's accepted asymmetry and its size is unmeasured. It is stated in `docs/CONTRACT.md` under the same heading, so a reader meets it where they look for limits, and it is the first thing a 0.37.1 should measure.

- **`EventKind::Answered` carries `turn_id` only, not `{ turn_id, run_id }` as the contract's prose named.** Stated rather than glossed: the kind is flattened into the event envelope, so a `run_id` field is a duplicate serde key and the variant would not round-trip — the same constraint `EventKind::Rewound` records in its own doc comment, and `every_variant_round_trips` is what would have caught it. Which run served the turn is the envelope's own `run_id`, where every other variant reads it from, so the criterion's substance — an attached process can tell an answer from a run, and knows which run, without reading the store — holds in full.

- **F1's "snapshot row count is zero" is read through `rewind_run`, not off the `snapshots` table.** The store exposes no public listing of snapshots (`snapshot_paths` is `pub(crate)`), so rather than widen the public API for a test, the assertion is behavioural: a rewind of the reply's run puts **0** files back, and a rewind of the control arm — which writes a file — puts **1** back. The same fact, stated as a behaviour, with the control that makes the zero mean something.

- **`cargo tree` was counted on macOS only.** The contract asked for macOS and Linux. The Linux legs of CI build and test the crate but do not count the tree, and no dependency edge in this release is platform-conditional — nothing was added at all. The number is 402 on this machine and the claim for Linux rests on the manifest being unchanged rather than on a second count.

- **`Store::check_resumable` now refuses one run it did not refuse before**, and that is wider than "a reply". A session turn typed as a classifying turn and left `running` is refused whatever it *would* have become — including one killed after its first completion reached for a tool but before the row was corrected to `run`. That window is one completion long and holds no committed step, so the refusal costs a re-ask rather than work; but it is a behaviour change to a public method for a class of run slightly larger than the one the criterion named.

- **A run that answers is still a `runs` row.** "How many runs did this session do" is answerable only by reading `turn_kind`, which is `pub(crate)`. That was deliberate — the fact reaches a caller as `TurnResult::kind`, and a second public reader is a second thing to keep true — but it means a reader with only a `Store` and a run id cannot ask the question. Recorded because the contract's own `business_value` names that query as one of the costs of the old behaviour.

- **`turn_kind` is not read on any resume path except `check_resumable`.** A reply cannot be resumed, so nothing else needed it; if a later release makes a promoted turn distinguishable in the trace (an open question below), that is where it will be read.

- **A pre-existing test flaked once on the release branch and is recorded rather than waved through.** `worktree::two_concurrent_children_with_their_own_worktrees_do_not_collide` — a 0.36.0 test, untouched by this release — failed once on `test (macos-latest, default)` in run 31064349963, finding **one** worktree where it expects two. That run's source is byte-identical to run 31063716477, where the same job passed: the only diff between the two commits is nine files under `.ultraship/`. It passed on re-run of the same job, and 8 of 8 locally (3 full `--test worktree` runs plus 5 of the single test). So it is a flake and not a regression, and it is not this release's. What it most likely indicates is real and belongs to 0.36.0's feature: two children running `git worktree add` concurrently in one repository contend for the index lock, and the loser's spawn fails rather than sharing the parent tree — which is the behaviour 0.36.0 chose, seen from the outside as a missing worktree. Nobody has confirmed that cause. It is written here so the next person who sees it starts from evidence rather than from scratch.

- Every `known_limitations` entry in `docs/CONTRACT.md` that predates this release was left standing. None was deleted on the strength of a release having probably fixed it.


### 0.36.1 known limitations

- **This record was sealed before the crates.io upload.** The publish follows the develop-to-main merge and the GitHub Release, by hand, and is always last.

- **F8 was met in a different form than its prose named, by owner decision on 2026-08-04.** The criterion asked for io-cli to link to its repository AND to crates.io; the owner asked for the crates.io link to be removed from the table. The row keeps the repository link and the Released status, both verified against the registry and the repository, so what the table claims is unchanged — one of two links was dropped, not a claim.

- **The contract's own prose carried three stale figures, each found by deriving rather than trusting it.** F3 enumerated five fixture examples; reading `tests/` returns EIGHT (`tests/handles.rs` also spawns `tick`, `orphan` and `flood`). The plan says 251 doctests; the baseline log says **366**. The plan records io-cli at 0.10.0 with 186 downloads; the registry says 0.11.0 and 219 — which is exactly why the table states status rather than numbers. In each case the criterion was met against the derived value, and the criterion was phrased to derive for precisely this reason.

- **`rust-lld` is proven on Windows only.** Linux and macOS keep their default linkers. Neither is the critical path and ELF and Mach-O are separate questions needing their own probe; nothing here claims otherwise.

- **`rust-lld`'s attribution comes from two legs, not three.** Warm-to-warm on the same leg it took the Windows doctest step 338s → 200s (-41%) and the Windows default build step 253s → 205s (-19%). There is no warm no-lld measurement of the Windows `--all-features` leg: that cache key is new in this release and the runs that would have populated it were cancelled, twice by a push to the same branch under `concurrency: cancel-in-progress`. Its 449s and 462s figures are cold-cache and are not used as a baseline.

- **`CARGO_PROFILE_DEV_DEBUG=0` was measured and remains excluded.** The owner declined it for this release to keep the backtrace frames that diagnosed the 0.21.0 Windows CRLF failure. Measured anyway on the doctest job, where a backtrace frame has the least value: 184.0s → 166.0s, **9.8%**, about 24s on the Windows doctest job. Not worth reopening a recorded decision. The number is written down so nobody re-measures it.

- **The next lever was identified, costed, and deliberately not taken.** 81% of the remaining build time is 59 test binaries linking (`cargo build --lib` 15.7s against `cargo build --tests` 84.0s on one tree), which is fully parallel; sharding them two ways was estimated at ~4m15s and would take the job count from 15 to 17, under the 20-concurrent ceiling. It is not in this contract — the contract's own open questions park it as a later lever — and the owner's decision on 2026-08-04 was to lock 0.36.1 here. Recorded so a later release knows it was weighed and by whom.

- **Three dependencies are behind and this release does not move them.** deps.rs reports 3 of 25 outdated. Out of scope for a patch release whose subject is CI wall clock and documentation, and this repository has an entire release (0.23.0) about what a dependency bump can cost.

- Every `known_limitations` entry in `docs/CONTRACT.md` that the release brief lists as open was checked and still reads as open. None was deleted on the strength of a release having probably fixed it.


### 0.36.0 known limitations

- **This record was sealed before the crates.io upload.** `mode: published` is the release's target and the flow's last step: the develop merge, the main merge and the GitHub Release all come after the record rides the `feat/0.36.0` commit, and `cargo publish --locked` comes after those. Sealing it `release-ready` and correcting it afterwards would mean editing an immutable record, which is worse.

- **The worktree slug carries a fourth component the contract's prose did not name.** `included` derives the path from `<agent>-<parent_run_id>-<step>`, which is not unique for the shape F3 actually names: two children of the same definition in the same step differ only in their goal. The slug therefore ends in an FNV-1a digest of the goal — the remaining part of the `(parent, step, goal)` key `find_spawn` adopts by. FNV is written out rather than `std::hash::DefaultHasher`, which is documented as unstable across releases; a slug that moved on rebuild would send a resumed spawn to a worktree that is not there.

- **N3 is met in a different shape and the difference matters.** Its prose asks for "a source-reading test in the shape 0.33.0 and 0.35.0 used" — those grep for a *module path*, which cannot work here because `rewind_run` lives in `src/run.rs` beside the loop and there is no module boundary to grep for. What is asserted instead is the load-bearing half: no `*_TOOL` constant names a rewind, and outside doc comments `src/run.rs` holds exactly three lines mentioning one — the two definitions and the delegation — with a splice-in control that makes the count four. Trap 70 applies: that is what it proves, not "the loop does no rewind work".

- **N4 lists two items the worked-example gate does not reach.** `GIT_BRANCH_TOOL` and `GIT_WORKTREE_TOOL` are `io_harness::tools::` constants rather than crate-root re-exports, so `every_public_item_has_a_worked_example` does not see them — and their five siblings carry prose and no example. They are documented to exactly that standard rather than being made the only two of seven with a doctest, which for a `&str` constant would assert its own spelling.

- **F7 compares a row count that is legitimately zero.** Its prose names `run_events` among the tables that must not move. A run driven by `run_with` rather than an observed entry point writes no `run_events` rows at all — the durable stream is `Broadcast`'s, not the loop's — so that count is 0 before and after. It is still compared rather than dropped, and the "the run left a trace" assertion is made against `steps`.

- **A worktree is visible to the parent, and this crate will not hide it.** The parent's own `git_status` reports `?? .worktrees/` — measured, not assumed — so a `git_add` naming `.` in the parent stages the children's trees. Writing to `.git/info/exclude` was considered and excluded: it is a write outside what the caller asked for and outside what the policy governs. The operator gets the one line instead.

- **Nothing removes a worktree or deletes a branch**, so both accumulate until an operator clears them. Removing a worktree deletes the work a child was spawned to produce, which is not a decision this crate makes.

- **A rewind bypasses the memory pin.** Restoring goes around `memory_write`, so an entry pinned *after* the run wrote it is still put back. The alternative is telling a caller a rewind happened when it had not.

- **A rewind is one run, not a tree**, and is not transactional. A caller wanting a subtree loops over it; choosing an ordering over children whose written paths may overlap is a decision this crate does not make.

- **A test failed once under disk pressure and passed twice afterwards.** `tests/handles.rs::a_handle_that_exits_on_its_own_stops_reading_as_running` failed in the first default-feature run on a volume at 11 GiB free with five rust-analyzer processes rebuilding into the same tree, then passed under `--all-features` and again on the re-run. It reads as a store assertion and is a wall-clock one: the exit is noticed by a poll that has to reach disk. This release touches no handle code. Recorded rather than quietly re-run.

- **The local suites ran with `CARGO_PROFILE_DEV_DEBUG=0` and `CARGO_PROFILE_TEST_DEBUG=0`,** and `target/` was deleted three times during the release (4.6 GiB, 12 GiB and a 6.9 GiB `cargo clean`). The volume reached 4.5 GiB free at its lowest. Debug info changes no test outcome and CI runs the unmodified commands on a fresh runner; recorded because the numbers above were measured under it.

- **Inherited and unresolved: 0.30.0's N2 wording.** "The aggregates do not scale with the trace" over-claims — counting every row is the answer. Raised rather than read as satisfied, and untouched by this release.


### 0.35.0 known limitations

- Nothing here verifies that a plugin directory is what its author published. There is no signature, no checksum and no provenance. The trust rule bounds what an untrusted bundle may *contribute*, which is a different claim and the only one the crate can honestly make.

- A hook or MCP server contributed by a plugin declared in a trusted scope runs a program with this process's privileges. Being introduced by a plugin neither sandboxes it nor changes the policy that governs it.

- Namespacing changes the names a model sees. A prompt or a skill that referred to another skill by its bare name stops matching once that skill moves into a bundle. The crate cannot rewrite prose, so this is documented rather than handled.

- The standing `[[mcp]]` gap is untouched: a project-scoped `io.toml` may still name an MCP command directly, and an unknown key inside an `[[mcp]]` table is still accepted because serde refuses `flatten` beside `deny_unknown_fields`. A plugin's `[[mcp]]` is refused at project scope, so the new surface is stricter than the old one — deliberately, and stated rather than smoothed over.

- `version` in a manifest is documentation. Nothing resolves it, checks it against the crate, or compares two bundles that contribute the same thing.

- **Inherited and unresolved: 0.30.0's N2 wording.** "The aggregates do not scale with the trace" over-claims — counting every row is the answer. Raised rather than read as satisfied, and untouched by this release.

- **The namespacing rule was applied to templates as well as the four the contract's prose named, stated rather than glossed.** The included list says "skills, agents, policy layers and MCP server ids"; templates are merged from several bundles by the same `merged`/`namespaced` pair and would collide in exactly the same way, so leaving them out would have been a rule with an unexplained hole. Nothing else was widened.

- **Contributions install through three calls, not one.** The contract's prose names `Plugins::apply_to(TaskContract)`. Policy and hooks are not on a contract — a `Policy` is passed to the entry point and a `Hooks` is an `Observer` the caller installs — so `apply_to_policy` and `apply_to_hooks` exist beside it. Three calls is the shape the crate already has; folding them into one would have meant this release inventing an install point.

- **`Plugins::templates()` is fallible where the rest of the type is not.** A `templates` key naming a directory that does not exist is a mistake inside a bundle that otherwise loaded, and reporting it as an empty catalogue would hide it. The infallible-loading claim covers the manifest, not the directories a loaded manifest points at.

- **N3 was met in the shape the crate already has, and the difference is worth stating.** Manifest loading is the caller's, before the run. A bundle's *skills directory* is read at run start by `discover_skills`, once, exactly as a contract's own skills directory has been since 0.9.0 — not per step, and not by the step loop. The source-reading test asserts `src/run.rs` names the plugin module only in documentation; `emit_plugins` reads the already-loaded value the contract carries and touches no filesystem.

- **`cargo package --locked` was run with `--allow-dirty`.** The working tree held one uncommitted file, `assets/initorigin-logo.png`, edited locally by the owner and unrelated to this release; it was deliberately left uncommitted rather than swept into a release commit. CI's `verify` job runs `cargo package --locked` on a clean checkout of `main`, which is the run that gates the Release.

- **This record was sealed before the crates.io upload.** `mode: published` is the release's target and the flow's last step: the develop merge, the main merge and the GitHub Release all come after the record rides the `feat/0.35.0` commit, and `cargo publish --locked` comes after those. Sealing it `release-ready` and correcting it afterwards would mean editing an immutable record, which is worse.

- **The local suites were run with `CARGO_PROFILE_DEV_DEBUG=0` and `CARGO_PROFILE_TEST_DEBUG=0`,** and `target/` was deleted twice during the release (13 GiB and 12.9 GiB). The volume reached 8.0 GiB free at its lowest. Debug info changes no test outcome and CI runs the unmodified commands on a fresh runner; recorded because the numbers above were measured under it.


### 0.34.0 known limitations

- A review is a model's judgement. It is not deterministic, not reproducible across model versions, and not a proof. It is recorded with its reasons so a human can disagree with it, and it never replaces an execution gate — the two compose.

- `allow_self_review` compares model *names*. Two different names can be the same weights behind a gateway, and one name can be two different snapshots a month apart. The refusal catches the obvious case honestly and cannot catch the disguised one.

- `Provider::reachable()` is a point-in-time answer, defaulted to `Ok(true)` for every provider that does not override it. It says nothing about the next minute, and a provider that stays silent about reachability makes `require_primary` a no-op rather than a failure.

- `retry_gate` re-runs the criterion against the workspace as it now stands. If something changed the tree between the failed gate and the retry, the retry grades the new tree. The crate does not snapshot the workspace at gate time.

- **F7 was met with `Error::Config`, not `Error::Unreachable`, stated rather than glossed.** The criterion's prose named an `Error::Unreachable`; no such variant exists in `Error` and adding one would have been a second break in a release whose whole N2 claim is that there is exactly one. `Error::Config` is the variant every other refusal-before-the-first-request uses — `ensure_web_supported`, the missing-reviewer refusal, the self-review refusal — and the discriminating half of the criterion is untouched: the run fails, the error names the provider, and the provider records **zero** completions.

- **One of the nine sabotages failed nothing, and it is recorded rather than hidden.** S8 replaced the consecutive gate-failure count with a cumulative one and no test failed. The reason is real and now documented at the function: a gate that passes ends the run, so no run can hold a pass followed by a failure, and the two readings coincide. The code counts consecutive anyway, for the case where that stops being true. A test asserting the difference today would have had to re-implement the counting rather than exercise it, which is theatre, so it was removed rather than kept green.

- **`INDEXED BY` was tried on the gate-attempt reads and removed, measured rather than assumed.** 0.33.0's tree tail needs the hint because a recursive CTE cannot be seeked into; these are plain `WHERE run_id = ? ORDER BY id` reads whose left prefix is the index's own, and the plan is identical with and without it across forty runs (sabotage S9 removed the hint and failed nothing; S9b removed the index itself and failed N3). The hint is left out where it buys nothing.

- **Routing governs the root agent only.** A spawned child takes the model on its `AgentDef`, exactly as it takes `Effort` there since 0.31.0. A rule that overrode a role's own model would be the roster's author being ignored by a counter. Stated in `docs/CONTRACT.md`.

- **The self-review refusal compares model NAMES, and can be unreachable.** Two names can be the same weights behind a gateway, and one name can be two snapshots a month apart. The model under review is read from `Provider::model_hint`, defaulted to `None` — so a provider that does not name its model makes the refusal impossible to reach rather than failing loudly. Both are in `docs/CONTRACT.md`.

- **This record was sealed before the crates.io upload.** `mode: published` is the release's target and the flow's last step: the develop merge, the main merge and the GitHub Release all come after the record rides the `feat/0.34.0` commit, and `cargo publish --locked` comes after those. Sealing it `release-ready` and correcting it afterwards would mean editing an immutable record, which is worse.

- **The local suites were run with `CARGO_PROFILE_DEV_DEBUG=0` and `CARGO_PROFILE_TEST_DEBUG=0`,** and `target/` was deleted twice during the release. The volume still reached 5.8 GiB free. Debug info changes no test outcome and CI runs the unmodified commands on a fresh runner; recorded because the numbers above were measured under it.

- **Inherited and unresolved: 0.30.0's N2 wording.** "The aggregates do not scale with the trace" over-claims — counting every row is the answer. Raised rather than read as satisfied, and untouched by this release.

- The reviewing model does not see the run's conversation, only the goal, the rubric and what was written. That is deliberate — a reviewer reading the author's reasoning is a reviewer being led — but it means a change whose justification lived only in the transcript is judged without it.


### 0.33.0 known limitations

- The crate does not detect whether the owning process is alive. `runs.status = 'running'` has never told a live process from a crashed one, and `Attach` does not change that: a run whose owner died still reports `Waiting`, and answering it writes a row that nothing will read until someone resumes. That is the same fact 0.7.0 shipped, exposed at a new surface rather than made worse.

- `Attach::poll` is a poll. Latency is the caller's interval, and the run's pickup of an attached answer is the gate's poll interval — not instant, and not push.

- `run_events` is never pruned by the crate. A long run's stream grows without bound and deleting it is the application's call.

- Attaching gives read and answer, not control. There is no attached steer, cancel or budget change, by design.

- **This record was sealed before the crates.io upload.** `mode: published` is the release's target and the flow's last step: the develop merge, the main merge and the GitHub Release all come after the record rides the `feat/0.33.0` commit, and `cargo publish --locked` comes after those. Sealing it `release-ready` and correcting it afterwards would mean editing an immutable record, which is worse.

- **The crate does not detect whether the owning process is alive.** `runs.status = 'running'` has never told a live process from a crashed one and this release does not change it. A run whose owner died still reports what it was holding through `Attach::waiting`, and answering it writes a row nothing will read until somebody resumes. In the other direction, `resume_*` refuses a request that has already been *decided* but cannot refuse one still held by a process that is still running. Both are stated in `docs/CONTRACT.md` and the module page; a liveness heartbeat is recorded as an open question because its false-negative story — a slow step looking dead — has to be designed rather than defaulted.

- **`Attach::poll` is a poll, at both ends.** Latency is the caller's own interval outbound, and `ATTACH_POLL` (200 ms) for an answer to reach a parked run. There is no push in either direction.

- **`run_events` is never pruned.** A long run's stream grows without bound and deleting it is the application's call. The crate has never pruned anything in the trace and did not start with its newest table.

- **A run only picks up an attached answer while it is *blocked in* its gate.** A gate that declines — `ResponderNone`, a `PlanGate` returning `None`, an approver deferring — pauses the run as it always did, and that pause is answered by `resume_*`, not by `Attach`. That is correct rather than a gap, but a reader could take "answer a live run" more widely than it holds.

- **F10 was met against 0.29.0 rather than 0.32.0, deliberately.** The criterion named "a database written by a real 0.32.0 build". The repository's pinned cross-version generator is `tests/fixtures/gen-0.29.0` with its own CI job, three releases back rather than one, and the 0.33.0 store it reads now carries a `run_events` row alongside the 0.30.0 memory rows and the 0.31.0 plan. Adding a 0.32.0 generator would be a second fixture crate and a second CI job for a strictly weaker claim. The substance — both directions executed, the 0.22.0 schema guard fed `run_events` and `run_events_run` by name — holds.

- **The two `cargo test` invocations were run with `CARGO_PROFILE_DEV_DEBUG=0` and `CARGO_PROFILE_TEST_DEBUG=0`.** With debuginfo on, `target/` reached 19 GiB and took the volume to 4.4 GiB mid-suite; the first attempt died with `No space left on device` inside an incremental copy. Debug info changes no test outcome, and CI runs the unmodified commands on a fresh runner. Recorded because the numbers above were measured under it.

- **Inherited and unresolved: 0.30.0's N2 wording.** "The aggregates do not scale with the trace" over-claims — counting every row is the answer. Stated honestly in `docs/CONTRACT.md` and raised rather than read as satisfied. Untouched by this release.


### 0.32.0 known limitations

- **This record was sealed before the crates.io upload.** `mode: published` is the release's target and the flow's last step: the develop merge, the main merge and the GitHub Release all come after the record rides the `feat/0.32.0` commit, and `cargo publish --locked` comes after those. Sealing it `release-ready` and correcting it later would mean editing an immutable record, which is worse.

- **The concurrency ceiling is per tier, not per tree.** A tree of depth *d* can hold up to `max_concurrent_agents * d` agents working at once. That is the deadlock argument rather than an oversight — a parent holds a slot at its own tier and waits only on the tier below, so the wait graph cannot contain a cycle — but a reader who takes the field name as tree-global will be wrong. Stated on the field, in `docs/CONTRACT.md`, in the composition guide and in the changelog.

- **The queue is FIFO and has no other policy.** No priority, no reordering, no way for an application to promote or cancel a waiting child, and no fairness rule between the parents feeding one tier. A queue with a policy is a scheduler and this is not one.

- **A queued child is invisible everywhere except `Store::queued_agents`.** That is the other side of "never started, never charged": it has no run id, so `Store::children` will not list it, `Store::agent_events` has nothing to say about it, and it emits no events of its own until it is admitted.

- **N2's control column was wrong as written and the substance was met another way.** The criterion named a control filtering on `goal` alone; `goal` is the index's last column and SQLite still uses the index for it, skip-scanning `ANY(parent_run_id) AND ANY(step) AND goal=?` — a full read wearing an index's name. The control is `queued_at`, which is in no index at all. The claim the criterion exists to make (a control that proves the check discriminates an index from a scan) holds; its example column did not.

- **`Containment::max_total_cost` is still inert**, for the reason it has always been inert: there is no price telemetry, so any figure the harness compared against would be one it invented.

- **The 0.30.0 N2 wording is still over-claimed and still not silently re-claimed.** Counting every row is what the aggregate answer is; the measured figures at 20k runs stand and materialised counters remain a different release.


### 0.31.0 known limitations

- **This record is sealed before the crates.io upload.** The authorized mode is `published` and the flow makes it true: `feat/0.31.0` into `develop`, `develop` into `main`, the GitHub Release cut by `release.yml`, then `cargo publish --locked` by hand, last. The record states the target; it does not claim the upload has already happened.

- **The gates ran before this record was written, and O1 and O2 cannot run locally at all.** The three-OS matrix including the MSRV 1.95 job, and the docs.rs nightly build, execute on the pushed commit. Nothing written after the gates is compiled, linted or packaged: `.ultraship` is in `Cargo.toml`'s `exclude` list, so the sealed evidence is still true of the shipped tree. Stated rather than implied, and the record is not amended afterwards — an immutable record that gets edited makes `immutable: true` a wish.

- **F9's boundary is 0.29.0 to 0.31.0, not 0.30.0 to 0.31.0.** The criterion named a 0.30.0 binary; the pinned generator already in the tree is 0.29.0, which lacks everything a 0.30.0 one lacks and more — 0.30.0's two `memory` columns and its `memory_recalls` table as well as this release's `plans` table. The claim executed is therefore strictly stronger than the one written, proven by the technique the criterion named, and it costs no second pinned crate to build and keep in sync. Recorded as a deviation rather than read as satisfied verbatim.

- **The plan gate binds the agent, not the caller.** The `plan-gate` policy layer holds for the run's own loop. The embedding program holding the `Store` and the workspace is not the run and is refused nothing — the same distinction 0.30.0 drew for a pinned memory entry, and stated in `docs/CONTRACT.md` beside it.

- **A read-shaped tool that quietly writes is outside every boundary this crate has.** The phase is an `Act::Write`/`Act::Exec` denial, and a registered `Tool` is checked as an exec — but the crate cannot see inside a caller's `invoke`. This is true in the phase and out of it; it is written down here because the phase's promise makes it easy to over-read.

- **Reasoning text is live-only.** `EventKind::Reasoning` is the only place it appears. It is not written to the trace, so a run whose `Observer` was not attached can see what the thinking *cost* through `Usage::reasoning_tokens` and never what it *said*. Persisting it would grow the trace by the most verbose thing a model produces, for a value nobody has asked for.

- **OpenAI returns no reasoning text.** `Effort` on `OpenAi` changes how the model behaves and leaves `CompletionResponse::reasoning` at `None`, because the Chat Completions API this crate speaks does not return it. Not a defect here, not worked around, and stated as a row in the contract's per-vendor table rather than discovered.

- **`Compatible`'s effort key is unverified.** `reasoning_effort` is passed through to whatever endpoint the caller pointed at. An endpoint that does not know it may ignore it or reject the request; the crate does not check, in keeping with everything else on that provider.

- **Anthropic's thinking budgets are three fixed numbers** — 1,024 / 4,096 / 16,384 — and `max_tokens` becomes `8192 + budget`. A caller who needs a specific budget rather than a tier cannot express one. That is the cost of a vendor-neutral knob, and it is the same trade `CompletionRequest::model` makes.

- **Two processes resuming one run after a single approval are not guarded.** `Store::decide_plan` refuses a *second verdict* with `Error::Resume`, so a race to approve is caught. A race to resume is not, and never has been for any resume in this crate; `Store::check_resumable` is the only lock there is.

- **Four public breaks.** `CompletionRequest` gains `effort`, `CompletionResponse` gains `reasoning`, `RunOutcome` gains two variants, and `EventKind` gains three (free, since it has been `#[non_exhaustive]` since 0.24.0). Each is in the changelog with a migration. A caller who followed the documented `..Default::default()` construction is unaffected; one who wrote an exhaustive struct literal or an exhaustive `match` on `RunOutcome` pays. This is the same cost 0.21.0 and 0.22.0 charged for the same two provider types.

- **The three open questions, answered by the implementation.** `Revise` *appends* rather than restarting the phase — the ledger keeps everything the agent already read, and re-reading the workspace to satisfy a tidier model would spend a run's budget on nothing. A second plan after an approval is *not* offered: the tool is withdrawn, because a second gate mid-run is a second way for an unattended run to stop. `Effort` is a **closed** enum: the three tiers are the vendors' shared vocabulary, and a fourth would be a vendor detail the projection absorbs rather than a tier a caller names.

- **Inherited from 0.30.0 and still open:** the N2 wording there ("the aggregates do not scale with the trace") over-claims, and is stated honestly in `docs/CONTRACT.md`. This release does not re-claim it and does not touch those accessors.


### 0.30.0 known limitations

- **This record is sealed before the crates.io upload.** The authorized mode is `published` and the flow makes it true: `feat/0.30.0` into `develop`, `develop` into `main`, the GitHub Release cut by `release.yml`, then `cargo publish --locked` by hand, last. The record states the target; it does not claim the upload has already happened.

- **The gates ran before the record was written, and O1 and O2 cannot run locally at all.** The three-OS matrix including the MSRV job, and the docs.rs nightly build, execute on the pushed commit. This is stated rather than implied, and the record is not amended afterwards — an immutable record that gets edited makes `immutable: true` a wish.

- **N2's wording over-claims and the measurement is what shows it.** "The aggregates do not scale with the trace" is not achievable for an aggregate over full history: counting every row is what the answer *is*. Measured on a debug build over an in-memory store, twenty times the runs cost roughly five times (runs_by_outcome) to twenty times (first_try) as long — at 20,000 finished runs, 1.8ms, 2.5ms, 7.6ms and 1.2ms. What is true, and is what the criterion's own second sentence asks for, is that each is one indexed query rather than a table scan, at roughly 90–380ns per run. Raised for the owner rather than read as satisfied; making it literally true needs materialised counters, which is a different release. `docs/CONTRACT.md` states the honest version.

- **Gate failures group by phase, not by criterion.** The trace records which phase of the gate failed — `compile`, `criterion-compile`, `test-run` — and not the text of the criterion. Counted per failure rather than per run; "how many runs failed this phase" is a different question and is not answered rather than answered ambiguously.

- **`Recovery` has no escalation count**, because nothing records an escalation as an event. When something does, it gets its own row rather than a place in the total.

- **A pin binds a run, not the caller holding the store.** `memory_delete` and `memory_clear` have no pin guard, so a pinned entry is not tamper-proof against the embedding program — only against the agent. Found while documenting rather than while building, and written into `docs/CONTRACT.md` rather than quietly left as an implied promise.

- **`first_try` counts a run whose gate never ran.** A contract with `Verification::None` that finishes successfully has no failed gate phase and is therefore a first-try pass. That is the honest reading of "succeeded with nothing failing", and it means the number is not a gate-pass-rate for a workspace that mixes verified and unverified runs.

- **`MemoryEntry` gained two public fields.** It is a row this crate returns and nothing takes, so this is additive for every reader — but a caller who constructs one in a struct literal has to name `kind` and `pinned`. Two of this crate's own tests did, which is exactly the cost, and it is in `docs/CONTRACT.md` beside the same warning for the two provider structs.

- **The backwards cross-version test is `#[ignore]` by default.** It needs the pinned 0.29.0 generator built, which resolves `io-harness =0.29.0` from crates.io. A local `cargo test` reports it as ignored rather than silently passing; the `cross-version-0.29.0` CI job is what runs it, and that job also fails if the generator's pin ever drifts off `=0.29.0`.

- **Provenance is a map built at load, not a per-key walk.** Decided against the merge code early: the known consumer renders a whole settings list, and a per-key walk would re-read the tables once per row. The cost is that every key a file set is held in memory for the life of the `Config`, which for a configuration file is nothing.


### 0.29.0 known limitations

- **`Compatible` is not a compatibility layer, and every vendor behind it diverges somewhere.** The crate sends one wire and will not grow per-vendor branches — that is the structural claim the single type rests on. The divergences are stated in `docs/CONTRACT.md` rather than absorbed: Zhipu's out-of-set `finish_reason` values, Groq's 400 on `messages[].name`, Mistral's nine-character tool-call ids and `schema_definition` field, DeepSeek's 400 when `reasoning_content` is not replayed on a request carrying tools, and Ollama silently ignoring `tool_choice`.

- **vLLM and SGLang emit no tool calls at all unless the server was started with a tool-call parser flag, and this is the failure mode that does not announce itself.** No status code, no warning and nothing in the trace distinguishes it from a model that chose to answer in prose. vLLM needs `--enable-auto-tool-choice` with a `--tool-call-parser`; SGLang needs its equivalent. Both are server launch arguments this crate cannot supply, cannot detect, and will not guess at.

- **Nearly every vendor's `/models` returns identifiers and no prices.** That is why `ModelInfo::price` is an `Option` and why the reference catalogue exists at all. Perplexity serves no `/models` endpoint whatsoever, so `Compatible::perplexity(..).models()` returns the vendor's 404 as a provider error rather than an empty list — reporting an empty catalogue there would say "this vendor serves no models", which is a different and false claim.

- **A reference price is the aggregator's price, not the vendor's.** It tracks the vendor's rate and is not identical to it. `PriceSource::Reference(host)` is how an operator tells; a figure derived from it is close rather than authoritative, and the crate does not pretend otherwise.

- **A model the reference does not carry stays unpriced.** Matching is an exact slug or one documented normalisation — drop a single leading `vendor/` segment, case-insensitively — and nothing else. An ambiguous normalisation resolves to nothing rather than to either candidate. `Spend::unpriced_calls` is where the gap surfaces, and it is a floor rather than a total.

- **The catalogue is cached on the provider instance, not in the store, and this departs from the roadmap entry deliberately.** That entry said "cached in the store with the instant it was fetched". `Provider::models()` takes `&self` and nothing else, so reaching a `Store` from it would have meant putting the store into the provider trait — the one type this crate has kept out of it. The fetch instant is not lost by the choice: it lands on `PriceTable::as_of`, which already exists to carry exactly that claim. A second process re-reads one free keyless request.

- **`Compatible` has no `from_env()`, unlike the three original providers.** Twenty-one vendor environment variable names would be twenty-one guesses made on the operator's behalf. `[[provider]]` with `api_key = "${env:GROQ_API_KEY}"` names the variable in the operator's own file and has resolved credentials from the environment since 0.19.0. The guide says so in one line, because the absence will otherwise read as an oversight.

- **`Fallback::models()` returns an empty list.** Which of two vendors' catalogues a chain should report is a question with no right answer, and inventing one would freeze it. A caller who wants a chain's catalogue asks each provider.

- **A preset's base URL is a default this crate ships, and a vendor that moves one makes it wrong until the next release.** Every one was verified against the live host at release time, but no test can dial them and none does. The mitigation is that `base_url` overrides any preset in one line of configuration, so a moved endpoint is an operator's edit rather than a release.

- **A preset's local-runtime port is that project's documented default and nothing more.** A runtime started on another port is `Compatible::new` with the URL.

- **`Auth` carries no Azure variant.** Azure OpenAI needs an `api-key` header, a deployment name in the path and an `api-version` query parameter. It is excluded on demand rather than on cost, and `Auth` is `#[non_exhaustive]` so it can arrive later without a break. Bedrock's SigV4 and Vertex's service-account JWT are likewise unreachable, though both are usable today by an application that mints a bearer token itself and passes it as the key.

- **Price tiers are keyed on prompt length only.** `min_prompt_tokens` is the single selector the observed catalogue uses, and the highest floor a prompt reaches prices the whole request. A vendor pricing on some other axis — output length, time of day, committed throughput — is not representable, and would be a new selector rather than a new value.

- **`${cmd:}` still has no timeout**, and `rusqlite::Error` is still in the public API. Both are standing limitations carried deliberately from earlier releases rather than fixed by proximity.

- The record is sealed with `mode: published` before the crates.io upload, which follows the `develop` → `main` merge and the GitHub Release. The record states the authorised target; the flow makes it true.


### 0.28.0 known limitations

- **A hook blocks the run loop while it runs.** `Observer::event` is synchronous and returns a `Flow` the loop acts on immediately, so the step that emitted the event waits for every matching hook. That is the property that makes `on_failure = "cancel"` mean anything — a hook that answered after the run had moved on could refuse nothing — and it means pointing a `run` hook at `token`, which is emitted per streamed token, is a decision to spawn a process that often. The guide says so beside the feature rather than in a footnote.

- **Hooks do not accumulate across scopes.** Unlike `[[policy.layers]]` and `[[agent]]`, the array is deliberately not in `APPENDING`, so a later scope replaces it whole: one `[[hook]]` in `io.local.toml` silently discards every hook the user-scope file declared. A half-appended set of hooks would be a set nobody wrote, and `Config::sources()` says which file won.

- **An `append` hook is serialized within this process and nowhere else.** It opens its file per event rather than holding a handle, and a lock keeps a sub-agent tree from interleaving halves of two events. A second *process* appending to the same log is not coordinated by anything, and there is no rotation and no size cap.

- **A hook's output is discarded.** `stdout` and `stderr` go to `null`, because a library must not write to its caller's terminal, so a hook talks by exiting non-zero. A failure is traced at warn level with the hook's index and the event tag, and never with the event itself — a `started` carries the goal.

- **A hook grants nothing.** It is not a permission mechanism: it cannot approve an action, deny one, alter an argument or answer an `approval_requested`. The only thing it can do to a run is end it, and only when the operator wrote `on_failure = "cancel"`. The boundary is still the `Policy` the caller loaded.

- **Refusing `[[hook]]` in the project scope does not make a cloned repository safe.** `[[mcp]]` still names a command, `[toolchain]` still names an argv, and a `[[policy.layers]]` entry can still allow what the defaults did not. One specific hazard is narrowed, and the contract, the guide and the changelog each say which array and no more.

- **One restore point per file per run.** `rewind` is all-or-nothing back to the run's start: there is no per-edit undo, no rewind to a chosen step, and no redo. That is what bounds the storage to the number of files a run touched rather than the number of edits it made, and it is the only definition under which "undo what this run did" is one operation rather than five.

- **A previous file over 1 MiB or one that is not valid UTF-8 cannot be put back.** It is recorded `Unkept`, `rewind` answers `NotKept` with the reason, and the file is left exactly as the run left it — reported rather than guessed at, and never truncated. An existing path that could not be read for any other reason is `Unkept` too rather than `Absent`, because folding a permission error into "the file did not exist" would make a rewind *delete* something whose contents were never read.

- **Only `write_file` and `edit_file` take a snapshot.** A file changed by `shell`, by `exec`, or by a git built-in has no restore point and answers `NotRecorded`. So does one whose bookkeeping row could not be written, which is warned about and swallowed exactly as an edit row is — so `NotRecorded` means "no restore point", not "the run did not write it".

- **`rewind` takes one path per call and is not transactional.** Undoing a whole run is a loop the caller writes, and it can be interrupted halfway.

- **`${cmd:}` still has no timeout.** 0.27.0's standing limitation, carried deliberately rather than fixed by proximity because this release wrote a bounded wait ten lines away. `Config::discover` is the caller's own call before any run exists; a hook fires inside a run loop. They are not the same hazard and one bound does not follow from the other.

- The record is sealed with `mode: published` before the crates.io upload, which follows the `develop` → `main` merge and the GitHub Release. The record states the authorised target; the flow makes it true.


### 0.27.0 known limitations

- **`${cmd:}` has no timeout.** A credential helper that hangs hangs the caller's own `Config::discover`, before any run exists, with the caller's own privileges. Deliberate rather than overlooked — the failure is visible at a call the caller made, and a timeout is a key to be argued on its own terms when a consumer meets it — but it is a real difference from every other substitution, all of which complete or fail promptly.

- **The trust rule is four keys and one substitution, and nothing wider.** A project-scoped `io.toml` can still name a command through `[[mcp]]`, still name an argv through `[toolchain]`, and still add a `[[policy.layers]]` entry that allows what the defaults did not. Cloning an unfamiliar repository is not made safe by this release; one specific hazard is narrowed. The contract, the guide and the changelog each say so in the same words rather than leaving the reader to infer the limit from the feature's name.

- **A named `[instructions]` file that does not exist is skipped silently.** A typo in a filename is not reported, because discovery over a file that is present in some repositories and absent in others is the normal case rather than the failure. It is the one place this module's "a substitution resolves or fails" rule deliberately does not apply, and the asymmetry is stated in the guide's limits block.

- **A discovered `AGENTS.md` is untrusted text that reaches the model verbatim.** It grants nothing — the boundary is still the `Policy` the caller loaded — but a repository's instructions file is written by whoever wrote the repository, and a caller pointing this at a clone should read it the way they would read any other text a stranger put in their prompt.

- **`[app]` is a hole in the strictness guarantee, by design.** An unknown key under it is never an error, so a typo in an application's own settings is the application's problem. It is the second and last such exception beside `[[mcp]]`, and the two are listed together everywhere they appear, because one stated and one hidden is a rule nobody can trust.

- **`IO_CONFIG` names the user scope, so a project file still beats it.** A caller who wants a specific file to win outright cannot express that: they must pass its directory as the discovery root, or hold the configuration themselves. Chosen over a fifth `Scope` variant, which would be a break to a public enum that is not `#[non_exhaustive]`, and over a bypass, which would make "four scopes, later wins" false.

- The record is sealed with `mode: published` before the crates.io upload, which follows the `develop` → `main` merge and the GitHub Release. The record states the authorised target; the flow makes it true.


### 0.26.0 known limitations

- **AppContainer is not what `Sandbox::select` chooses on Windows.** The mechanism is built, tested and reachable, but a Windows run still gets the Job Object, so a sandboxed payload there is resource-contained and not access-confined. The platform table in `README.md`, `docs/CONTRACT.md` and the crate rustdoc is unchanged, and says so. See `US-IO-HARNESS-0.26.0-I02`.

- **The grant set is the unclosed half.** An AppContainer is default-deny for reads, and a binary runs inside one once its directory is granted — but deriving that set for `cargo`, `npm`, `go`, `pytest` and `dotnet`, including the shims that re-exec into another tree, is a design question about what this crate should know. That is what stands between the mechanism and a default.

- The AppContainer spawn collects stdout and stderr into **one** temporary file rather than two pipes, so the two streams interleave as a terminal would show them and cannot be told apart afterwards. Deliberate — a pipe nobody drains fills and blocks the payload — but it is a real difference from the other backends and not an accident.

- A handle's Job Object carries the teardown guarantee and no resource limits. A handle is a lifetime rather than a boundary, and a twenty-minute build is exactly the workload the sandbox's caps exist to kill, so `SandboxLimits` does not reach a handle's processes on any platform.

- The record is sealed with `mode: published` before the crates.io upload, which follows the `develop` → `main` merge and the GitHub Release. The record states the authorised target; the flow makes it true.


### 0.25.0 known limitations

- **Killing a handle on Windows does not reach a grandchild whose parent has already exited.** The unix guarantee is a process group; Windows has no equivalent and its Job Object needs the child handle at the spawn site, where this release's interface carries a pid. Deferred to 0.26.0 by `US-IO-HARNESS-0.25.0-I01`, which rewrites that spawn path for AppContainer regardless. Stated in `docs/CONTRACT.md` and the sandbox guide rather than left to be discovered.

- **Output skipped by a poll is gone, not archived.** `Store::handle_output` holds only what polls actually read. A poll arriving to find more than one window waiting keeps the newest and advances the cursor past the rest, so bytes no poll ever read reach no store and go with the capture file when the run ends. The poll's own message to the model says so; an earlier wording claimed the skipped bytes were in the trace and was corrected before release. The real fix streams the skipped region to the store in bounded chunks.

- The record is sealed with `mode: published` before the crates.io upload, which follows the `develop` → `main` merge and the GitHub Release. The record states the authorised target; the flow makes it true.

- A handle has no wall-clock ceiling of its own. That is the point of one, but it means a model that starts a handle and never polls or kills it holds a process for the life of the run. The per-run cap of 8 and the kill-on-run-end are what bound that, not a timeout.

- The diagnostics pass runs on every edit rather than being debounced across a burst, so a model rewriting four files in one step pays four checks. Left as written and recorded as an open question in this contract rather than guessed at.

- `every_kind()` in `src/observe.rs` claims an exhaustiveness it does not prove — the guard matches on the vector's items, so it proves the arms are exhaustive and never that the vector mentions every variant. `TodoWrote`, `QuestionAsked` and `QuestionAnswered` have gone round-trip-untested since 0.21.0. Pre-existing, found while adding this release's variants, and left alone under scope freeze.


### 0.24.0 known limitations

- This record is sealed before the crates.io upload, which follows the develop-to-main merge and the GitHub Release. `mode: published` states the authorized target; the delivery sequence makes it true.

- Process handles — `shell_start`, `shell_poll`, `shell_kill` — are deferred to 0.25.0 by `US-IO-HARNESS-0.24.0-I01`, with acceptance criteria F5, F7 and the handle half of F6. Long-running work is therefore still unrepresentable: a dev server or a twenty-minute build blocks one step of the loop until the exec timeout decides for it. The Job Object they need shipped here, so they arrive in 0.25.0 on a platform that can already contain them.

- `2>&1` on a stage whose stdout is piped is refused rather than supported. Merging the two streams into a pipe needs a descriptor duplication this crate does not perform; on a final stage the merge happens in the captured output. `cmd 2>&1 | grep x` is therefore not expressible.

- Glob metacharacters are refused outside quotes rather than expanded. This is deliberate — expanding would let the argv the policy checked differ from the argv that ran — but it is a real usability cost: `ls *.rs` does not work, and the model has to use `find` or `list_dir` and then name paths.

- `cd` is applied when the line is planned rather than when it runs, so in `cd nope && ls > out.txt` the redirect resolves under `nope/` where a real shell would have failed the `cd` and written in the original directory. Both paths are checked and neither escapes the root. It is the safe direction, and it is a difference from a shell rather than a match.

- The Windows job's CPU limit counts user-mode time only, where unix `RLIMIT_CPU` counts kernel time as well, so the cap is genuinely weaker there for a kernel-heavy workload. And `JOB_OBJECT_LIMIT_PROCESS_MEMORY` makes an allocation *fail* rather than terminating the process, so a payload over the cap typically dies of its own failed allocation. The memory detection is a 90%-of-cap heuristic, because the job reports the peak a process reached and never the allocation it was refused; the exact mechanism would need a job completion port and a parked thread per run.

- Windows has resource containment and no access containment. There is no filesystem boundary and no network boundary, because a Job Object contains neither. AppContainer is 0.25.0's.

- `CompletionRequest` and `CompletionResponse` are deliberately NOT `#[non_exhaustive]`, so adding a field to either remains a break. Recorded in `docs/CONTRACT.md` rather than left to be discovered. The attribute forbids struct-literal construction including `..Default::default()` from outside the crate, and `Provider::complete` must return a `CompletionResponse` — measured at 11 `E0639` errors across 10 files in `tests/`.

- The `exec` tool's gate still checks `argv.join(" ")` as a second target, which cannot distinguish one argument containing a space from two arguments. Left deliberately untouched: changing an existing tool's permission decisions does not belong in the same diff as a new tool. `shell` does not have this looseness, because it checks each stage's argv separately.

- Live evidence is OpenRouter-only. The `.env` here carries real credentials for no other vendor, so the Anthropic and OpenAI paths rest on their wire tests — unchanged by this release, which touched no provider code.

- Carried forward unchanged, none of them touched by this release: `resume_tree` never calls `record_run_policy`; there is no seccomp filter on Linux; `git_status` lacks `-z`; `pdf::read_text`'s `catch_unwind` is a no-op under `panic = "abort"`; `TEST_BINARY` is inert; `Containment::max_total_cost` is inert; there is no `Session::resume_turn`; `Session::history` is linear per hop; the stall window still does not survive a resume; and `rusqlite::Error` remains in the public API.


### 0.23.0 known limitations

- This record is sealed before the crates.io upload, which follows the develop-to-main merge and the GitHub Release. `mode: published` states the authorized target; the delivery sequence makes it true.

- The MSRV rose from 1.88 to 1.95, which is the one place this release makes a consumer's life harder — in a release whose whole purpose is to make it easier. It is not avoidable: every `rusqlite` at or above the 0.40 floor calls the std `cfg_select!` macro, and below that floor the `links` collision returns. A consumer pinned to an older toolchain should stay on 0.22.0.

- F3's fixture does not exercise the adopt-a-completed-child path. 0.22.0 committed the step-capped root's step, so on resume the coordinator replans rather than replaying the fan-out and `Store::find_spawn` has nothing to adopt at that step. The no-re-run assertions are stronger for it — the finished child is proven untouched by its own rows and its own file rather than by trusting adoption to skip it — but it is a different proof. A fixture whose root paused mid-fan-out would cover it; `tests/checkpoint.rs` covers adoption within a single release.

- NF4's premise was wrong and is recorded rather than quietly satisfied: the criterion speaks of "the existing benchmark path" and this repository had none — no `benches/`, no criterion, no `[[bench]]`. Both sides were built for the measurement. The host was also under real load throughout (load average 9.23 on 8 cores), which is why the spread is wide and why 33 samples were taken instead of 5. A rerun on an idle machine would tighten the bound considerably.

- Nothing runs the throughput comparison automatically. On shared CI runners the noise floor is worse than the one measured here, and a throughput gate against a 20% noise floor would be a flake generator rather than a guard. The two binaries and the protocol are in the tree for a manual rerun.

- `docs/public-api.txt` is unchanged, and that is correct rather than an oversight: the snapshot enumerates re-exported names as `<kind> <name> <file>`, so a variant's payload type is not a line in it and never will be. The fact went to `docs/CONTRACT.md`'s "What is public", which now states outright that `rusqlite` is a public dependency of this crate.

- Live evidence is OpenRouter-only. The `.env` here carries real credentials for no other vendor, so the Anthropic and OpenAI paths rest on their wire tests — unchanged by this release, which touched no provider code.

- `rusqlite::Error` remains in the public API, so the next `rusqlite` major bump will break consumers again. The intent to wrap it is stated in `docs/CONTRACT.md` but deliberately not done here: a migration release must be reviewable for exactly one property, and an error-type redesign in the same diff destroys that. Not yet slotted to a version.

- Carried forward unchanged, none of them touched by this release: `resume_tree` never calls `record_run_policy`; `SandboxLimits::max_processes` is enforced by nothing; there is no seccomp filter on Linux; the Windows Job Object is unimplemented; `git_status` lacks `-z`; `pdf::read_text`'s `catch_unwind` is a no-op under `panic = "abort"`; `TEST_BINARY` is inert; `Containment::max_total_cost` is inert; there is no `Session::resume_turn`; `Session::history` is linear per hop; the stall window still does not survive a resume.

- `skills::discovery_over_a_32_skill_directory_is_under_50ms` remains a wall-clock assertion that fails intermittently on the Windows runner under load. It passed on this release's CI run.


### 0.22.0 known limitations

- `Usage::server_tool_requests` is the vendor's counter, and only Anthropic reports one. The OpenAI wire carries no such member today, so a run that demonstrably searched on OpenAI or OpenRouter records a zero. The crate records the zero it was told rather than deriving a count it was not given, and the `server_tool_calls` row is the vendor-independent record that the call happened.

- The live evidence was obtained on OpenRouter, the only vendor with working credentials in this workspace — `.env` holds placeholders for `ANTHROPIC_API_KEY` and `OPENAI_API_KEY`. So the live run proves the OpenRouter body, plugin and annotation parse end to end; the Anthropic path, including the dated server-tool types, the beta header and the `web_search_tool_result_error` parse, is proven by its wire tests and not by a live call. A first Anthropic run is the first thing to do when a key is available.

- A paused turn resumes as a fresh request. The crate sends one flattened user turn and does not echo the vendor's partial assistant blocks back, so a provider may repeat — and re-charge for — a search it already ran. `WebAccess::max_uses` is the only lever against it.

- The domain filter is enforced by the vendor, not by this process. The provider dials the URL, so `Act::Net` never sees it, no approver is consulted, and a caller who needs the boundary enforced in-process must leave this off. Stated in `docs/CONTRACT.md`, in the guide, and on the type.

- A citation is what the provider returned. This crate does not fetch the url, check that the page exists, or verify that it says what the model claimed.

- No per-child narrowing of web access. A spawned child inherits the root's declaration exactly; `AgentDef` has no field for it, so "the researcher may search, the writer may not" is not expressible in one tree today. Candidate for 0.23.0.

- `max_uses` means three different things across the three vendors: uses on Anthropic, results on OpenRouter (`max_results`), and nothing at all on OpenAI, whose Chat Completions body carries no cap field. Documented in the guide rather than normalised, because normalising it would mean inventing a bound the vendor does not enforce.

- This record was sealed on `feat/0.22.0` before the crates.io upload, which follows the `develop` → `main` merge and the GitHub Release. `mode: published` states the authorized target and the flow makes it true.


### 0.21.0 known limitations

- This record is sealed on `feat/0.21.0` before the crates.io upload, which is the standing flow: the record states the authorized target and the flow makes it true. The upload follows the develop -> main merge and the GitHub Release, and is the last step.

- **NF4 — the three-OS suite is proven by CI on the pull request, not from this machine.** Everything above was run on macOS. The question tests are store- and process-boundary-bound and the template tests read a directory, which are the two places the Windows leg historically disagrees. `skills::discovery_over_a_32_skill_directory_is_under_50ms` is a wall-clock assertion that fails intermittently on the Windows runner under load and passes on re-run.

- **This record was re-sealed once, before publication, because CI found a defect after it was first sealed.** The Windows leg of PR #68 failed on `tests/template.rs::render_substitutes_placeholders_and_routes_the_remainder_to_arguments` and nowhere else: the GitHub Windows runner checks out with `core.autocrlf=true`, so every LF in `tests/fixtures/**` became CRLF, and that test compares a rendered template body byte for byte — which compares the fixture's bytes too. It is not the known flaky timing test and it was not a flake; it failed identically on both runs of the same commit. The parser is not at fault and was not touched: `src/skills.rs` returns a frontmatter body verbatim on purpose, pinned by its own `crlf_line_endings_parse` unit test, so a Windows user with a CRLF template legitimately gets CRLF in the rendered prompt. The fix is a `.gitattributes` marking `tests/fixtures/** text eol=lf`, which makes a fixture's bytes the same on every platform for CI and for anyone cloning on Windows, and covers the older `readme-0.15.0-headings.md` fixture that had the same latent exposure (commit 19806ae, with a CHANGELOG entry under Fixed). Every gate above was re-checked against the fixed tree and the three-OS matrix is green on it. The evidence figures are unchanged, because nothing in the crate changed. Recorded here rather than left out: the first seal predated a real defect, and a reader of this ledger is entitled to know that.

- **The live run's two agent models are not the two the example documents.** Its defaults are anthropic/claude-haiku-4.5 and anthropic/claude-sonnet-4, and both were refused with HTTP 402 — the OpenRouter balance could afford 59,266 completion tokens and those models' 64,000-token ceiling costs more than that. The first attempt is kept as evidence/0.21.0/live-run-agency-attempt1-402.txt, where claims 1 and 2 had already passed. The recorded run used openai/gpt-4o-mini and openai/gpt-4o instead, which proves the same thing — two definitions, two model strings, read back from `provider_calls` — on a smaller balance. What was not exercised live is a per-agent model from a *different vendor* than the root's.

- **The live run's `searcher` never attempted a write, so its `deny_write` was not exercised in anger.** It obeyed "do not modify any files" on its own, which is the model behaving well rather than the boundary being tested. That a definition's deny actually refuses is proven offline by tests/agents.rs against a provider scripted to try the write, with the child layer named in the attribution.

- **A plan is the agent's stated intent, never a commitment.** Nothing checks that the agent did what its plan said, no outcome depends on a todo state, and a run whose plan is unfinished still reports whatever its verification and budgets dictate. In the live run the agent left item 2 `active` and item 3 `pending` at the step cap while having already made the edit — an honest illustration of the limitation. A plan the harness policed would be a second verification layer competing with the one the crate has; the answer here is documentation, and it is the honest one.

- **A `Responder` is the only answering channel, and there is no UI for it.** `StdinResponder` and `FixedResponder` ship; a TUI, an HTTP endpoint and a JSON event envelope do not, deliberately — io-cli and io-studio own their front ends. An answer is also an untyped `String`, optionally one of `choices`; nothing validates it against them.

- **A question can only be asked of the operator.** No provider-side clarification round-trip, no question routed to another agent, and a parent cannot answer for its child — a child's question pauses the whole tree and reaches the human, which is the only answer path this release has.

- **A definition names a model string, not a provider.** One run has one provider and one key; two definitions naming models from different vendors is a `provider::Fallback` question. The recorded `CompletionResponse::model` keeps saying what actually served each call, so a vendor silently substituting a model reads honestly in the trace rather than being hidden — but a definition asking for a model the account cannot reach fails the call, as the 402 above shows.

- **There is still no run-level or turn-level model override.** `CompletionRequest::model` exists because named agents need it; exposing a model swap on `TaskContract` or on a session turn stays shut, as 0.20.0 left it.

- **`AgentDef` narrows by `deny_write`/`deny_net` only.** The contract's second open question is resolved in the narrow direction: there is no per-agent tool allow-list, because it would be the one narrowing not proven by `Policy::contain`.

- **A template renders text and can set nothing** — no policy, no budget, no toolbox, no model, no verification criterion — and its language is placeholder substitution plus `$ARGUMENTS`, nothing more. Discovery stays caller-side (the contract's third open question): a run cannot name a template by id in its trace, because a `TaskContract` field would have been a wider surface and a break.

- **The stall window still does not survive a resume.** `Progress` is in-memory, so a resumed run starts its repetition counter and its unproductive window at zero, exactly as on 0.20.0. Making it durable is a checkpoint change and this release makes none. Repetition also resets with the window on a replan (the contract's fourth open question, resolved consistently with the existing "a replanned agent gets a clean window" rule), so an agent that resumes the same loop immediately after being told to stop needs the second window to catch it again.

- Carried unchanged from 0.20.0 and untouched by this release, as `scope.excluded` requires: `resume_tree` still does not call `record_run_policy`; `SandboxLimits::max_processes` is enforced by nothing on any platform; the Linux backend installs no seccomp filter; `Containment::max_total_cost` is inert; `server_tool_requests` is zero everywhere until 0.22.0; the crate ships no prices; the `[toolchain]` config override does not reach this crate's own run loop; an unknown key inside a `[[mcp]]` table is not rejected; `git_status` lacks `-z`; `fill_form` has never been opened in a real viewer; the Windows Job Object is unimplemented; `pdf::read_text`'s `catch_unwind` is a no-op under `panic = "abort"`; `TEST_BINARY` is inert; there is no `Session::resume_turn`, no `turn_bounded_steered`, and `Session::history` is linear per hop.


### 0.20.0 known limitations

- This record is sealed on `feat/0.20.0` before the crates.io upload, which is the standing flow: the record states the authorized target and the flow makes it true. The upload follows the develop -> main merge and the GitHub Release, and is the last step.

- **NF4 — the three-OS suite is proven by CI on the PR, not from this machine.** Everything above was run on macOS. The session tests are store- and channel-bound, which is where the Windows leg historically disagrees.

- **The live steer was delivered, not obeyed.** Turn 2 of the live session carried the operator's message into the very next request and recorded it, and then the model looped on `find` and `read_file` until the stall detector stopped it, writing neither file. What this crate guarantees is delivery at the next step boundary; whether a model changes course on being told to is the model's business, so the example asserts the delivery and prints the rest. That a steer *changes what the agent does next* is proven offline by F8 against a provider scripted to branch on it.

- **A turn resumed directly does not move the session's head.** A turn is a run, so `resume_with(contract, provider, store, run_id, policy, approver)` continues one — but the session bookkeeping (`finish_turn`, the head) happens in `Session::turn*`, so a turn resumed outside the session keeps `outcome: null` in the tree and the head stays where it was. There is no `Session::resume_turn`. This resolves the contract's third open question in the narrow direction: what is promised is resumability by run id, not a session-level resume.

- **No `turn_bounded_steered`.** Steering is available on the unbounded turn only. A caller who wants both a verification gate and a mid-turn steer has to reach for `turn_steered` and enforce the criterion themselves. Four turn methods already cover the cases asked for; a fifth combination waits for someone to want it.

- **`EventKind::Token` carries no delta index.** This resolves the contract's second open question: the `RunEvent` already carries `run_id`, `step` and `depth`, and an index would be another field to keep true. A consumer therefore cannot detect a dropped event from the event itself — what holds is that the crate's own path does not drop one, asserted by concatenation equality in F5.

- **A session turn cannot spawn sub-agents.** This resolves the contract's first open question: there is no `turn_tree`, because a conversation whose turn is a tree raises the question of which agent the operator is steering — which is 0.21.0's subject.

- **A streamed delta is provisional and a `Fallback` mid-stream shows it.** If the primary provider streams and then fails, its deltas were already emitted and the secondary's answer arrives whole, so an operator sees partial text followed by different complete text. Stated in CONTRACT.md and in the guide rather than smoothed over.

- **An interrupt is indistinguishable from an observer's cancel in the trace.** Both record `cancelled`, deliberately — it is the same path, which is what makes an interrupt resumable and whole-stepped. A steered *message* does leave its own `steered` row; the interrupt does not.

- **A session has no aggregate budget, and one session takes one driver.** Every turn is a fresh run with its own ceilings, so `max_steps` on one turn does not bound the next and a conversation-wide limit is the caller's to enforce per turn. Two processes taking turns on the same session id concurrently is unsupported and undefended beyond SQLite's busy timeout.

- **`Session::history` is linear per hop over the session's turns**, so walking the path is quadratic in the number of turns in the tree. Bounded in practice by how long a conversation gets and by the one query it costs; a recursive CTE is the upgrade if a session ever holds thousands.

- **A refused git built-in ends the run** — found while running the live session, recorded in docs/CONTRACT.md, and NOT fixed here because this release touches no tool. A policy denying `Act::Exec` for `git` turns a speculative `git status` into `Error::Refused` out of the run loop instead of an observation the model adapts to. The live example allows `git*` for that reason. It belongs to the next release that touches the tool layer.

- Carried unchanged from 0.19.0 and untouched by this release, as `scope.excluded` requires: `resume_tree` still does not call `record_run_policy`; `SandboxLimits::max_processes` is enforced by nothing on any platform; the Linux backend installs no seccomp filter; `Containment::max_total_cost` is inert; `server_tool_requests` is zero everywhere until 0.22.0; the crate ships no prices; the `[toolchain]` config override does not reach this crate's own run loop; an unknown key inside a `[[mcp]]` table is not rejected; `git_status` lacks `-z`; `fill_form` has never been opened in a real viewer; the Windows Job Object is unimplemented; `pdf::read_text`'s `catch_unwind` is a no-op under `panic = "abort"`; and `TEST_BINARY` is inert.


### 0.19.0 known limitations

- This record is sealed on `feat/0.19.0` before the crates.io upload, which is the standing flow: the record states the authorized target and the flow makes it true. The upload follows the develop -> main merge and the GitHub Release, and is the last step.

- **NF4 — the three-OS suite is proven by CI on the PR, not from this machine.** Everything above was run on macOS. The Windows leg matters more than usual this release: `config::user_path` resolves `%APPDATA%\io\io.toml` there and `$XDG_CONFIG_HOME`/`$HOME` everywhere else, and only CI exercises the Windows branch. The open question about that path is resolved in favour of `%APPDATA%`.

- **The `[toolchain]` override does not reach this crate's own run loop.** `run` detects the ecosystem for itself; `Config::toolchain(detected)` gives the embedding application the merged value. Wiring it into the loop needs a new `TaskContract` field, which is a break, and this release carries none — so an operator who overrides `test` in `io.toml` changes what io-cli or io-studio would show, not what the harness's own prompt says. Stated in the guide and in CONTRACT.md.

- **An unknown key inside a `[[mcp]]` table is not rejected.** `McpServer` is `#[serde(flatten)]`-based and serde refuses `flatten` beside `deny_unknown_fields`. Every other section rejects what it does not know; that one accepts and ignores it.

- **`${` always begins a substitution and there is no escape**, so a literal `${` in a value — in a glob pattern, say — is not expressible. An unknown prefix is an error rather than a passthrough, which is the trade: a typo cannot reach a policy rule as text.

- **`${file:...}` is not restricted to the config file's own directory.** This resolves the contract's open question in the permissive direction: it resolves any path relative to that directory, `../` included. Restricting it would bound what a collaborator's committed project file can read from a teammate's disk, which is a real difference — but a project file is already code review's problem, and a restriction that stops `${file:~/.secrets/token}` would break the ordinary case it exists for. Stated in the guide.

- **A config file is not a security boundary against the agent, and the guarantee is narrower than it reads.** What holds is that the file is read once, by the caller, before the run, so a config the agent writes mid-run cannot widen the boundary of the run in progress. What does not hold is anything about the *next* load: an agent that can write the workspace root can leave an `io.toml` there, and the caller's next `Config::discover` will read it.

- **`Config` derives `Debug`, and a substituted secret is in it.** The crate never logs or traces a config value — that is now tested — but a caller who debug-prints a `Config` prints whatever `${env:...}` resolved to. Redacting was not done: a config a caller cannot inspect is a config nobody can debug.

- **The live run recorded no refusal.** The model declined to attempt the denied write, so the boundary's live proof is that the secret is untouched and that the policy read from the file answers `Deny` — refusal-under-a-config-derived-policy through the real loop is proven offline by F3, against a scripted provider that does attempt it.

- **The default dependency tree grows for the first time since 0.1.0.** 401 to 413 lines. Stated in the changelog rather than discovered by a consumer, but a consumer who audits dependencies has five more crates to audit.

- Carried unchanged from 0.18.0 and untouched by this release, as `scope.excluded` requires: `resume_tree` still does not call `record_run_policy`; `SandboxLimits::max_processes` is enforced by nothing on any platform; the Linux backend installs no seccomp filter; `Containment::max_total_cost` is inert; `server_tool_requests` is zero everywhere until 0.22.0; the crate ships no prices; `git_status` lacks `-z`; `fill_form` has never been opened in a real viewer; the Windows Job Object is unimplemented; `pdf::read_text`'s `catch_unwind` is a no-op under `panic = "abort"`; and `TEST_BINARY` is inert.


### 0.18.0 known limitations

- This record is sealed on `feat/0.18.0` before the crates.io upload, which is the standing flow: the record states the authorized target and the flow makes it true. The upload follows the develop -> main merge and the GitHub Release, and is the last step.

- **NF4 — the three-OS suite is proven by CI on the PR, not from this machine.** Everything above was run on macOS. The Linux and Windows legs run on the feat/0.18.0 PR, and two of this release's rewritten gate tests now shell out to `cargo test --offline` inside the sandbox, which is new behaviour on those two platforms rather than a rerun of something 0.17.0 already proved there.

- **The crate ships no prices, so cost is zero out of the box.** This resolved the contract's open question in the direction it listed second. A crate cannot keep a vendor's price list accurate on its own release schedule, and a stale shipped number is a confident wrong answer; an operator must enter their own, and `PriceTable::new` requires the as-of date so the table cannot silently rot. The cost of the decision is that the feature does nothing until someone types a price in.

- **A token count is the provider's report, not a measurement.** If a vendor's own figure is wrong, the trace is wrong in the same direction and this crate cannot tell. A latency, by contrast, is the harness's own wall clock and *includes* this crate's request building and stream consumption, so it sits slightly above what a vendor would call its own latency.

- **`server_tool_requests` is zero everywhere.** It is a meter for a capability the crate does not have until 0.22.0, proven against fixtures and against nothing live. It ships now so that adding provider-executed search later is not a second widening of `Usage`.

- **Cache-write tokens are never populated on the OpenAI wire, and reasoning tokens never on Anthropic.** Neither vendor reports the figure in question, and inferring it would invent money the invoice does not contain. Both were verified live: the OpenRouter runs recorded real `reasoning_tokens` and real `cache_read_tokens`, and zero cache-write throughout.

- **Anthropic's cache path is proven against fixtures only.** The live evidence is OpenRouter, the one provider with credentials on this machine; `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` are empty in `.env`. The Anthropic reconciliation — including that its prompt count now *includes* the cached tokens it reports separately — rests on its accumulator tests.

- **A run older than 0.18.0 has no rows and nothing is backfilled.** The queries return empty rather than zero, which is the honest answer, but it means a store with history predating this release answers the cost question only for runs made after it.

- **Lines added and removed are a size, not a patch.** `Edit::measure` trims the common head and tail; it is not a minimal diff, so a change that moves a block reports the whole block on both sides.

- **Nothing spawns `TEST_BINARY` any more, and the constant remains.** Removing it would have been a break the contract did not plan, so a `deny_exec(TEST_BINARY)` rule now reads as a restriction and enforces nothing. Stated in CONTRACT.md and the verification guide, and asserted by a test.

- Carried unchanged from 0.17.0 and untouched by this release, as `scope.excluded` requires: `resume_tree` still does not call `record_run_policy`, so a tree resumed under a widened policy leaves an audit that understates what was permitted; `SandboxLimits::max_processes` is enforced by nothing on any platform; the Linux backend installs no seccomp filter; `git_status` lacks `-z`; `fill_form` has never been opened in a real viewer; the Windows Job Object is unimplemented; and `pdf::read_text`'s `catch_unwind` is a no-op under `panic = "abort"`.


### 0.17.0 known limitations

- This record is sealed on `feat/0.17.0` before the crates.io upload, which is the standing flow: the record states the authorized target and the flow makes it true. The upload follows the develop -> main merge and the GitHub Release, and is the last step.

- **A command runs with the embedding program's privileges, outside the sandbox.** The widest capability this crate has ever granted, taken deliberately — the sandbox denies egress and discards its workdir, which would make `npm install` impossible. The policy decides what may start, not what a started process then does. A policy written for file access does not constrain command execution, and `cat secrets/prod.env` is a command rather than a read. Stated in CONTRACT.md and in the command-execution guide; the residual risk is an operator who reads neither.

- **An exec timeout kills the direct child only.** A process that child started itself may outlive it, on every platform. This was the release's own open question about Windows and the answer is not Windows-specific: `kill_on_drop` reaches one process, and the crate has no Job Object backend to reach a tree with. It is recorded rather than papered over, and it is the same honest floor the sandbox already reports for its wall-clock kill.

- **The toolchain table will be wrong for someone.** It was already wrong once, in this release's own live run: npm's install default was `npm ci`, which requires a lockfile the fixture did not have, and the agent's first move was to run it and watch it fail. Fixed to `npm install`, but the class remains — half of all `npm test` scripts do not run tests, and a Python project with a `pyproject.toml` may really be driven by a Makefile. It is data shown to the model rather than commands the harness runs, so a wrong entry costs a turn and cannot widen a boundary. Overridable in 0.19.0; until then the workaround is the goal text.

- **Only the workspace root is examined for a marker.** A monorepo whose packages each carry their own `package.json` or `go.mod` gets one detection, for the root. A real case, deliberately not handled here; per-directory configuration is 0.19.0's.

- **An allowed exec writes no `policy_events` row.** Refusals and approver decisions do; a silent allow does not, for exec no more and no less than for a read or a write. "What actually ran" is answered by `steps.tool_call`, not by `policy_events`. Both the guide and CONTRACT.md claimed otherwise in draft and were corrected when the live run's trace showed it.

- **A consumer who skips 0.17.0 entirely meets the removal with no warning.** The three Rust verification variants are deprecated here and removed in 0.18.0, the minimum cycle, so an upgrade straight from 0.16.2 to 0.18.0 never sees the deprecation. The 0.18.0 changelog must say so plainly rather than assume a reader upgraded one minor at a time.

- **`Verification::Command` runs inside the sandbox, so a gate cannot reach the network.** A criterion that needs to fetch dependencies will not work; the install belongs in the agent's own `exec` calls, where egress is available, and the gate belongs on the command that tests.

- Carried unchanged from 0.16.2 and untouched by this release, as `scope.excluded` requires: `resume_tree` still does not call `record_run_policy`, so a tree resumed under a widened policy leaves an audit that understates what was permitted; `SandboxLimits::max_processes` is enforced by nothing on any platform; and the Linux backend installs no seccomp filter. Each needs a release that touches its module.


### 0.16.2 known limitations

- The record is sealed on `feat/0.16.2` before the crates.io upload, which follows the two merges and the GitHub Release. `mode: published` states the authorized target; the flow makes it true. O1 — the docs.rs build succeeding and the page rendering — can only be checked after the upload, since docs.rs builds from the published crate.

- Nothing in this repository can catch the next occurrence. The failing configuration is one no local or CI command builds, which is the whole reason two releases shipped the defect, and this release deliberately adds no nightly `--cfg docsrs` job to fix that: a workflow change has its own failure modes and its own matrix cost, and it does not belong in the release that exists to unbreak the page. It is the first thing the next release should carry.

- `doc_cfg` is itself an unstable feature and can be renamed or removed exactly as `doc_auto_cfg` was. Nothing about this fix makes the crate durable against that; it makes it correct against today's nightly.

- The CI matrix was not waited on, at the owner's instruction and on the stated grounds that this is a one-line change. The trade is narrower here than it was for 0.16.1: the edited line is inside a cfg that no configuration CI builds ever sets, so the matrix could not exercise it even if it ran. Every claim above rests on a local macOS run on stable, plus the nightly docs build.

- 0.16.0 and 0.16.1 stay without documentation on docs.rs permanently. Neither was yanked — both build, both pass their suites, and both are what a consumer is running; only their doc pages are missing, and yanking would be a disproportionate response the owner has not authorized.

- Everything 0.16.0 and 0.16.1 carried is carried still. The four defects documented in `docs/CONTRACT.md` — the reserved tool-name gap, `resume_tree` not recording its policy, `SandboxLimits::max_processes` enforced by nothing, and no Linux seccomp filter — are untouched, because each is a behaviour change and none belongs in a patch.


### 0.16.1 known limitations

- The record is sealed on `feat/0.16.1` before the crates.io upload, which follows the two merges and the GitHub Release. `mode: published` states the authorized target; the flow makes it true.

- The CI matrix was not waited on, at the owner's instruction and on the stated grounds that this is a text change. That is a deliberate, narrow trade: every claim above rests on a local macOS run, and the Linux and Windows legs will report after the merge rather than before it. It is safe here only because the release contains no code whose platform behaviour could differ — the same decision on a release with a `src/` change would not be.

- Nothing checks the product table. The five 0.16.0 checkers enforce the rest of the README, and this release deliberately adds no sixth: a product's status is not derivable from anything in this repository, so a checker for it would assert a fact it cannot verify. The table stays correct by someone remembering to update it when io-cli or io-studio ships — which is exactly the failure mode that produced this release, now with one fewer wrong row to inherit.

- `src/state.rs:1507` still names io-eval, as an implementation comment about why a schema field exists. Judged out of scope rather than missed: it makes no availability claim and appears on no rendered page.

- Everything 0.16.0 carried is carried still. The four defects its audit found and documented in `docs/CONTRACT.md` — the reserved tool-name gap, `resume_tree` not recording its policy, `SandboxLimits::max_processes` enforced by nothing, and no Linux seccomp filter — are all untouched, because each is a behaviour change and none belongs in a documentation patch.


### 0.16.0 known limitations

- This record was sealed on feat/0.16.0 before the crates.io upload, which follows the develop→main merge and the GitHub Release. `mode: published` states the authorised target; the release flow makes it true.

- A registered tool named after one of the git, image or document built-ins passes Toolbox validation and is then permanently unreachable. RESERVED_TOOL_NAMES still lists only the original seven, and dispatch tests every built-in arm before reaching the toolbox. Found while auditing the README against the source. Documented in CONTRACT.md rather than fixed, because extending the reserved set turns a currently-accepted registration into an Error::Config, which belongs in a release that can carry a break.

- `resume_tree` does not call record_run_policy, unlike run_tree and both flat loops, so a tree resumed under a widened policy leaves an audit that understates what was permitted. The new resume_tree_from_stored_policy is unaffected, since it reads that row back.

- `SandboxLimits::max_processes` is enforced by nothing on any platform. The doc comments claiming otherwise were corrected here; the field's behaviour was not changed.

- No seccomp filter is installed on Linux. Three doc comments claimed one and were corrected. The backend is namespaces and rlimits, and any syscall restriction is the kernel's own default under an unprivileged user namespace.

- The public-surface snapshot covers the 106 items re-exported from the crate root. Items reachable only through a public module path — io_harness::context::assemble, io_harness::tools::Workspace, the whole documents API — are public and are not individually snapshotted, so the deprecation cycle's mechanism does not yet cover them. Stated in CONTRACT.md.

- The worked-example check counts fenced blocks. It cannot distinguish a worked example from a token one, so the quality of the 106 examples rests on review rather than on the checker.

- Carried from earlier releases and documented rather than fixed: git_status uses --porcelain=v1 without -z, so a path containing a newline renders ambiguously to the model; commit idempotency across a resume rests on git's own semantics rather than a durable marker; fill_form's output has never been opened in a real PDF viewer, which needs Pdfium and therefore a system package; the Windows Job Object is still unimplemented, so containment there is the portable floor and the CPU and memory caps do not fire; pdf::read_text's catch_unwind is a no-op under a panic = "abort" profile; and no binary document fixture was written by a real application.

- The three-OS CI matrix had not run on this branch when the record was sealed. Every gate above was run on macOS. Linux and Windows are proven by the matrix on the feat→develop pull request.


### 0.15.0 known limitations

- Commit idempotency across a resume rests on git's own semantics rather than on a durable marker taken around the commit. A replayed `git_add` finds the file unmodified and stages nothing, so the replayed `git_commit` finds nothing staged and makes no commit. It is asserted by a test that replays the identical add-and-commit after a resume, and it holds — but the mechanism belongs to git, not to this crate's checkpointing, and a future git built-in that is not naturally idempotent would not inherit it.

- `git_status` uses `--porcelain=v1` without `-z`, so its output is newline-separated. A path containing a literal newline would be rendered ambiguously to the model. This is a display concern and not a boundary one — the path gate and the argv builder both handle such a path correctly, and a test asserts a path with a newline survives as one argv element — but the report the model reads would be wrong about it.

- `resume_from_stored_policy` covers the single-file and workspace loops. `resume_tree` has no stored-policy form, so a crashed agent tree still requires the caller to supply the policy.

- A viewed image rides exactly one request and is then dropped. That is deliberate — it is a tool result, not a permanent part of the conversation — but a model that needs to look again spends another step, and nothing tells it that in advance beyond the tool description.

- The crate never decodes an image. The media type comes from the file extension and the bytes are forwarded unparsed, so a corrupt file named `.png` reaches the vendor and comes back as an HTTP 400. Parsing it here would mean an image decoder in the default path of a crate that has one only behind the barcode feature.

- A run under a narrow write policy must allow `.git` or its commits are refused. The refusal is correct and traced, but the only signal is the refusal itself; there is no earlier or clearer error, and the requirement is documented rather than checked at configuration time.

- Git is local only, by design: no push, fetch, clone or remote, and no branch, merge or tag either. Commits land on the branch that is checked out. A caller who wants the agent on its own branch checks one out before starting the run.

- The live run used one provider, one model, one image and one repository. It exercises both halves end to end under a permissive policy; the refusals and their controls are covered by tests rather than by a live model.

- `fill_form` now generates real appearance streams for text fields, which closes 0.14.0's weakest claim at the level it can be closed without rasterising: the stream draws the value and the test asserts on the decoded bytes. It has still never been opened in a real PDF viewer, because proving that means Pdfium, which is a per-OS binary this release's no-system-package constraint forbids. Checkboxes and radios keep their existing state-selection behaviour; choice fields are drawn as plain text, single-line and left-aligned, and quadding, multiline and comb fields are not honoured.

- MCP images are attached only when the tool did not report an error, on the grounds that an image arriving with a failure is not something anyone asked to see. That is a judgement, and a server that reports an error alongside a diagnostic screenshot loses the screenshot.

- `CompletionRequest` gaining a field is source-breaking for a caller using an exhaustive struct literal. Under 0.x that is a minor bump, which is what this is, and the type now derives `Default` and documents `..Default::default()`, so this is the last time a field addition breaks it.

- Carried forward again, all three unrelated to this release's subject: the Windows Job Object is still unimplemented and still needs `windows-sys` under `[target.'cfg(windows)'.dependencies]` and its own release; `pdf::read_text`'s `catch_unwind` guard is still a no-op under a `panic = "abort"` profile; and the tree still carries no binary document fixtures written by a real application.

- The record is sealed before the crates.io upload, which follows the develop->main merge and the GitHub Release per the repo's release order. `mode: published` states the authorized target; the flow makes it true.


### 0.14.0 known limitations

- `fill_form` sets `/V`, sets `/NeedAppearances` and deletes the stale `/AP` rather than generating appearance streams itself, and it has never been opened in a real PDF viewer. `/NeedAppearances` is a request to the viewer to redraw the field; a viewer that ignores it will show the field blank. The test is named for what it proves — the value is set and the viewer is asked to redraw it — and F7's "renders it" is satisfied at that level rather than by rasterising a page.

- `pdf::read_text` wraps `pdf-extract` in `catch_unwind` because that crate is written to panic on input it does not understand. The guard depends on unwinding, so it does nothing under a `panic = "abort"` profile, and the default panic hook still prints the message before the error is returned. Sound, but unusual enough to name.

- The tree carries no binary document fixtures. The open question about where fixture documents live and how large they may be was answered by not having any: fixtures are generated in-test, and F3's "a file the test did not itself generate" is met by generating it with a *different* crate than the one under test. A workbook Microsoft Excel itself wrote is still not asserted against, and the strongest evidence for the preserving edit is the cross-crate test plus the live run, where three crates touch one workbook in sequence.

- The spreadsheet preserving-edit is `umya-spreadsheet`'s round trip. What that library models it keeps; what it does not model it can drop or normalise. Charts, drawings, pivot tables, slicers and macros are where that bites, and nothing here can tell a caller which of those a given workbook holds — so the limit is stated in the doc comment rather than enforced by a check that would pass on the workbook it could not understand.

- PDF text extraction is best-effort on reading order. A PDF stores placed glyphs, not a document: columns can interleave, tables lose their shape, and a scanned page returns an empty string rather than an error. Treat the output as evidence, not as the document.

- Word is generate-and-read with no in-place edit, and PowerPoint is read-only with no writer. Both are deliberate and both are documented at the call site; neither is a later release's task.

- `Verification` is not `#[non_exhaustive]`, so adding `DocumentContains` breaks a downstream `match` that has no wildcard arm. Under 0.x that is a minor bump, which is what this is, and the public-surface method — a grep for `pub` items — cannot see enum variants at all, so the addition is recorded in the diff's prose rather than in its counts.

- Document reads are bounded by the same context caps as any other observation, but a large document is rendered whole before the cap applies. The rendering choices per format are what keep that affordable — a sheet is tab-separated with a row/column gutter rather than a padded grid — and no streaming or paging reader exists.

- The live run used one provider and one model (OpenRouter, openai/gpt-5.6-luna) and one format. It exercises the spreadsheet path end to end under a policy and its negative control; the other four formats are covered by tests rather than by a live model.

- The record is sealed before the crates.io upload, which follows the develop->main merge and the GitHub Release per the repo's release order. `mode: published` states the authorized target; the flow makes it true.


### 0.13.0 known limitations

- F5 is satisfied one step removed from its own wording. The criterion asks for the restored ledger to be asserted equal to the ledger an uninterrupted run held "on the ledger itself, not only on the resulting trace". There is no API that hands a caller a running run's in-memory `Ledger`, and exposing one to satisfy a test would be the wrong trade, so the assertion is made on the two surfaces that ledger produces: the observation section of the prompt the model was sent, and the durable rows. The prompt comparison is the one that carries the claim, and it was verified to fail when the restore is removed. The durable-row comparison alone is vacuous — it was written first, found to pass with the restore disabled, and kept only for what it does prove, which is that the watermark does not re-write what the restore just read.

- F1's negative control is permanent rather than historical. The criterion described running the same scenario "through today's code path" to show the action performed. That path no longer exists after the fix, so the control is `resume_with` supplied with `Policy::permissive()` instead, which shows the same thing — the write lands when no boundary is supplied — and keeps working in every future release rather than only during this one.

- The store cannot be observed in the state where the new tables are genuinely absent. `Store::from_conn` runs every `CREATE TABLE IF NOT EXISTS` before it returns, so a query against a missing table is unreachable through the public API. What the back-compatibility test proves is that a 0.12.0-shaped file — both tables dropped, verified absent through a raw connection first — opens, migrates and resumes. "Tolerates missing tables" is not a state the code can be in, and the CHANGELOG does not claim it.

- The live run used one provider and one model (OpenRouter, openai/gpt-5.6-luna). It exercises the resume; the observer and recording paths are only incidental to it. A second provider would say more about portability and nothing more about this release's subject.

- The record is sealed before the crates.io upload, which follows the develop->main merge and the GitHub Release per the repo's release order. `mode: published` states the authorized target; the flow makes it true.

- The stored policy is written but there is no entry point that resumes from it without the caller supplying one. That was excluded deliberately — it would require deciding what happens when a stored policy and a passed policy disagree, and that question deserves a caller who has hit it.

- The public surface diffs published for 0.11.0 and 0.12.0 were generated with a pattern that missed every indented item and every `async` entry point. Their conclusions stand — both were separately asserted to remove nothing — but their item counts are not comparable to this release's. Corrected method and the reasoning are recorded in evidence/0.13.0/public_surface_diff.md.

- The Windows Job Object is still unimplemented; Windows containment remains the portable floor with a wall-clock cap only. It needs a `windows-sys` dependency scoped to `[target.'cfg(windows)'.dependencies]` and its own release.


### 0.12.0 known limitations

- The record is sealed before the crates.io upload, which follows the two merges and the GitHub Release. mode: published states the authorized target.
- A RESUMED RUN IS NOT THE SAME RUN. 0.7.0's resume restores the durable half -- step, spend, elapsed clock -- and drops the assembled half: the 0.10.0 context ledger is in memory only (src/run.rs:1183, :1758), so a resumed workspace or sub-agent run re-derives its context from the workspace and asks the model a different question. The agent usually recovers, which is why this went unnoticed through five releases. It matters beyond replay: anything scoring long unattended runs is scoring the runs most likely to have been resumed. Found by this release's replay work, recorded in iterations/US-IO-HARNESS-0.12.0-I01, and given a destination at roadmap 0.16.0. The single-file loop is unaffected -- its prompt comes from the file on disk.
- RESUME SILENTLY DISCARDS THE CALLER'S POLICY. `resume` substitutes Policy::permissive() and ApproveAll for any resumed workspace run, so a caller who ran under a restrictive policy via run_with and then resumed via resume loses that boundary without being told. The crate states the opposite principle elsewhere (src/run.rs:193-196: refusing a policy it cannot enforce rather than ignoring one), so this is a defect, not a design. This release closes the specific path a human's network refusal took, by making "refused" terminal. The general case changes a public signature and is outside this contract's scope. There is currently NO policy-preserving general resume: resume_with_decision takes a policy but requires a request_id and a pending Decision, so it only delivers an approval and cannot resume a run that merely crashed. Until it is fixed, re-run rather than resume when the boundary matters.
- Replay's guarantee is narrow and stated narrowly. It does not cover a diverged prompt (a reworded goal, an edited fixture, or an observation that differs produces an unrecorded request -- reported, never absorbed), duplicate identical requests across a process restart (the per-key cursor is in memory, so a resumed process re-serves the first answer for a key whose ask was really the second), or ordering past a key's recorded answers (the last one repeats). An overstated determinism claim is worse than a narrow one.
- canonical_trace assumes each run being compared has its own fresh store. Run ids are excluded from the text, but a child agent's run id is embedded in its parent's composed observation, which is real content the model was shown.
- Cancellation requested on the LAST step of a budget has no next boundary, so that run reports StepCapReached rather than Cancelled. It stopped anyway.
- A panicking observer takes the run's future with it and leaves the run row running. event() returns no Result on purpose -- an observer is a spectator and must not be able to fail a run -- but a panic is not absorbed, and that is documented rather than hidden.
- Observer::event is called synchronously on the run's own task, so a slow observer slows the run. Documented; the contract is to push to a queue and do the work elsewhere.
- Store::open now sets journal_mode = WAL, which is a persistent property of the DATABASE FILE, not of the connection. A store opened once by 0.12.0 stays in WAL afterwards and rolling back to 0.11.0 does not undo it. 0.11.0 reads WAL happily; it simply never set it.
- Containment::max_total_cost remains unenforceable and is now documented as inert rather than left looking functional. A provider reports tokens and never a price, so any figure compared against would be one the harness invented. Bound spend with max_total_tokens.
- Spend is tokens everywhere in the outcome record. There is still no cost telemetry, so a scoring tool comparing money must convert at its own provider's rate.
- The live-run evidence O7 asks for was NOT produced: no run against a real provider was made this session. The observer, replay and record paths are covered by fixtures including real local sockets, a real stalling socket, a real sandboxed rustc and a real stdio MCP server -- but that is not the same claim as a live model run, and it is recorded as not done rather than counted as done.
- The README's factual errors are corrected but its structure is unchanged: it is still organised as a per-release history rather than a landing page. That rewrite, with the per-capability depth moved into docs/ guide pages, is deliberately 0.15.0's subject so the documentation describes a finished twelve-pillar harness once instead of being redrafted after each pillar.
- Windows containment is still the portable floor with a wall-clock cap only, and Backend::WindowsJobObject remains a public variant nothing constructs on the enforcement path. Unchanged since 0.9.1; it needs a windows-sys binding and its own release.
- Every Windows claim in this record rests on CI run 30209627766. Nobody on this project has a Windows machine.

### 0.11.0 known limitations

- The record is sealed before the crates.io upload, which follows the two merges and the GitHub Release. `mode: published` states the authorized target.

- Stall detection is a heuristic over two signals, not a proof. It reads whether a write changed the file's bytes and whether a tool call repeated. An agent making real progress the harness cannot see — reasoning, or work inside a registered tool whose effects are outside the workspace — could in principle be flagged, which is why the window is configurable and can be set to 0.

- The stall signature is the joined tool-call text, so two calls that differ only in an irrelevant argument read as different work and reset nothing. An agent that varies a padding argument while achieving nothing would not be caught.

- `with_stall_policy` is inert for a single-file run, which has one tool and one file. Documented on the builder rather than silently accepted.

- The request deadline is 600s by default. Long enough that no realistic single completion reaches it — a full 8192-token stream at a sluggish 15 tokens/second is about nine minutes — but a caller running a slower model or a much longer output should raise it. A hung socket is caught by any finite deadline, so the default is deliberately generous rather than tight.

- Backoff is deterministic, with no jitter. Many clients retrying in lockstep after a shared outage would synchronise; jitter needs a randomness dependency this release does not take, and would make the wait assertions untestable without loosening them until they assert nothing.

- Falling over swaps the model mid-run and the harness cannot see whether the replacement is as capable, as well tuned, or even the same size. The provider that answered is recorded per step so an operator can tell, but nothing verifies equivalence, and a run that fell over may behave differently from one that did not.

- Nothing tracks a provider's health across runs. A provider that failed every call last run starts the next one trusted; a circuit breaker needs a store surface and an expiry policy and is a different feature from surviving the failure in front of you.

- Honouring `Retry-After` is not rate-limit management. There is no token bucket and no quota model, so a run that is being throttled will keep asking and keep waiting.

- `Retry-After` handles delta-seconds and IMF-fixdate only. RFC 850 and asctime dates degrade to "no hint" rather than to a wrong wait, marked `ponytail:` with the upgrade path named.

- The malformed kind fires only when NOTHING in a stream parsed. A provider that emits a valid but truncated response — half the tool call, then a clean end — is still `Ok`, and looks to the loop like a model that changed its mind.

- Windows containment is still the portable floor with a wall-clock cap only, and `Backend::WindowsJobObject` remains a public variant nothing constructs on the enforcement path. Unchanged since 0.9.1; it needs a `windows-sys` binding and its own release.

- Every Windows claim in this record rests on CI run 30202128038. Nobody on this project has a Windows machine, and this release is the second time in three releases that CI found a real Windows-specific behaviour nothing local could see.


### 0.10.0 known limitations

- The record is sealed before the crates.io upload, which follows the two merges and the GitHub Release. `mode: published` states the authorized target.

- The token figure the assembler enforces against is an ESTIMATE — four chars per token, computed in-crate, no tokenizer dependency. The trace records it beside the provider's reported usage for the same request, and the live runs show the estimate running roughly 55-75% of what the provider charged for the whole request (which also carries the system prompt and tool schemas the estimate does not). The ceiling is therefore a bound on the observation section, not on the whole request.

- Assembly bounds size and applies exactly two staleness rules. It does not promise the model sees everything relevant: an observation older than the current window is a stub, and a stub the model does not act on is information it does not have. The README says so.

- A stale read is re-read on every subsequent turn, since history is immutable — so one `reread` trace row and one disk read per turn per stale path. Correct, and worth knowing before reading row counts.

- Memory is a small keyed set the agent writes deliberately: 64 entries, 16,000 chars total, 2,000 chars per entry, oldest-first eviction. There is no retrieval, no ranking and no semantic recall, so a workspace with more to remember than that loses its oldest notes rather than its least useful ones.

- A note one run writes is read by every later run over that workspace. Entries are attributed, bounded, rendered as the agent's own notes rather than as directives, and listable and deletable through `Store` — but a wrong or planted note persists until someone removes it. This is the release's main new trust surface and the README states it plainly.

- Recency ordering for memory is millisecond-resolution, so two notes written in the same millisecond tie and fall back to insertion order. Deterministic and correct for the caps; a monotonic sequence column would be the fix if it ever matters.

- Both 20-step live runs ended in `StepCapReached` with the model re-reading the same four files without writing. The observations were correct and current every turn, so this is a stalled agent rather than a context defect — precisely what 0.11.0's stall detection and bounded replan are scoped to catch. Observed here rather than predicted.

- Windows containment is still the portable floor with a wall-clock cap only; the Job Object needs a `windows-sys` binding and its own release. Unchanged from 0.9.1, and the manifest description — which still claimed "Windows Job Objects" as a shipped backend — was corrected in this release to say what Windows actually enforces.

- The `Backend::WindowsJobObject` enum variant remains in the public API from 0.9.0 while nothing constructs it on the enforcement path. 0.9.1 made sure no trace claims it; removing the variant is a public break and belongs with the release that implements it.

- The context section's ceiling bounds observations and recalled notes. The task contract's own goal, constraints and success criterion are always sent in full, by design — they are the task, not history — so a pathologically long goal string is not bounded by this release.


### 0.9.1 known limitations

- The record is sealed before the crates.io upload, which follows the two merges and the GitHub Release. mode: published states the authorized target.

- Windows containment is the portable floor with the wall clock only. No CPU, memory, or process caps, and no kernel network boundary. The Job Object needs a Win32 binding and is deliberately not in a patch release; this is the fallback scope the contract planned for, chosen rather than stumbled into. It is documented in src/sandbox.rs, src/sandbox/windows.rs and the README, and asserted by a test so the documentation cannot drift away from the behaviour.

- On a Linux host that restricts unprivileged user namespaces — Ubuntu 24.04 and its derivatives, which includes GitHub's own runners — io-harness now degrades to the portable floor and has NO kernel network boundary; network denial there is proxy-env stripping only. This is strictly better than the previous behaviour, where every gate simply failed, and the degradation is reported in the trace rather than claimed away. It does mean a Linux operator who needs a hard network boundary must run on a host that permits user namespaces.

- The glob cache is unbounded, holding one compiled regex per distinct pattern. Patterns come from configuration rather than from the model, so this is bounded in practice; a caller that generates pattern text at runtime would want an LRU.

- `cpu_capped` still matches SIGXCPU only. With the hard limit now above the soft one that is the signal Linux sends, but a process killed by SIGKILL for an unrelated reason is still not attributed to a cap, which is correct and worth stating.

- Windows was verified only through CI. Nobody on this project has a Windows machine, so every Windows claim in this record rests on run 30195506996 rather than on local reproduction.

- The verify gate's per-platform behaviour is now asserted, but the 9 integration binaries had never run on Linux or Windows before this release. Two further defects surfaced the moment they did. A third wave is possible as more of the suite is exercised on more platforms.


### 0.9.0 known limitations

- The record is sealed before the crates.io upload. mode: published states the authorized target; the upload itself is the last step of the initorigin release flow and follows the feat->develop and develop->main merges and the GitHub Release, all of which the owner performs by hand.

- Linux: 9 lib tests fail (CI run 30191362799). 7 in verify::tests, where passes_guarded(...) returns false — the 0.3.0 execution-based verification gate does not work on Linux at all. 2 in sandbox::tests, where the CPU and memory caps never fire: cpu_cap_kills_a_busy_loop_and_names_the_cpu_cap observes `SandboxOutcome { backend: PortableFloor, exit_code: None, cap_hit: None }` against an expected Some(Cpu). These are defects in the released 0.3.0 and 0.6.0 pillars, not regressions from this release, and they were undetectable before because the repository had no CI. Issues #25 and #26; scheduled into 0.9.1.

- Windows: the suite does not terminate. verify::tests all pass there, then memory_cap_kills_a_heap_hog_and_names_the_memory_cap fails and the job produces no further output for roughly sixteen minutes until the 20-minute cap cancels it — the behaviour expected when a cap never fires and the capped process is therefore never killed. Issue #27; scheduled into 0.9.1.

- Containment on Windows is the portable floor only. src/sandbox/windows.rs creates no Job Object and calls no Win32 API; it computed its JobLimits and dropped them. 0.9.0 corrects the false claim rather than the gap: the Windows path now reports Backend::PortableFloor instead of Backend::WindowsJobObject, and the module docs, the sandbox module docs, linux.rs and the README no longer claim enforcement that does not happen. The real implementation needs a Win32 binding (a new runtime dependency, which NF1 forbids here) and lands in 0.9.1. Consequently 0.6.0's promise that "the same harness isolates code the same way on mac, linux, and windows" is not yet true, and the crate now says so.

- The declared MSRV was wrong from 0.8.0 onward. 0.8.0 and 0.8.1 are live on crates.io declaring rust-version 1.87 while depending on rmcp 2.2.0, which needs 1.88 for let-chains and declares no rust-version of its own. 0.9.0 corrects the declaration going forward; the published 0.8.x manifests are not corrected, since a published version cannot be edited. Neither is yanked: both build fine on 1.88+, which is what anyone building them today has.

- NF3 holds with less headroom than the number suggests. The 0.442 ms debug dispatch figure is dominated by Policy::check(Act::Exec, name) recompiling its globs to a regex on every call (the standing ponytail: note at src/policy.rs:384) — the tool layer's own share is about 9 us debug. The debug margin against the 1 ms bound is therefore 2.2x, not orders of magnitude, and it scales with the number of exec rules in the policy. Caching compiled globs is the fix if it ever needs to grow, and it belongs to the policy, not to this release.

- Single-file mode gets no skills catalogue. It has one tool and no tool layer, the same line with_mcp and with_tools already draw.

- A skill's relevance is entirely the model's judgement. The harness tells it what exists and never ranks, matches, or auto-injects a body; automatic selection is a context-construction question and belongs to 0.10.0.

- A registered tool runs in the harness's own process with the embedding program's privileges. The policy governs whether it is called, not what it does once running — the same bound 0.8.0 states for a stdio MCP server. Stated in lib.rs, the README, and the CHANGELOG rather than left to be discovered.

- TaskContract has public fields and is not #[non_exhaustive], so the new tools and skills fields do break struct-literal construction. The public API is additive in every other respect (42 items added, 0 removed, 0 signature-changed) and the type documents the builder as the supported path, which is the pattern 0.8.0 set when it added mcp the same way.


### 0.8.1 known limitations

- A passing execution gate is evidence that the stated criterion was satisfied. It is not proof the artifact is correct, and this release does not make it one — a gate asserting `hello() == 42` stays silent about everything else the file does. Hardening the gate against the subject makes the evidence trustworthy; it does not widen what the evidence covers.

- A subject that legitimately defines a macro named after a prelude macro the criterion invokes (`assert`, `assert_eq`, and the rest of the re-imported list) now fails to compile with E0659 rather than passing. This is the shadowing defence working as designed and it cannot distinguish a hostile redefinition from an innocent one. A macro under any other name is unaffected.

- Verification costs more compiler spawns: the compile-only gates went from one to two and the test gates from two to four. On the reference machine that is roughly 29 ms to 59 ms and 290 ms to 381 ms. A caller running a large fan-out of gated agents pays this per gate, and `EachCompilesRust` pays its share per listed file. The reverted two-crate structure was cheaper; it was also wrong.

- The record is sealed before the crates.io upload. `mode: published` states the authorized target and the publish step is the last in the sequence — feat→develop merge, develop→main merge, GitHub Release, then `cargo publish`. If the upload fails, the correction is a forward patch, not an edit to this record.

- F5's serde half is vacuous: `Verification` has no serde derive, so there is no representation to round-trip. The criterion was written expecting one. The substantive guarantee — a 0.8.0 caller's `Verification` values compile unchanged — holds and is verified.

- The `#![cfg(any())]` vector was found by reproducing the recorded bypass, and the compile-only hole by reviewing gates the contract predicted were safe. Both were cheaper to find than the bypass that started this release. No systematic search for further vectors against the gate was performed, so the honest claim is that three known classes are closed, not that the gate is exhaustively hardened.

- The offline suite passed 186 tests against a build that failed the live run, because every test wrote `pub fn hello` and inherited the structure's own assumption. The regression is now pinned, but the general lesson is not: a structural defence must be checked against honest code that does NOT resemble the tests already written for it.


### 0.8.0 known limitations

- This record was sealed on feat/0.8.0 before the crates.io upload, because the repo's release order puts publication last — after the feat→develop and develop→main merges and the GitHub Release. `mode: published` is the contract's authorized target and the sequence the owner directed, not a claim that the upload had already happened when the file was written.
- F9 is met in substance but not as literally worded. The contract says a server whose binary does not start is "handled by the 0.2.0 retry/recovery path"; it instead fails the run with a typed Error::Mcp at session construction, because connecting happens before step 1, retrying a missing binary cannot make it appear, and proceeding without a configured capability is the worse failure. The other three legs — tool error, per-call timeout, and a server killed mid-run — do go through the loop as recoverable observations, each with its own test.
- NF2 is proven indirectly. The whole pre-existing suite, including the 0.5.0 fan-out tests, runs green with the network act live, and the MCP session is shared by reference through Tree rather than per agent — but there is no dedicated timing benchmark asserting that egress checking does not serialize a 100-agent fan-out. Same shape of limitation 0.7.0 recorded for checkpointing.
- The harness governs the connections it opens, not the connections a running stdio MCP server opens for itself. That server is a separate process: the harness decides whether it may start (an exec check on the binary) and which of its tools may be called (an exec check on the namespaced name), and nothing more. Isolating a server's own egress needs OS-level containment, which is not built here.
- Calling an MCP tool needs a SECOND exec allowance, on the namespaced tool name, beyond the one that lets the server binary start. This is deliberate — it is what lets a policy admit a server and still refuse one of its tools — but it is easy to miss: the first live run connected the server, discovered its tools, offered them to the model, and refused every call. Documented in README and in examples/mcp_run.rs, but the failure mode is quiet.
- MCP resources, prompts, sampling, roots, and elicitation are not supported — tools only. Neither is OAuth: static credentials pass through configuration (headers, environment), but there is no interactive authorization flow. Both are excluded scope, not defects.
- Egress policy decides WHETHER a host may be reached, not how much may flow to it. There are no byte, rate, or bandwidth ceilings, no payload inspection, no TLS pinning, and no proxy or DNS-level enforcement — the check is in-process at the point the connection is opened.
- Redirects are now disabled on every built-in provider's HTTP client, to close the checked-then-redirected hole. A provider endpoint that legitimately relies on a 3xx would now surface it as a non-success status rather than following it.
- The live run used a single-agent workspace run. A live run of a 0.5.0 agent tree with MCP tools is not part of this release's evidence; the tree path is covered offline by the fan-out suite running green with the session wired in.
- In the live run the model satisfied the criterion by writing "echo:" rather than the full server reply. The criterion passed as written, and that the tool's output does reach the next prompt is pinned separately by an offline sentinel test — but the live artefact is thinner than the goal text asked for.
- MSRV rose from 1.75 to 1.87 and reqwest from 0.12 to 0.13, both forced by rmcp 2.2.0. Callers on an older toolchain cannot take 0.8.0.

### 0.7.0 known limitations

- NF5 concurrency has no dedicated 100+-agent timing test asserting non-serialization. Each agent commits its own step independently (per-agent unchecked_transaction, no new global lock over the 0.5.0 single-connection model), and the existing subagents fan-out suite now runs green with checkpointing default on — but "checkpointing does not serialize the fan-out" is proven indirectly, not by a dedicated concurrency benchmark.
- resume_tree_with_decision re-checks the approved write against the tree (root) policy, not the resumed child's narrowed policy (not reconstructed at the decision point; marked with a ponytail comment). A policy deny still blocks, and a remembered-allow cannot defeat a deny beneath it, but a child-specific deny of an action the tree policy allows is not re-enforced on that one performed write.
- The live example run happened to solve in a single step, so it demonstrates durable-store + idempotent-resume across a real process boundary rather than a live mid-run crash-and-continue. The mid-run continue path is proven deterministically and offline by the real-SIGKILL resume test; a live provider run that is genuinely interrupted mid-task is not part of this release's evidence.
- The literal 24h+ wall-clock unattended run is out of scope by design (contract excluded scope). The horizon is proven by the real-SIGKILL resume test plus a 250-step long unattended run that checkpoints throughout; a real multi-hour run is not executed here.
- Resume is single-machine only — checkpoints are the local rusqlite store and resume runs on the same host (product.yaml: local, single-user). No distributed or cross-machine checkpoint/handoff.
- The crate ships the durable-run mechanism and the trace; there is no daemon, scheduler, or watchdog that re-invokes the process after a crash — who restarts the process (a shell, io-cli, io-studio, systemd) is the caller's job, per the embeddable-library / no-daemon constraint.

### 0.6.0 known limitations

- The Linux (namespaces + seccomp) and Windows (Job Object + restricted token) native backends are compiled and unit-tested on the macOS build host but NOT live-run on real Linux/Windows hardware. A full target-triple cross-check is additionally blocked only by rusqlite's bundled-C cross toolchain (an environment limit, not a limit of the sandbox code). The portable floor is real on all three OSes, so isolation is never absent — but the native backends' runtime behaviour on their own OS is unverified here.
- macOS does not enforce address-space rlimits (RLIMIT_AS/RLIMIT_DATA — verified empirically), so the memory cap is an RSS poll-and-kill monitor (~40ms sampling), not a hard kernel limit; a very fast allocation spike could briefly exceed the cap before the monitor kills it.
- RLIMIT_NPROC is deliberately NOT applied by the portable floor — it is per-real-uid, not per-sandbox, so capping it there would throttle the operator's whole login session. Per-sandbox process-count limiting exists only in the native pid-namespace (Linux) and Job-Object (Windows) backends.
- Network is deny-by-default only. A configurable egress allow-list (letting a run reach a named host) is 0.8.0. On the portable floor and the Windows backend, "network denied" is best-effort (proxy env stripped), not a kernel boundary; Linux (net namespace) and macOS (sandbox-exec profile) deny for real.
- The portable floor is the weakest backend by design — filesystem-scoped (a fresh ephemeral workdir) and resource-capped, NOT a full syscall jail. It is the guaranteed-present minimum, not a substitute for a native backend where one exists.
- Isolation is in-process on one host using OS-native primitives and a portable floor — no container, microVM (firecracker/gVisor), or remote/multi-machine sandbox, per product.yaml (local, single-user, embeddable library).
- The Windows Job Object backend maps SandboxLimits to Job-Object fields (unit-tested) but the actual Job-Object syscall enforcement is not exercised here; on Windows the backend currently provides wall-clock capping, output capture, and kill-on-drop via the shared path, with full Job-Object enforcement to be verified when live-run.
- T08 sibling-independence test gap (accepted by owner): "a child sandbox start-failure does not take down siblings" is proven indirectly (typed Error::Sandbox start-failure unit test + run_tree's existing child-error isolation shown by the subagents suite), with no single tree+start-failure injection test, because making one child's rustc unspawnable is not reachable through the public API.

### 0.5.0 known limitations

- No execution sandbox — children still compile model-produced code directly on the host (the risk carried since 0.2.0), now multiplied by the fan-out factor. 0.5.0 bounds what the tree may touch and spend, not where code runs; per-run isolation is the very next release, 0.6.0.
- No whole-tree crash-resume. 0.5.0 reuses 0.4.0's single-pending-action persistence; a process restart does not resume an in-flight fleet. Full tree checkpointing is 0.7.0.
- Concurrent siblings share one workspace with last-write-wins — a decomposition that fans out onto overlapping files can corrupt state before 0.6.0's per-run sandbox brings real write isolation. The trace records both writes; the parent is expected to decompose to avoid collisions.
- The aggregate ceiling overshoots slightly by design: when the tree budget is exhausted mid-flight, in-flight children finish their current step before stopping, so no child is left with a half-applied edit. The small overshoot is recorded honestly in the trace rather than prevented.
- The spend ceiling is denominated in tokens as the hard, provider-agnostic ceiling; max_total_cost is an optional derived estimate only, and max_total_duration is optional wall-clock.
- No per-agent provider or model selection — the whole tree runs under the provider given at root construction (deferred, not committed for 0.5.0).
- No inter-agent coordination beyond parent<->child spawn and compose-back — no sibling messaging, shared mutable blackboard, or agent-to-agent RPC. Coordination is through the parent and the shared workspace only.
- The aggregate budget ledger is a single shared lock — deliberately the one point of serialization, adequate for one-host in-process fan-out and not tuned for throughput beyond that.
- mode is release-ready, not published: 0.5.0 is built, tested, documented, and packaged but not uploaded to crates.io; publish is deferred to the release/merge stage by owner decision, as for 0.1.0-0.4.0. cargo publish --dry-run passes and CARGO_REGISTRY_TOKEN is available.
- `Containment` field names are now a cross-repo serde contract the moment io-cli and io-studio adopt it, like `Policy` in 0.4.0; neither app has adopted it yet, so the composition value is unrealised until they do.

### 0.4.0 known limitations

- Single-file mode is not policy-enforced. A policy only applies to workspace contracts (TaskContract::workspace). Passing a non-permissive policy with a single-file contract now returns an error rather than running unenforced — this was found by the completion gate, where it had been a silent no-op, and fixing it was release-blocking. Extending enforcement to the single-file loop is outstanding.
- A refused action can still waste a run. Refusals count against the step budget (a decided trade-off), so a model that repeatedly retries a denied action is bounded by the step cap but achieves nothing. Confirmed live: with no guidance the model retried a denied read on steps 2, 4, 7 and 8 and hit StepCapReached without doing the work. The mitigation today is a TaskContract constraint telling the model not to retry refused actions, which turned the same task into Success{steps:4}. A repeated-refusal early stop is the named follow-up.
- Rules remembered before a Defer are not persisted with the pending action. They are returned on RunResult::remembered, so a caller can merge them into the policy it passes to resume_with_decision, but the crate does not do this for it — a caller that ignores the field silently loses them on resume.
- The model is not told when approve-with-changes altered its action (matching Claude Code's behaviour). The loop re-reads the workspace each step so it observes reality eventually, but it may waste a step believing it wrote a path it did not.
- The command policy gates the binary name only, not arguments. Sound today because every argv element is harness-constructed (src/verify.rs) and no model or caller output reaches it; the full argv is traced. Argument gating becomes required in 0.6.0 when plugins/MCP can supply argv.
- Which form is remembered when an approver both rewrites and remembers is the approver's choice — the crate applies exactly the rules handed to it and does not derive them from either the requested or the performed action.
- The default is permissive: a caller who passes no policy gets no enforcement. Deliberate for 0.3.0 compatibility, but it means 0.4.0 ships a boundary nobody gets unless they opt in, and neither io-cli nor io-studio has adopted it yet — the release's value is unrealised until they do.
- Policy layering is composed, not discovered: the crate merges a stack it is handed and never reads a config file. Where the shared base lives, and in what precedence, is unsettled across io-cli and io-studio.
- mode is release-ready, not published: 0.4.0 is built, tested, documented and packaged but not uploaded to crates.io; publish is deferred to the release/merge stage by owner decision, as for 0.1.0-0.3.0. cargo publish --dry-run passes and CARGO_REGISTRY_TOKEN is available.
- Execution-based verification still compiles model-produced code locally with no sandbox (sandboxes are 0.5.0), inherited from 0.2.0. The command policy constrains which binary runs, not what it may do.
- Policy glob matching compiles a regex per check and matches full relative path or basename; adequate for a handful of rules per tool call, not tuned for large rule sets.

### 0.3.0 known limitations

- mode is release-ready, not published: 0.3.0 is built/tested/packaged but not yet uploaded to crates.io; publish deferred to the release/merge stage (owner decision). cargo publish --dry-run passes and CARGO_REGISTRY_TOKEN is available.
- Live provider proof is OpenRouter-only — the sole live key. The Anthropic and OpenAI request-build and stream-parse are unit-tested offline against synthetic wire fixtures; neither has been run against its live API. Cross-vendor live parity is unproven (matches the contract's Anthropic-only fallback and the logged key/quota risk).
- Agent working memory is an observation log folded into the user turn, not native multi-turn message history; long read_file/grep results are truncated to the last 4000 chars in that log.
- grep/find honor a fixed ignore list (.git, target, node_modules), not .gitignore; the walk is synchronous std::fs (fine for local repos, not tuned for huge trees).
- Multi-file execution-based verification is Rust-only (EachCompilesRust / WorkspaceTestPasses); other languages have no multi-file gate.
- Workspace resume reuses the files on disk as state and restarts the observation log; the Anthropic request caps max_tokens at a fixed 8192.
- Execution-based verification still compiles model-produced code locally with no sandbox (sandboxes are 0.5.0), inherited from 0.2.0.

### 0.2.0 known limitations

- 0.1.0 was never published to crates.io (it landed release-ready), so 0.2.0 is the crate's first version on the registry; a 0.1.0→0.2.0 upgrade was never exercised against crates.io itself.
- Execution-based verification runs model-produced code locally with no sandbox (sandboxes are 0.5.0); a hostile or buggy artifact could affect the workspace.
- The live model run covered one model (openai/gpt-5.6-luna) and one simple task; broader model and task coverage is unproven.
- The cost budget is counted in tokens, not currency — OpenRouter carries no reliable per-request price.
- Only Rust execution-based gates exist (CompilesRust, RustTestPasses); other languages fall back to content checks.
- Resume reuses the file on disk as the run's state; if the file is changed out of band between interruption and resume, resume continues from the changed file.

### 0.1.0 known limitations

- Not published to crates.io. target_mode was published; owner chose to stop at release-ready. Publish is the remaining step (`cargo publish` with CARGO_REGISTRY_TOKEN); crate name `io-harness` confirmed available.
- The end-to-end test uses a mock provider for deterministic offline CI. A real OpenRouter model run was not executed; the live path exists only in examples/edit_file.rs and is unproven against a real model.
- Verification is deterministic substring/exact-match only; no schema or model-judged verification.
- Single file per task, single agent, single provider (OpenRouter). No budgets beyond a step cap, no retry/recovery, no permissions, no human approval — all roadmap.
- No default OpenRouter model; OPENROUTER_MODEL must be set by the caller.


_Canonical sources: products/<id>/releases/<version>.yaml_
